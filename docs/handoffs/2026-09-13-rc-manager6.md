# Handoff — rc-manager6 → rc-manager7, 2026-09-13

Tree clean, everything pushed. `origin/work` = `79485a0`. **`master` is untouched at `3327bfc`** —
nothing has been promoted today and that is the standing rule, not an oversight.

---

## 1. READ THIS FIRST: the tree is not on master

`git rev-parse --abbrev-ref HEAD` is **`infra-audit-fixes-2026-09-13`**, not master. A **second agent
outside this fleet** (Claude Sonnet 5, session `01DM4PnMENSxVc3RV8KhXaSr`, not in maestro) created that
branch in the shared working tree at 14:14 and committed CI workflow, `.nvmrc`, a Node pin and a
staging-environment note. **It was still running when I checked**, so I deliberately did NOT switch the
branch back — yanking it out from under a live session is exactly the harm this fleet avoids.

Nothing is lost: every commit is on `origin/work`, which is what the push gate checks. **Do not
`checkout`, `reset` or `stash`.** Whether that branch merges to master is the user's call and is the
one decision I left open.

---

## 2. Three user rules given today. They are in `CLAUDE.md`; this is the summary.

- **NEVER STOP WORKING.** Finishing the brief is not the end of the turn. **And an OFFER to continue is
  the same stop** — I closed a turn with *"continuing unless you want me somewhere else"* and was
  correctly called on it. If the next task is known, the turn ends with it DONE or IN PROGRESS.
- **CLOSE TABS WHEN THEY ARE NOT NEEDED.** `maestro archive <id> --force`; there is no `maestro close`.
- **A MANAGER SPAWNS LANES; IT DOES NOT SERIALISE THEM.** I burned hours doing lane work in my own tab
  with four veins open. The constraint that makes parallel safe is **disjoint paths**, not fewer lanes.

---

## 3. The fleet, and what each lane owns

| lane | id | state |
|---|---|---|
| `rc-kw` | `fbb1d2ed-786d-4d28-a2cb-9e215777cd5a` | **done, idle — needs a task** |
| `rc-gap` | `70f43aa8-92af-47f7-ace9-05c3e3e4685b` | working, holds no files |
| `rc-synth2` | `8c3f5677-3989-4c89-87a3-c895d8fbbaf0` | working, holds `scripts/adversarial-check.mjs` |

**Ownership is declared and must stay declared.** The manager holds `CLAUDE.md`, `data/combos.json`
and `data/synergies.json`; lanes report findings and the manager applies them. A lane needing a
contended file gets a **formal handoff** (*"it is yours until you hand it back; I am not editing it"*)
and hands it back explicitly. That protocol worked — rc-gap refused to touch `synergies.json` without
one, and said so.

All three lanes are excellent: they measure before claiming, state their predicate, refuse to report
numbers they cannot defend, and have each corrected their own published work unprompted. **Take their
findings seriously and still verify the load-bearing ones — I caught nothing wrong in their work, but
verifying is why that is worth saying.**

---

## 4. Outstanding, in priority order

1. **`.scratch-synth2/staged.md` TEXT A** — the empty-Main-Deck citation for the EIGHT INFINITEs that
   genuinely need it. Drafted, verified, **not applied**. TEXT B was withdrawn by its author because my
   `ff4d576` already covers the six that do not. `.scratch-*` is gitignored — read it from disk.
2. **39 emphasis-added defects in `CLAUDE.md`** (rc-gap report 13). Found with a **case-insensitive
   discriminator**: a passage matching the source case-insensitively but not case-sensitively is a pure
   emphasis defect and **cannot be anything else** — zero false positives by construction. Probe:
   `.scratch-gap/case-only.mjs`. **The fix is not to stop shouting** — move the emphasis outside the
   quotation marks. **One needs a human**: *"The first time ANOTHER friendly unit dies each turn"* is
   the file quoting 383.1.b's example precisely to show it DIFFERS from `OGN-118`'s printed text. Keep
   the observation, move the emphasis into the prose.
3. **The Plaza family is measured against one Energy** — 19 of 23 entries using `OGN-293` name only
   `OGN-133 Flurry of Blades`; seven mass answers exist and four reach a Might-2 body, plus
   `VEN-133 Glowstone` which is in no entry. rc-synth2's play 2 has the list.
4. **The #159 Azir refusal is partly reopened** by today's uniqueness correction. Nobody has re-walked
   the arithmetic. It is marked open in `CLAUDE.md`.
5. **~40 of rc-gap's 48 drafted bullets** remain unapplied in `.scratch-gap/bullets.md`. I applied 8
   **deliberately** — `CLAUDE.md` is 530 KB and loads every session, so it takes what a session should
   not have to re-derive and the rest live in the committed walk document. **That selection is a
   judgement to keep making, not a backlog to drain.**
6. **rc-synth2's diagnosed-and-unpatched clock defect**: `deployTurn` prices every card against the
   rune curve, which is right for a BURST and **wrong for an energy engine** that funds itself. A fix
   needs a model of when the loop turns on. Deliberately not attempted.

---

## 5. What changed today that a successor must not re-derive

**Five populations carry quoted rules text. Four are now checked; the fifth is measured.**
`combos.json` (since #202), `synergies.json` (**references were verified and quotations never** —
fixed today), the 87 walk documents (**nothing** — now `test/walk-quotes.test.ts`, a **ratchet at 75**,
not an assertion of zero), `docs/plays/` (26 of 27, and it is rendered on the live site), and
**`CLAUDE.md` itself, which nobody had ever checked** — six defects, now 0 of 164 rules-claiming
passages. One of the six was **the exact defect the file documents in another bullet** (437.4's doubled
*"that all of that"*), and it was the source a walk-document copy had been taken from.

**The ratchet earned its keep within the hour**: repairing `CLAUDE.md` pushed walk-quotes 75 → 76,
because a walk quote had been passing **only against CLAUDE.md's identical error**. A propagated
misquote was self-validating.

**The counting-trap family is now FIVE members**, all the same shape and all producing a confident
wrong answer that looks like a finding: a preceding hyphen (`OGN-304`), a preceding `#` (issue
numbers), a trailing dot (a parent absorbing its block **and being hidden by its own child**), a bare
number colliding with prose quantities / line numbers / lane labels / **the catalogue's own entry
count**, and a first-match preview showing a cross-reference tail. **The blind spot grows**: as the
catalogue passes 800 entries the fourth will eat rules 767–829 one at a time.

**Two bugs I shipped and then caught**, both worth knowing: the turn-clock fix set a new `unit` flag in
`costsOf()` and not in the `entries` loop that actually feeds the clock — **two places built the cost
objects and I patched one**; and my first `LOCATION-GATED` bucket reproduced this project's own
recorded precedence error by flattening a Hold line. **Both were caught by hand-walking a turn table,
not by a test.**

**A passing `vitest` run is not evidence the code typechecks** — 125 tests green over code that did not
compile. The three-leg gate catching the mirror of the case it was built for.

---

## 6. Verification

`npm test && npm run typecheck && npm run build:web`, and read all three. **Under fleet load the full
suite reports DOM timeouts that are not failures** — several 5s DOM files pass in isolation. A hang
reports as a worker crash with neither pass nor fail, which is why the gate is exit code **and** the
absence of `failed` **and** a `Tests N passed` summary line. When the machine is loaded, run the named
files instead and say so.
