# rc-manager11 handoff — 2026-09-19

## READ THIS FIRST: what NOT to spend time on

- **Do not re-report that the diagram does not change when you move "Within N cards".** It is
  correct. `cappedHits` sorts closest-first and keeps `TRAY_LIMIT` (7), and widening the distance
  only adds combos that are FURTHER away, so the nearest seven are identical and the SVG is
  byte-identical while the pill moves 17 → 33 → 62. I investigated it as a bug through the real UI
  and it is not one. The docblock now says so.
- **Do not re-derive the Run plays decision or the four items in rc-manager10's handoff.** Both
  still stand.
- **Do not "fix" the audit panel by editing `data/combos.json` prose.** The strip is at build time
  on purpose; the authored file keeps its full trail.
- **`.strix-gate` is real and strix cannot run here** — no LLM key is exported and no scan has ever
  been run for this repo. That is not a thing to work around; it is German's call.

## 1. State

- Branch `infra-audit-fixes-2026-09-13`, HEAD **`85dd241`**. `origin/master` = `origin/work` =
  `88470b1`. **Twelve commits ahead, nothing pushed. Production is still stale.**
- Gates at handoff: **typecheck 0 · 707 tests / 51 files · `build:web` 0.** Tree clean.
- Everything rc-manager10 left uncommitted is now committed, in six coherent commits.

## 2. The finding that mattered most

`web/main.ts` renders every entry's `notes` in a panel titled **"How this entry was audited"**, and
127 of 771 entries carried sentences addressed to another agent — `NOTE FOR THE MANAGER, not a wrong
verdict:`, `at rc-manager5's direction after this lane raised it rather than staging it
unilaterally`. `data/combos.json` was byte-identical to master, so **it was live on the site**. Same
defect the run plays were pulled for, in a corpus nobody had looked at.

Stripped at BUILD time (`scripts/web-combo-prose.mjs`), because `web/main.ts` *imports* the JSON and
esbuild inlines it — a render-time filter would hide the panel and still ship the bytes.

**The file list was short TWICE**: covering `combos.json` missed `synergies.json`; covering both
missed `data/features.json`. It now walks every string in every `data/*.json` that `web/main.ts`
imports, and the test derives that list from `web/main.ts` rather than repeating it. Twelve cases —
two combo `name`s and ten `sources[].title` — are single lines with no sentence to remove and were
repaired by hand instead, under a guard that proved entry count, id order and every other field
unchanged.

## 3. QA actually completed (local build, real browser)

The Playwright **MCP failed to connect**, but `playwright-cli` is a separate verified path and works
headless. `vercel dev` self-recurses via the project's Development Command — serve `public/` with a
static server instead, and build with `SUPABASE_URL=` to get the documented `data-auth="open"` mode.

| Item | Result |
|---|---|
| Near misses + "Within N" | Works, monotonic 17 / 33 / 62. Diagram deliberately unchanged, see above. |
| Radial graph mode | Works — 26871 → 16970 bytes, new viewBox. |
| Deck-code paste | Works. Generated a real code from `test/fixtures/lux.txt`; same 6 combos. |
| Keyboard / focus order | Passes. Solid 2px ring on every button and link; graph `<g>` uses `outline: none` and strokes its own frame, which `test/a11y.test.ts:110` pins as the one allowed exception. |
| URL import | Empty-URL guard works. Error path works and re-enables. **Happy path still untested** — it needs `/api/deck-url`, which only exists on a Vercel deploy. |
| Console | Clean throughout, apart from my own probe's 404s against a server with no `/api`. |

**The stale-request race fix is validated end to end**: with the request stalled by a route, all
three buttons read disabled mid-flight and enabled after.

## 4. Open, and genuinely needing German

1. **The deploy.** Twelve commits including a data-loss fix, a dead-app fix and the deploy fix
   itself. Blocked by `.strix-gate`, which cannot be satisfied here.
2. **HSTS `includeSubDomains`** — unchanged from rc-manager10's handoff.
3. **Neon.** Ceded to `panel3` (soluciones-panel). rc-manager12's spec is `81412d7`,
   `docs/superpowers/specs/2026-09-19-neon-migration.md`. **Two things gate it**, both verified by me
   against the repo rather than taken on trust:
   - The decision note says the four RLS policies become "un where user_id en la app". **That is a
     security regression.** `supabase/migrations/20260905120000_decks.sql` says in its own header
     *"Isolation is RLS, not a server: the browser talks to PostgREST directly with the anon key"*,
     `web/privacy.html` promises isolation is *"enforced in the database itself by row-level
     security"*, and CLAUDE.md says the anon key is public by design. `api/deck-url.ts` exists but
     only proxies deck-link imports. A where clause in the browser is enforced by nobody.
   - `web/privacy.html` promises deletion happens *"at once and for good; there is no copy kept and
     no waiting period"*, and Neon's auth deletion is documented as a soft delete setting
     `deleted_at`. That is a published promise, not a wording preference.

## 5. Traps paid for today, beyond rc-manager10's list

- **`git checkout <file>` on a file that also carries uncommitted work destroys it.** I wiped my own
  new tests that way while cleaning up a probe. Back up to `.scratch-*` first.
- **A post-commit review's findings are worth verifying and were 3 for 3 real here** — but the third
  was latent, not live, and saying which is part of the report.
- **A hook can match the text of a MESSAGE, not an action.** The strix gate fired on a `maestro send`
  whose body merely contained the words for pushing. Same class as the `pkill -f` trap.
- **`grep -c` counts LINES**, and `public/app.js` is one minified line, so it returns 1 for anything
  present. Use `grep -o | wc -l`.
- **My first count was too big every single time**: 433 → 127 on the leak, and a "bug" in the
  near-miss diagram that reading refuted. Four "bugs" investigated today, one real.
