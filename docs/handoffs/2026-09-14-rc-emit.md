# rc-emit handoff — 2026-09-14

Lane `rc-emit`, one session. Paths owned: `scripts/adversarial-check.mjs` and its tests
(`test/adversarial-holes.test.ts`, `test/turn-clock.test.ts`). I never touched `data/combos.json`,
`data/synergies.json`, `CLAUDE.md` or `docs/status.md` — those are the manager's.

Issues closed: **#220** (the dead notable emitter), **#222** (the emitter reading its own output).

---

## 1. The state of the instrument

`scripts/adversarial-check.mjs` has eleven modes. What matters to a successor is which of them can be
TRUSTED to mean something when they come back empty, because most of this session was spent
discovering that an empty result is not a result until it can say WHICH empty it is.

| mode | what it does | state |
|---|---|---|
| *(default)* | hole sweep + turn clock + report | live; 0 unanswered holes of 80 finishers |
| `--turns` | the turn clock | live; **exact on all 80 rows** |
| `--engines` | ENGINE deployment clock | live |
| `--strict` | exit 1 if any finisher has an unanswered hole | live, exits 0 today; **nothing runs it** |
| `--emit-notables` | corrections for unanswered holes | **dormant** — prints `[]`, states its denominator |
| `--recheck-notables` | replace a stale shipped notable | **[SPENT]** — 0 stale, 14 carry the replacement |
| `--holds-notables` REPLACE half | replace a false shipped notable | **[SPENT]** — 1 entry carries the replacement |
| `--holds-notables` APPEND half | name a garrison answer | **live** — 4 rows at handoff |
| `--holds` | garrison floor + answer per Hold finisher | live; sampled |
| `--stalled` | board-state classification, six buckets | live; sampled |
| `--selftest` | allocator ordering, synthetic | live |
| `--selftest-holes` | hole sweep + notable emitter, synthetic | **new this session** |
| `--selftest-garrison` | garrison floor, REAL entries, hand-derived | **new this session** |

### The three state tokens — the most reusable thing here

A count with no denominator and an empty result with no explanation are the same defect, and this file
now answers both the same way: **a stable bracketed token, with the prose beside it free to be
reworded.** Grep for the token, never for the sentence.

- **`[SPENT]`** — a one-shot repair that has already run. It prints 0 *because it worked*, and the
  positive evidence (the count of entries carrying its replacement) is printed with it, plus a date.
- **`[INDETERMINATE]`** — the needle finds nothing AND the replacement is nowhere either, so something
  changed underneath the mode and its 0 is evidence of nothing at all.
- **`[SCORES NOTHING: produces X]`** — the row is a fuel engine, so a board verdict about it is a
  malformed question and the reader is routed to the consumer.

There is a fourth, `CANNOT DETERMINE`, in `garrisonFloor`: a null floor is reported, counted and named
rather than guessed, because **a floor that is too LOW is worse than no floor** — it names a confident
answer that does not work.

---

## 2. The sampling method, and what is UNCHECKED

Verdicts were never checked against a hand-derived board before this session — only denominators. The
method: take a **stratified** sample so every LABEL is tested rather than only the biggest bucket,
derive the board from printed card text, and **report a disagreement before deciding which side is
wrong**.

**Running total: 22 of 80 finishers hand-derived, 27%.**

| bucket | size | sampled | notes |
|---|---|---|---|
| ATTACK | 8 | 2 | 1 row carries a scoring leg outside the gate (rider added) |
| CONQUER | 18 | 2 | 2 rows likewise |
| HOLD | 46 | 12 | biggest bucket; drives the most-quoted figure in the project |
| LOCATED | 4 | 2 | clean |
| GARRISONED | 2 | 2 | new bucket, both rows are its reason for existing |
| INDEPENDENT | 2 | 2 | clean after the label ruling |

**WHAT IS UNCHECKED, which matters more than what is checked:**

- **`--holds` verdicts are only part-sampled.** I started that sample, found one defect immediately
  (§3), and handed off. **34 of 46 HOLD rows are unread**, and `--holds` prints a garrison floor and a
  named answer for every one of them.
- **`--emit-notables` has never been exercised on real data at all**, because every hole is answered.
  Its two arms are covered only by `--selftest-holes`.
- **`--engines` and `--strict` have not been sampled in any way.**
- **The `--holds` header is STALE.** It still says the token half is "a TEXT SCAN of the entry's prose,
  so it is a FLAG that says read it, never a verdict". That has been false since the garrison repair —
  it now reads the board. **I ran out of room before fixing it; it is the first thing to do.**

---

## 3. My mistakes, and what caught each one — read this hardest

**THE FAILURE MODE, stated once, because it is worth more than the six fixes below.** Every defect this
script has produced had **CORRECT CARD FACTS and a WRONG PREMISE ABOUT THE BOARD** — a phantom Mech read
out of its own prose, a phantom Bird read out of a tag choice, a floor read off a body at BASE, a printed
Might read off a self-scaling body, a protection that cannot reach. The one exception is the last, which
had correct facts and a wrong sentence wrapped around an **ABSENCE**. Five were caught by **reading**;
one by a self-test built minutes earlier. **A successor who knows that has a direction to look in; a list
of six fixes gives them nothing.**

**I committed #220's own defect inside the test for #220.** The assertion matched `/SPENT, not silent/`
— prose I had *intended* — while the script printed "is SPENT rather than silent". A consumer matching a
producer's prose, in the test written for exactly that. *Caught by:* the test going red immediately.
*Fix that generalises:* the state tokens above. **It then happened three more times** (a `# (\d+)`
prefix that had the mode's name in between; a label wording I pinned and then changed). Prose is not an
interface. **Key on a token, or on the distinctive tail, and write the regex from the printed line in
front of you rather than from the line you meant to print.**

**My first `--selftest-garrison` pinned ONE clause of four.** I only know because I broke each clause in
a scratch copy and watched nothing go red. *Caught by:* deliberately breaking things, not by writing
tests. **A self-test is not validated by passing; it is validated by failing when it should.** The three
cases added afterwards are the sharp ones — Rumble playing a Mech *to your base*, Ivern *mentioning*
Bird, Spiderling self-scaling.

**My first repair was too strict and measuring caught it.** I required the entry's steps to name the
token as well as a card playing it; that lost `UNL-044 Flurry of Feathers`, whose only text is playing
four Bird tokens. *Caught by:* re-measuring against known positives before believing the result.

**Three over-counts in one pass, all caught before shipping.** 5-of-8 → 1-of-8 (Draven's "They score 1
point" is the OPPONENT scoring); 10-of-18 → 2-of-18 (the gate must be PER BUCKET, or a bucket's own gate
reads as a second leg); 53-of-80 → 10-of-80 (I guessed a `points` tag; the vocabulary says
`ability-points` and `burst-points`). *Caught by:* **printing the distribution before writing the
predicate.** That is the only order of operations that did not over-count for me today.

**And the worst one, because it was my own repair failing in its own way.** After fixing the emitter for
reading its own prose, my replacement used a proximity regex without the word `token` — so `UNL-177
Ivern, Friend to All`'s *"As you PLAY me, choose BIRD, Cat, Dog, or Poro"* became a Might-1 Bird, and
four entries got a phantom floor of 1. *Caught by:* **hand-sampling `--holds`**, not by any test.
Anchoring on `token` then broke the base exclusion in the same edit, because both clauses read one match
window — *caught by `--selftest-garrison`*, the first time a test caught one of my regressions.

---

## 4. What I deliberately did NOT do — these are decisions, not omissions

- **I did not delete or generalise `--recheck-notables`.** It is spent, but the SHAPE is live: notables
  here have shipped wrong at least four times. Generalising means parameterising a thirty-line
  replacement constant with interpolated measurements and a bespoke `why` — an interface nobody asked
  for. It was made self-explaining instead.
- **I did not widen any predicate to make a mode find work.** The manager asked for this explicitly and
  it is the right instinct: a mode that goes looking produces exactly the over-counts in §3.
- **I did not pin `holes == 0` as a test.** A new finisher with a hole is a legitimate finding to act
  on, not a defect, and pinning it would block another lane's merge. The DENOMINATOR is pinned instead,
  so "0 of 80" cannot become "0 of 0" unnoticed, and the count cannot drift from the printed list.
- **I did not leave the equipment arm parsing its own label.** It recovered the copy count with a
  regex over a string it builds three hundred lines earlier — #220's shape one notch down. It carries
  `u.quantity` now. Latent rather than live (no card name contains `x<digit>`), and measured: reword
  the label and the old code names the single-copy answer for all 33 rows, 24 of which need the mass
  answer.
- **I did not touch `data/combos.json`.** Corrections are handed over as
  `.scratch-emit/notables.json`, `[{entry, notable}]`, for the manager to apply with the #204
  discipline.

---

## 5. The two open questions, and how the manager ruled

- **INDEPENDENT's label** — ruled: change the word, not the bucket. It is **STALL-INDEPENDENT**, which
  is what the mode measures; `bottled-constellation-time-warp` needs three friendly bodies to kill, so
  removal answers it while a stall does not. **Applied.**
- **Fuel engines in the finisher population** — ruled: **keep the rider, do not move the population.**
  `class` describes the SHAPE of a line and INFINITE describes the LOOP, not the payoff, so ten
  INFINITEs can produce only fuel without being miscategorised. Redefining FINISHER would mean
  redefining a field 767 entries use to fix a question one mode asks. **Applied.**

---

## 6. Two convergences worth not re-deriving

Both are results nobody arranged, and each is two derivations meeting that never touched each other.

1. Adding the GARRISONED bucket left INDEPENDENT holding exactly `bottled-constellation-time-warp` and
   `lux-infinite-power` — and CLAUDE.md states, from a hand walk that never touched this code, that
   exactly ONE finisher both scores and needs nothing from the board, the other being a fuel engine
   that scores nothing.
2. A structured predicate over `produces` and a prose one over `terminatesIn` agree that **10 of 80**
   finishers score nothing, differing on exactly one row for a nameable reason
   (`gemdragon-henge-vi-blind-fury`'s prose mentions points its CONSUMER scores). CLAUDE.md's
   hand-derived figure is ten of the fourteen INFINITEs.

---

## 7. Immediate next actions, in order

1. **Fix the stale `--holds` header** (§2). It describes the defect that was removed.
2. **Finish the `--holds` sample** — 34 of 46 HOLD rows unread, and that mode prints a floor and a named
   answer for each.
3. **Hand the 4 pending `--holds-notables` rows over** once read: `ivern-sentinel-hold`,
   `ivern-arena-sentinel-hold`, `ivern-arena-trinity-body-order-hold`,
   `ivern-arena-draven-chaos-order-chain`. All four moved from a phantom floor of 1 to 4 or 6, so their
   named answer changed from Flurry of Blades to `OGN-123 Unchecked Power`. **Read them before handing
   them over** — this emitter has produced three false notables and every one was a premise about the
   BOARD rather than about a card.

## 8. Environment notes that cost me time

- `cp` is aliased to `cp -i`: a scripted backup prompts and **silently does nothing**. Use `/bin/cp -f`.
- zsh does **not** word-split an unquoted `$var`, so `node $S $m` passes `"--a --b"` as ONE argument and
  silently runs default mode. Use `${=m}`. This produced a false diff that looked like my patch had
  moved 40 turn rows.
- `noclobber` is on: `cat > existing` fails. Use `>|`.
- `rm -f` / `rm -rf` are blocked by a hook. Use `python3 -c "shutil.rmtree(...)"`.
- `npm test` green does **not** mean `tsc` is green, and test files are where the two disagree.
- A red test in a file you do not own is usually another lane mid-edit: re-run at a committed sha in a
  throwaway worktree before reporting it.

---

## 9. Appended after the handoff was first pushed — the `--holds` sample, continued

Two more defects, both found by **reading**, both the same family: the card facts were right and the
premise about the board was wrong. That family has now produced **four** false notables in this one
script, and not one of them was caught by a test.

**A tag choice is not a token.** `--holds` gave `ivern-sentinel-hold` a garrison floor of M1 whose
battlefield bodies are Might 6 and 4. The phantom body came from my own repair: `UNL-177 Ivern, Friend
to All` reads *"As you PLAY me, choose BIRD, Cat, Dog, or Poro"*, and a proximity regex without the word
`token` read that as a Might-1 Bird. Anchoring on `\btokens?\b` fixed it and **broke the base exclusion
in the same edit**, because both clauses read one match window — `--selftest-garrison` caught that in the
same run and named the row.

**A protection is an answer only if it REACHES.** Both Hold modes printed *"THE IDENTITY DOES HOLD AN
ANSWER"* and listed **+1 Might** grants beside `OGN-123 Unchecked Power`, which deals **12**. Every
garrison-wide protection in this pool is +1 or +2 (105 and 11 instances), so nothing in the pool saves a
garrison from that card. The gate is arithmetic — `floor + plus` must EXCEED the damage — and it fires
in both directions: at floor M0 against 1 damage it drops the +1 grants too. `VEN-130 Aurok General` was
excluded in the same pass (Empowered-gated, so inert on any token garrison; CLAUDE.md had recorded him
as inert and the sweep had never been told).

### The four rows now pending in `.scratch-emit/notables.json`

`ivern-sentinel-hold`, `ivern-arena-sentinel-hold`, `ivern-arena-trinity-body-order-hold`,
`ivern-arena-draven-chaos-order-chain`. All four moved from a phantom floor of 1 to a real 4 or 6, so
their named answer moved from Flurry of Blades — which kills nothing there — to `OGN-123 Unchecked
Power`, and none of them now claims a protection. **I have read all four against their boards.** The two
earlier rows (`svellsongur-trevor-leblanc-plaza`, `sprite-mother-burst-leblanc-plaza`) were applied by
the manager and are confirmed landed.

### The thing I said I had NOT done — done before handoff

**DONE at 54d84b6.** Left below as written, because the sequence is the point: it was identified while
reading the pending rows, recorded as an omission, and then closed rather than bequeathed.

The emitted garrison notable **never said its answer is symmetric.** `OGN-123 Unchecked Power` reads
*"Exhaust all friendly units, then deal 12 to ALL units at battlefields"* — it kills the caster's board
too. The equipment arm's notable says exactly this about Thermo Beam ("symmetric, which makes it cheap
for a gearless deck and expensive for anyone else"); the garrison arm quotes the text and never draws
the conclusion. That materially changes whether an opponent would cast it, and it is the next thing I
would fix. Measured over the seven swept answers: four are symmetric (Unchecked Power, Flurry of Blades,
The Ruination, Tibbers) and three say "enemy units" (Firestorm, Bullet Time, Alpha Strike). Both halves
are now stated, since "enemy-only" is information too.

### Running sample after this pass

**26 of 80 hand-derived, 33%.** HOLD is the bucket still most unread: **34 of 46**.

---

## 10. The `--holds` floor sweep — a clean result that explains itself

Two checks over all 43 rows `--holds` prints, and **no defect in either**. Recorded because a sweep that
comes back empty is only worth reporting when it can say WHY, and both of these can.

**Every floor re-derives: 43 of 43.** Stated precisely, because the strength of the claim matters — this
is a **re-implementation** check, an independent second coding of the same rules. It catches a coding
error and it cannot catch a MODELLING error, which is what every defect in §3 and §9 actually was. The
modelling is covered by `--selftest-garrison` (hand-derived floors on real entries) and by the hand
sample, not by this.

**The author's prose against the card-derived floor: 2 flags, both false positives, one shared cause.**
This is the stronger check, because the two sources are genuinely independent — an author's steps and a
sweep over `cards.json`. Both flags are `ivern-sentinel-hold` and `ivern-arena-sentinel-hold`, whose
prose claims a Might-1 body while the floor says M4. Reading them: the prose is the manager's own
repaired notable, which says the Might-1 body is `OGN-216 Soaring Scout` **at zone BASE**, where the
sweeper cannot reach it — and the floor excludes it for exactly that reason. Both are right and they
answer different questions.

That is the **right file, wrong sentence** shape this project already records: the predicate matched a
sentence that refutes the reading in its own words, the same way a `plaza-armory-miss-fortune` check once
matched "Sand Soldier" inside a cross-reference saying *this is not that*.

**I did NOT ship the prose cross-check as a test**, and that is a decision. It needs an exclusion for
"prose discussing a body the floor deliberately excludes", which is the second narrowing of a
prose-grounded check — and this file's own rule is that a rules-grounded check finds things and needs one
narrowing, while a prose-grounded one finds nothing and needs several. Two flags, both explained, no
defect: that is the prose signature exactly. The re-implementation check is likewise not shipped, because
`--selftest-garrison` already covers the part that matters.

### Final sample state

**26 of 80 finishers hand-read (33%).** All 43 `--holds` floors machine-re-derived and prose-cross-checked.
HOLD remains the bucket with the most unread rows for the sentences OTHER than the floor — the named
answer, its price and its reachability are hand-checked for 12 of 43.

---

## 11. The rest of what `--holds` prints — audited, and one more empty-state defect

The floors were clean (§10), so this pass took the OTHER sentences the mode prints per row.

**Clean, over all 43 rows:** every named answer REACHES its floor, and every printed price matches
`cards.json`. Zero defects on both.

**One defect, and it is the smallest member of this script's family.** Five rows said *"no swept mass
answer reaches it"* — and every one had a floor of **M?**, CANNOT DETERMINE. A reader was told the
garrison is too big for everything in the pool when the truth is that its size is unknown. No card fact
was wrong; only the sentence wrapped around an absence. Same repair as `[SPENT]` against
`[INDETERMINATE]`: **an empty result has to say WHICH empty it is.** The original message remains for
the case it was written for — and today no row has a floor genuinely beyond the pool's biggest sweep
(12, `OGN-123 Unchecked Power`), which is itself worth knowing.

**A probe of mine nearly produced a false finding, and the tell is reusable.** My first parse counted
"37 of 43 name an answer" and I was one step from reporting *"6 rows name no answer"*. That number was a
**parse artifact** — my regex did not match the refusal lines. Printing the UNPARSED lines rather than
trusting the count showed all six were the mode correctly refusing. **When a count implies an absence,
print the rows that produced it before believing the absence.**

### Where the audit stands now

| what `--holds` claims per row | checked how | result |
|---|---|---|
| the garrison floor | machine re-derivation + prose cross-check, all 43 | clean (§10) |
| the named answer reaches the floor | arithmetic, all 43 | clean |
| the printed price | against `cards.json`, all 43 | clean |
| the protection reaches | arithmetic, all 43 | **fixed** (§9) |
| the empty states | read all 6 | **fixed** (§11) |
| the Signature carve-out | **NOT CHECKED** | open |
| whether a CHEAPER reaching answer was skipped | **NOT CHECKED** | open |

The last two rows are the honest remaining gap in this mode.

---

## 12. SUCCESSION — to `rc-emit2`

Handed over at 76% context. Everything of mine is committed and promoted; nothing is pending from me.

### In flight at this instant

**I had just started #218 and had read `src/bodies.ts` end to end without measuring anything yet.**
That reading is the only thing in flight and it is worth having:

- The file is **85 lines** and its docblock is unusually good — it states its own weakness, cites
  107.4.b and 107.4.d for why a legend is not one of these bodies, and records that citing 107.4.c
  alone argued the opposite. Read it before touching it.
- **The flattering paths I had identified by reading, none yet measured.** `Math.max(0, …)` is floored
  with no mechanism to over-report (documented). `held` counts every unit card in `main` +
  `battlefields`, so it counts a unit that is banned in the requested format (the manager's question 2,
  exactly one card: `OGN-177 Stealthy Pursuer`), and it would count a unit mis-parsed into the
  battlefields bag — which the tournament corpus is known to contain (`vancouver-06`, `utrecht-06`).
  Whether that second one is reachable is **unmeasured**; I would check it, because a transcription
  erratum putting a unit in the wrong bag is precisely a wrong premise about the board.
- **The measurement I would have run first**, and the reason: #218's "24 of 24 report a shortfall of
  zero" is over the suite's own FIXTURES. There are **222 real registered tournament lists** in this
  repo and `test/tournament-lists.test.ts` already drives them. Running the body check over those is a
  far stronger population and it answers "does this check ever fire in practice" on real decks rather
  than on constructed ones. That is where I would start.

### What I would take next, asked and answered

The manager offered me two honest options and I would pick neither first. **I would do #218 as briefed**,
because it is my own failure mode in a different file — *correct card facts, wrong premise about the
board* — and because unlike the sampling work it has a stated, bounded deliverable.

Between the two I was offered: **widening the hand sample past 26 of 80 is the lower value now.** The
`--holds` audit table in §11 has a tick in every row, `--stalled`'s buckets have been corrected four
times and the last correction moved only three rows, and the marginal sample row is finding less each
pass. **Going outside this script is the better use of a fresh lane** — the instrument is in good repair
and the catalogue is not.

### Probes on disk that matter

All under `.scratch-emit/` (gitignored). Nothing here is load-bearing; **re-derive rather than trust
any of it**, since several were written against catalogue states that have since moved.
`verify.sh` is the one worth reading: it runs every mode of the script into a directory so two versions
can be diffed byte-for-byte, and it is how every refactor this session was proved output-identical. Note
it uses `${=m}` deliberately — see §8.

### The one habit I would ask you to keep

Every defect I found was caught by **reading the output**, not by a check — and the two checks that did
catch something were ones I had built minutes earlier and then deliberately tried to break. **Before
believing a self-test, break each clause it claims to pin and confirm it goes red by name.** Mine pinned
one clause of four and I only learned that by trying.

---

# rc-emit2 — 2026-09-14

Succeeded `rc-emit` in the same lane. Two pieces, both committed and pushed: **#218 answered**, and
the last two NOT CHECKED rows of §11's audit table closed. Paths touched: `src/bodies.ts`,
`test/body-requirements.test.ts`, `test/dom/body-requirement.dom.test.ts`,
`scripts/adversarial-check.mjs`, `test/adversarial-holes.test.ts`. Nothing of the manager's.

## READ THIS FIRST — what NOT to spend time on

**The instrument is in good repair and the catalogue is not. Go outside this script.** rc-emit wrote
that at handover and I spent a session testing it from the inside: both of my findings here were
one-line COUPLINGS — a pin asserted in one state of two, and two arms of one mode agreeing by
coincidence — and neither was substance. That is what a well-repaired instrument looks like when you
keep digging. **The run-play vein is where the value is; it is also the half of #200 closest to what
the user actually asked for.**

Four specific things NOT to do, each a decision rather than an omission:

- **Do not sample `--holds` further.** rc-emit hand-derived 26 of 80 finishers and closed the audit
  table's last two rows here. Every sentence that mode prints per row is now checked by machine, by
  hand, or both, and the marginal row found less on each pass.
- **Do not widen a predicate to make a mode find work.** rc-emit refused this when asked directly and
  so did I. `--emit-notables` printing `[]` and `--strict` exiting 0 are CORRECT states. A mode that
  goes looking produces exactly the over-counts both of us spent the day correcting.
- **Do not "fix" the `max()` merge to `sum()`.** It would over-report every pair whose legs genuinely
  share a body. The per-pair question is named in the test's own words for whoever authors the first
  live case.
- **Do not file a follow-up issue for the parse-failure over-report.** It is a documented
  approximation with a named instance and a test pinning its population. A second issue would pad a
  backlog rather than record a defect.

And one thing that IS worth a successor's time, because it is the only claim here a reader will
otherwise dismiss: **#218's check is vacuous in the median and ONE BODY from firing at the tail.**
478 of 478 silent reads like decoration until you see `barcelona-31.txt` sitting at slack 0 with
exactly two unit cards in its Main Deck. Do not re-derive that measurement — it is at corpus scale
and it did not move when the catalogue did.

## What a successor should DO

1. **Nothing in this lane is half-done.** Both deliverables are committed with their controls; there
   is no staged artifact waiting and no measurement left dangling. `.scratch-emit/` holds probes;
   **re-derive rather than trust them.**
2. **`--strict` still has nothing running it.** rc-emit recorded that and it is still true. It is the
   one mode with an exit code and no consumer.
3. **`--emit-notables` has still never been exercised on real data**, because every hole is answered.
   Unchanged from rc-emit's §2.
4. The `--holds` audit table in §11 now has **no NOT CHECKED rows**.

## Finding 1 — #218: it flatters in the arithmetic and can over-report on the INPUT

**Measurement.** #218 answered its own question over three fixtures (24 of 24 silent). Over the 222
registered tournament lists instead — `matchDeck` at `maxMissing: 2`, included + almostIncluded,
constructed — **478 matched routes across 190 of the 222 lists carry a body requirement and 478 of
478 report a shortfall of ZERO** (re-measured at `434015a`; it read 477 of 477 at 769 entries, and
the conclusion did not move when the catalogue did). Twenty times the population, same answer.

**The half "nearly vacuous" hides, and it is the useful one.** Slack per row (`spare - count`): **min
0, p10 5, median 16, max 30**, with 11 rows at 2 or less. One row is at **exactly zero** —
`barcelona-31.txt` against `targons-peak-defy-delayed-ready+amateur-recital-free-evacuation`, spare 2
and needs 2 — and that list is an Azir deck with **exactly two unit cards in its Main Deck**, every
other body a Sand Soldier token. **The check is vacuous in the median and one body from firing at the
tail, and the tail is the shape it was written for.**

**Control.** The one over-reporting mechanism is on the INPUT side and is reachable today: `held`
reads the parsed bags, so a unit line the parser could not resolve is invisible. `sydney-07.txt`
carries `x2 Adaptatron` in `deck.unresolved` and `OGN-056 Adaptatron` is a UNIT — two unit copies the
check cannot see, on a real registered list, latent by a margin of 14 there.
**`test/tournament-lists.test.ts` pins those four unresolved lines by name, so it cannot grow
silently.** Two paths that look reachable and are not, checked so nobody re-opens them: a unit cannot
enter the `battlefields` bag (both construction paths classify by CARD TYPE before the section
header) and the champion is never outside `main` (both paths put it there) — **structural, with 0 of
222 as corroboration rather than proof.** Equivalents double-counting in `committed` would
OVER-report: 0 of 1,641 variants.

**Two corrections #218's own body needs.** There are **TWO** banned units, not one — `OGN-177
Stealthy Pursuer` and `SFD-020 Draven, Vanquisher`, both formats, measured two ways that agree
(`legality.json`'s own `entity` field, and the card's type); 0 of 222 lists hold either. And the
coverage argument is **`deckRestrictions`, not `hit.illegal`**: `hit.illegal` reads only the LINE's
cards, so it cannot see a banned unit sitting in the list as a **spare body**, which is exactly the
flattering case. `deckRestrictions` reads main, battlefields, runes, sideboard and the legend. **The
ruling stands; the reason under it was one step off.**

**Q1, the `max()` merge — still zero, and STRUCTURALLY so.** `expand` folds a second entry in through
one mechanism, `combo.needs`, and **zero of the 83 `anyBodies` entries declares `needs`** (33 entries
in the whole catalogue declare one at all). So "no pair has co-occurred" understates it: there is no
pair to co-occur. **The first live case is ONE authored field away** — a `needs` on one of the three
tags both needed by somebody and produced by a body-requiring entry, with **28 distinct eligible
inner contributors** behind them (`conquer-engine` 8, `resource-engine` 19, `token-body-engine` 1,
disjoint). Not pre-emptively summed, per the issue.

**The pin, and the gap it closed.** #218 names two surfaces that state the requirement whether or not
the list is short — the drawer and the route node — and **the node was asserted only for a list that
IS short, which is exactly the state a conditional would still satisfy.** `renderGraph` already
receives the hits, so the "simplification" is one line. *Control:* applying it turns the new test red
and leaves **all six existing ones GREEN**; both clauses (badge and accessible title) were broken
separately and each goes red by name.

**And the half of the merge that IS live was not pinned at all.** 68 variants fold a body-requiring
entry in with one carrying no requirement. `bothBodies` returning `undefined` there — the "tidy the
optional field" edit — silently disarms all 68, **and the existing ratchet is blind to it by design**,
because it reads the contributing entries rather than the variant's field. *Control:* proved in a
throwaway worktree at HEAD, since `src/combos.ts` is another lane's file — the new test goes red, the
ratchet stays green, 17 of 18 pass.

## Finding 2 — the two Hold arms agreed by coincidence, three cost-points wide

**Measurement.** `sweepMassAnswers` returns the array sorted by **damage**. `--holds` took the first
reaching card and printed it as the **"cheapest answer"**; on **23 of the 37** rows with a computable
floor the price-order cheapest is a different card (`OGN-268 Bullet Time` at 5 against `OGN-123
Unchecked Power` at 9). `--holds-notables` sorted by price and split Signature off. `--holds` did
neither and had **no Signature carve-out at all**.

**It named the right card anyway on all 37, which is why nothing caught it.** Against the right
comparand — the cheapest **non-Signature** answer, since 103.2.d.2 binds a Signature card to one
legend — damage order and price order agree **37 of 37**. *My first number was 23 of 37 and the right
comparand made it 0; the predicate overstated exactly as this project says it will.*

**Control, and it is the whole point.** `OGS-018 Tibbers` IS a non-scalable Signature answer at dmg 3
and loses the dmg-3 tie to `OGS-002 Firestorm` on cost alone — **E8+P2 against E6+P1, three points.**
Repricing Tibbers below Firestorm, changing nothing else, makes the old selection name a Signature
card on **SIX rows**, none of which name it — so `--holds-notables` would have written that claim into
`data/combos.json`.

One selection now serves both arms. **Byte-level control:** the named card is identical on all 37
rows; `--holds-notables` is byte-identical but for a PID in a temp path; and default, `--turns`,
`--engines` and `--stalled` are byte-identical against the committed baseline built in a worktree.

`--selftest-answer` pins it, measured against every wrong selection buildable by a single clause:

| variant | cases failing |
|---|---|
| damage order + NO Signature split (**the code as it was**) | **1 — the counterfactual alone** |
| price order + NO Signature split | 3 |
| damage order + split KEPT | **0, correctly** — with the filter both orderings pick the same card |

The last row is why this pins the ANSWER and not the algorithm.

## My mistakes, and what caught each

- **My counterfactual was INERT when first written** and passed against every wrong selection
  including the original. I repriced the **swept array** when the damage tie is broken by cost
  **inside** `sweepMassAnswers`. *Caught by:* reconstructing the original selection and watching all
  four cases go green. **A self-test is validated by failing when it should** — rc-emit's own §12 ask,
  and it paid within the hour.
- **My docblock claimed the three live cases could not catch a missing filter.** Removing the split
  fails three of four. The claim was true of the OLD code and I wrote it about the NEW one. *Caught
  by:* running the break I had just described. Rewritten as the measured table above.
- **My label change broke a downstream parser to zero rows checked.** It read `rows[i+1]` and
  `rows[i+2]`; one inserted line moved the protection out from under it. *Caught by:* that test's own
  non-vacuity assertion. **A line offset is as false an interface as a prose match.** Rewritten to
  scan the block a row owns — and it checks the **same 16 rows** as before, so it found nothing and
  says so.
- **My first block terminator was `floor M\d+`**, which cannot see `floor M?`, the mode's own CANNOT
  DETERMINE token — so the scan ran into the next row and produced one confident failure naming an
  innocent entry. *Caught by:* reading the block the script actually prints. **Written from the line I
  had in mind rather than the line in front of me**, which is this script's oldest mistake and I made
  it on my first day in the file.
