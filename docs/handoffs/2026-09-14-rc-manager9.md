# Handoff — rc-manager9 → rc-manager10

Read this in full, then `CLAUDE.md` in full, then `docs/status.md`. **Section 3 is my mistakes and is
the part to read hardest** — my predecessor said the same and was right.

---

## 1. State

- **`master` = the last sha my gate promoted**; `origin/work` may be ahead. **768 combos + 222 synergy
  rules = 990** of the user's 1,000. Suite **684**, typecheck 0, `build:web` 0.
- **SEVEN promotions this session**, every one gated at a **pinned sha** in a throwaway worktree:
  `880deee → d99c0db → 579a284 → a4b3dc3 → 4085979 → e079f40 → 6e9847a`. Tests 666 → 684.
- **#222 filed and closed.** Open: **#137, #187, #191, #200, #218**.
- The tree is on `infra-audit-fixes-2026-09-13` — inert, leave it.

## 2. The fleet — four lanes, all self-dispatching, none idle

| lane | id | owns |
|---|---|---|
| `rc-gap3` | `17fead6d` | `.scratch-gap3/`, its walk docs. Citation vein CALLED; now harvesting synergy rules. |
| `rc-emit` | `72097442` | `scripts/adversarial-check.mjs` + its tests. Handoff already written at `docs/handoffs/2026-09-14-rc-emit.md`. |
| `rc-synth4` | `2ac5a523` | `.scratch-synth4/`, `test/dom/plays.dom.test.ts`, `docs/plays/2026-09-14-*`. |
| `rc-plaza` | `827fa826` | `.scratch-plaza/`, `scripts/sweep-vs-prose.mjs`. |

**You hold `CLAUDE.md`, `data/combos.json`, `data/synergies.json`, `docs/status.md`.** A lane gets a
FORMAL handover and hands it back in those words. All four stage and I apply; that protocol held all
day with zero collisions that mattered.

## 3. My mistakes

1. **I LEFT AN UNTRACKED `test/zz-probe.test.ts` INSIDE VITEST'S GLOB AND MADE THE SHARED SUITE RED
   FOR TWO LANES.** It was my scratch control for a new validation. `CLAUDE.md` names this exact
   hazard — there is no vitest config, so the default glob collects `**/*.test.ts` including scratch —
   and I did it anyway, twenty minutes after reading it. Both lanes correctly flagged it as not-theirs
   rather than working around it.
2. **A DRY RUN THAT RE-READS ITS INPUT AT WRITE TIME IS NOT A DRY RUN.** I validated SIX staged items
   and then wrote TWENTY-ONE, because the lane backfilled between my two commands. It landed exactly
   on the predicted state, which is **luck, not method**. Fixed in `scripts/apply-staged.mjs`.
3. **I PUBLISHED A RULE COUNTER IN `CLAUDE.md` THAT HAD A FIFTH TRAP**, introduced by my own fix to the
   first four: a bare `/` in the lookbehind, while this file writes rule PAIRS with a slash. A lane
   caught it by reading its own hits. **The fix to a false positive created a false negative in the
   same edit.**
4. **I APPLIED GARRISON CORRECTIONS AS APPENDS AND CREATED THREE PLAYER-FACING CONTRADICTIONS.** Two
   garrison notables disagreeing on the same entry, both in front of a reader. I had spent the whole
   day enforcing *amend in place, never append beside* on everyone else.
5. **MY STRUCTURAL GUARDS WERE WRONG BEFORE THE EDITS THEY GUARDED WERE.** One aborted on two correct
   edits because a splice is intra-line and I had the containment test backwards; another assumed one
   staged row per entry. **A structural guard is as capable of being wrong as the thing it guards, and
   its first failure is more likely to be its own.**

## 4. What NOT to re-derive

- **`scripts/promote.sh`** — the gate WITHHOLDS the push. One `if`, five conditions, push inside it,
  all six red branches proved to refuse before first use. Pin a sha, never a moving ref.
- **`scripts/apply-staged.mjs`** — refuses an item whose `new` is already present, requires the sha of
  the list you validated, retires the list on success. A lane measured that **17 of 21 would re-apply**
  under the naive gate: an append never consumes its anchor and an extension splice leaves `old` intact.
- **All 63 citation gaps are dispositioned** — 58 applied, 4 refused on measurement, 1 already carried.
  The vein is CALLED. Do not send anyone looking for a 64th.
- **The adversarial emitter has produced SIX false notables and every one had CORRECT CARD FACTS and a
  WRONG PREMISE ABOUT THE BOARD.** Five were caught by reading, one by a self-test. That sentence is
  the direction to look in.
- **This catalogue describes what a card does FOR you far more often than what it does TO you**, and
  every answer-sweep inherits that blind spot. Three instances, all in `CLAUDE.md`.

## 5. Open, in priority order

1. **rc-emit's `--recheck-garrison` may find more** as floors keep being repaired. It is a queue.
2. **#218** — `bodyCheck` fails flattering; the mitigation covering it is one edit from vanishing.
3. **#200** is the live vein and is producing: two synthesized entries landed today, both hand-walked,
   both suite-validated, both ruled `verified` rather than `candidate` — **`candidate` means UNWALKED,
   and overloading it to mean SYNTHESIZED would mislead the next reader.** Provenance goes in `notes`.
4. **990 of 1,000, and the standing instruction not to pad is recorded as correct. Do not reopen it.**
   I would rather hand over 990 real rows than 1,000 I have to defend.
