# Handoff — rc-neon4, 2026-09-25

Neon migration, steps up to 8b. Worktree `/private/tmp/rc-neon3`, branch `neon-migration`
(local only, not pushed), from `f6f0bf5` to `da0040b` plus this file. Plan:
`docs/superpowers/plans/2026-09-21-neon-migration-plan.md` (see its "Progress, 2026-09-25").

## Done (commit `da0040b`)

| Item | Verify, as run |
|---|---|
| Review finding 5 — distinct owners, read-only | Supabase `decks`: 2 rows, `content-range` 2, **2 distinct `user_id`**, 7 auth users; both owners hold one identity, Google, numeric `sub`. The 0004 staging design stands. |
| Review finding 3 — which `riftcombo` | `neonctl projects list` → `quiet-breeze-27436036` us-east-1 (good) and `round-waterfall-07137684` sa-east-1 (orphan). §0 was already right; six stale ids in the step bodies replaced. |
| Neon main ready | `staged:0 decks:0 users:0 profiles:0`; FK `ON DELETE CASCADE`; `claim_staged_decks`, `delete_account` present. |
| Step 8 | Rehearsed with the real script on throwaway branch `br-aged-term-avkxc7iw` (`rehearsal-step8-2026-09-25`): `supabase decks: 2 from 2 owners \| staged on Neon: 2`, exit 0; 2 distinct numeric subs; re-run refused (exit 1, no export). Main still `staged:0 decks:0`. Export `~/riftcombo-exports/2026-09-25-rehearsal-br-aged-term.json` (0600, outside the repo). |
| Step 8b | `security_runs/RiftCombo_3ba7df34` completed 2026-09-24T20:14Z, after every code commit and the merge-base; 3 findings, 0 blocking; triaged in the plan. |
| Gates | typecheck 0 · `npm test` exit 0, `Tests 749 passed (749)`, 0 `failed` · `build:web` 0. |

## Decisions taken at forks

1. **Step 8 was not run on Neon main.** Staging now would strand any deck saved on Supabase before the
   deploy, and undoing it would be a delete (forbidden). So it ran on a child branch, and the
   production run is the first command of the cutover ("Step 8 at cutover" in the plan). "Step 8
   done" therefore means *verified end to end and scripted*, not *real rows on main*.
2. **Supabase credentials** were read from the main tree's `.env.local` inside each command
   (`set -a; . …; set +a`), nothing copied, nothing printed. The worktree has no `.env.local`.
3. **No new security scan**: this session changed docs only; the 2026-09-24 run covers every code
   commit on the branch and the gate hook accepts it.
4. **Nothing pushed.** Not asked for, and the push is gated behind the owner's go anyway.
5. Added **O0** (Supabase transfer to the Free org, being done by aures-p10) to the plan's owner table.

## What is left for German

- **O1**: Google still runs on Neon's shared credentials (`oauth-provider list` → `google type:
  shared`). Enter your own client in the Neon console; redirect URI `<Auth>/callback/google`.
- **O2** (go/no-go, step 9): run the Step-8-at-cutover command, then deploy. Check that Vercel
  keeps `SUPABASE_URL` until step 10 (review finding 6) — the 2026-09-24 progress row says the two
  Neon vars are already set in production; verify in the dashboard rather than trusting that row.
- **O3** (one week after step 9): delete the Supabase project; if Free paused it meanwhile, restore
  before any rollback (review finding 2).
- **Orphans, nothing deleted**: Neon project `round-waterfall-07137684` (sa-east-1, empty, same name
  as production) and branch `br-aged-term-avkxc7iw` on `quiet-breeze-27436036` (holds the 2 staged
  rehearsal rows). Both are safe to delete with your OK.

## How to verify this handoff

```
cd /private/tmp/rc-neon3 && git log --oneline -3 && git status --short
neonctl projects list --org-id org-billowing-dawn-47109886
neonctl psql --project-id quiet-breeze-27436036 --branch br-wandering-haze-av7v6x9v --role-name neondb_owner -- -Atc "select (select count(*) from migration.staged_decks)||' staged '||(select count(*) from public.decks)||' decks'"   # 0 staged 0 decks
neonctl neon-auth oauth-provider list --project-id quiet-breeze-27436036 --branch br-wandering-haze-av7v6x9v   # google shared until O1
```
