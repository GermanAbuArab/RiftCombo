# Handoff — rc-manager7 → rc-manager8, 2026-09-13

Read this in full, then `CLAUDE.md` in full, then `docs/status.md` (re-measured today, current).

---

## 1. State  *(updated late in the session — read this version, not the first)*

- `origin/work` is the live branch. **`master` = `1f12d61`, the SECOND of two promotions today**
  (`df5de99` then `1f12d61`), each verified green in a throwaway worktree at that exact sha before
  pushing — 577 then **606 tests**, typecheck 0, `build:web` 0. Both deployed Ready.
- **766 combos + 220 synergy rules = 986.** `Combo.anyBodies` now carries **65 entries**; issues
  **#208, #210, #211, #212, #213, #215** all closed.
- The tree is checked out on `infra-audit-fixes-2026-09-13`, a branch created by a **non-fleet agent**.
  Leave it. Its commits are all on master now; it has **0 commits not on master** and nothing is
  pushing it, so it is inert. It is unslashed and therefore *not* covered by `vercel.json`'s `*/*`
  globs — if anything ever pushes it again it will burn a Preview deploy per push against the shared
  account quota.
- **766 combos + 220 synergy rules = 986** of the user's 1,000.

### Promotion is YOUR job
`CLAUDE.md` assigns it to the orchestrator — "sessions push to `work`, the orchestrator promotes to
`master`, in batches, a handful of times a day". My predecessor deliberately left it to the user;
that was a choice, not a rule change. The procedure that worked: pick a **sha, not a moving ref**
(lanes push every few minutes), `git worktree add --detach /tmp/<name> <sha>` with `node_modules`
symlinked, run all three gates there, then `git push origin <sha>:master`, then **verify on the remote
ref** with `git merge-base --is-ancestor <sha> origin/master`. Clean up with `git worktree remove`,
never `rm -rf` — a hook blocks it.

---

## 2. The fleet

| lane | id | holds |
|---|---|---|
| `rc-gap2` | successor to rc-gap | its own walk doc; **keyword-gate axis** (812 Legion, 821.1.c Weaponmaster, 829.1 Flow) |
| `rc-synth3` | successor to rc-synth2 | `scripts/adversarial-check.mjs`, `docs/plays/` |
| `rc-kw2` | successor to rc-kw | its own walk doc; reading the 11 base-parked entries |
| `rc-builder` | `33831ac1-a71b-4d82-b07e-b870d996ec37` | `src/builder.ts`, `web/builder.ts`, `src/build.ts`, builder tests, `test/a11y.test.ts` |
| `rc-schema` | `1660c51a-73ae-45cc-a5c9-182f913cbd3f` | `src/types.ts`, `matcher.ts`, `plan.ts`, `combos.ts`, `web/main.ts`, `web/graph.ts` |

**You hold `CLAUDE.md` and `docs/status.md`.** You get `combos.json`/`synergies.json` back when rc-gap
says so in those words. **rc-gap, rc-synth2 and rc-kw were all past the 70% context threshold** and
were told to close out, write handoffs into their *walk documents* (never `/tmp`), and report; spawn
successors from `~/.riftcombo-prompts/`.

All five lanes are exceptional: they state predicates, print denominators, refuse numbers they cannot
defend, and **each corrected its own published work unprompted**. Verify load-bearing claims anyway —
that is the rule, and it cost one grep and caught a real reversal today.

---

## 3. Mistakes I made — do not repeat them

1. **I ran another lane's in-flight script against a file I own.** Three runs minutes apart gave 298,
   326, and 28 spans; the author was editing it between them. My 298-span write damaged ~270 correct
   sentence-initial splices. Reverted in `0cb4314`. **A `.scratch` directory belongs to its author
   exactly as a source file does.** Now in `CLAUDE.md`.
2. **I printed `PUSHED` for a commit that never happened.** The commit failed on `.git/index.lock`,
   the rest of the line still ran, and `git push` said "Everything up-to-date". **Verify a push by
   re-fetching and testing `merge-base`, never by the push command's message.** Now in `CLAUDE.md`.
3. **I posed a falsifier that tested a different claim.** I told a lane "if your CHAINs do not MOSTLY
   run Time Warp the mechanism is refuted". The answer was 7 of 17 and the lane **refused my test
   rather than applying it** — it asked about the majority where the claim was about the split.
4. **I reproduced the #202 emphasis defect inside the bullet documenting it**, an hour after a lane
   repaired 35 of them. Its probe caught it in one run.
4b. **I read 383.2.a.1 backwards on SEVEN live entries** while applying it CORRECTLY to three other
   cards the same afternoon. The positional test fails silently and always FLATTERS, and neither
   `source-quotes` nor `rule-refs` can see it. Found by rc-kw2 auditing my notables on the page.
4c. **I ran a lane's script mid-edit against `combos.json` and got 298 / 326 / 28 spans on three
   runs**, damaging ~270 correct splices before reverting. See mistake 1.
5. I gave rc-builder a brief one file short (`test/a11y.test.ts` pins `web/builder.ts` source text),
   which showed up as a red suite.

---

## 4. What NOT to re-derive

- **The requirement-gap class** (`uses` not supplying a stated requirement) is an **ENGINE** class,
  found 11+ times, now covered by `Combo.anyBodies` and a shipped predicate-E test. Design decisions
  were argued and are in `CLAUDE.md`: no zone field, no severity flag, `count` merges with `max`.
- **Rules-grounded checks find things; prose-grounded ones do not.** Three measured: 14 defects/one
  narrowing vs. empty/three and near-empty/four. Ask what rule decides a check; stop when none does.
- **A check that comes back clean is worth PINNING**, because pinning costs nothing at a clean state.
- **The clock has three board states, not one baseline.** Hold both → no finisher wanted; hold one →
  the ordinary game, 8 on T9, where a finisher pays; hold none → Hold-gated lines are off. **41 of 80
  slower than unopposed, SIX of 80 slower than contested; BURST 20/23 → 1/23.**
- **Removal in this pool is priced at Might 3** — 13 printed gates, 10 at exactly 3, one at 5 for E10.
- Five counting traps, all in `CLAUDE.md`; `grep -E "^[[:space:]]*NNN\."` with no trailing space.

---

## 5. Open, in priority order

1. **`sett-kingpin-peak-guardian-mass-buff`** — rc-gap2 flagged it as the weakest of its twenty and
   held it out of the batch **for your explicit ruling**. Rule on it; do not let it ride in.
2. **#216** (nothing bounds an entry's declared quantity ABOVE, so `planDeck` could price an illegal
   card set) — latent, not live, proven by rc-builder over 766 entries / 1,638 variants / 6,085 cells
   at ZERO. It needs `src/combos.ts` and `test/combos.test.ts`, which are **rc-schema's**.
3. **Tournament Rules 403.4.b row-level cases** in `test/build.test.ts` — assigned to rc-builder.
4. **rc-synth2 is pricing `[Equip]` into the clock** (818.1 makes it a separate ability; it is worth a
   whole turn on `svellsongur-copy-hold`). If the text will not parse, the instruction is to
   **document the omission rather than approximate it**.
5. **Issues #137, #187, #191, #200** remain open; #200 is the live vein.
6. **986 of 1,000, and rc-kw refused to pad.** That refusal is correct and is recorded — 986 real rows
   beat 1,000 with fourteen soft ones. Do not reopen it.
