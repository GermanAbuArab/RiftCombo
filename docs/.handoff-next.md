# RiftCombo — handoff for the next session (written 2026-09-02, ~15:50 local)

Read in order: `CLAUDE.md`, `docs/plan.md`, `docs/phase0-findings.md` (Result 5 is the big
correction), `tasks/todo.md`, `docs/design/research-2026-09-02.md`. Then `git log` (5 commits).

## State

- **Data**: `npm run build:data` builds `data/cards.json` (1,189 printings, Riot gallery API,
  Equipment `effect` text included), applies `data/errata.json`, resolves `data/legality.src.json`.
- **Matcher**: `src/` — card index, deck codes + plaintext, authored combos → variants, six-bucket
  matcher. 22 tests green (`npm test`, `npm run typecheck`).
- **Combos**: `data/combos.json` — 4 verified (Lux energy / power / Renata kill; banned Recruits),
  4 candidates from the first hunt. The first hunt's "no infinite combos" claim was WRONG; see
  phase0-findings Result 5 for the walked Lux loop and Riot's ban text.
- **Web**: `npm run dev` (esbuild + wrangler dev on :8787). Design follows LOOPLINE's route map
  after the user rejected the first pass and said "fíjate bien la página de referencia": card-art
  nodes, layered + circular layouts, tray, drawer. User has NOT yet reacted to the redesign.
- **Re-hunt**: only L4 (points/alt-wins) finished → scratchpad `hunt2/L4-points-altwins.md`
  (16 candidates; top: Red Brambleback ×2 + Trinity Force ×3 + Skyfall of Areion = 10 points in
  one Conquer; multiple Equipment on one unit is legal per 434.1.b.1). L1, L2, L3, L5, L6 died on
  a session rate limit — re-dispatch with `hunt2/SHARED_BRIEF.md` (scratchpad
  `/private/tmp/claude-502/-Users-germanabuarab-proyectos-RiftCombo/63bed381-993e-48dd-801b-56962eb6d7e9/scratchpad/hunt2/`),
  two or three at a time, then a REFUTE pass with `docs/phase0/REFUTE_SPEC.md`, then human review
  into `data/combos.json` as `candidate`.

## Open decisions (user's)
- D1/D2: card art now hotlinks Riot's `cmsassets.rgpub.io` via the gallery API URLs (`&w=160&fm=webp`).
  Policy says assets must come from the Riot API; this is Riot's own CDN but not the dev-portal API.
- GitHub remote + Cloudflare deploy (`wrangler login`, `npm run deploy`). Repo is local-only.
- Whether per-outcome categorical colours in the graph are acceptable under the "one accent" rule
  (they encode a real property, as in the reference).

## Gotchas
- CSP forbids inline `style=` — colour SVG via attributes and HTML via CSSOM (`el.style.x = …`).
- Workers static assets need `run_worker_first = true` for security headers to apply.
- `[hidden]` needs `display:none !important` because `.empty` sets `display:grid`.
- Playwright MCP writes screenshots into `.playwright-mcp/` (gitignored); it cannot write elsewhere.
- The subagent session limit resets at 15:30 local; six heavy agents at once exhausted it.
