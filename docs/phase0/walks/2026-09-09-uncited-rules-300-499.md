# Walk — uncited Core Rules sub-rules 300–499

Issue **#187**. Session `rc-walk-mid`, opened 2026-09-09 by rc-manager5. Rules version 2026-07-16.

## 0. The slice, and how it relates to the other three lanes

The card populations are spent (926 deckable name+type groups, 890 catalogued). The vein is
`data/Riftbound-Core-Rules-2026-07-16.txt`. rc-manager5 split the sub-rule probe four ways on
2026-09-09; **this lane holds 300–499 and nothing else** — rc-walk-blocks has 100–299 (its walk
document is `docs/phase0/walks/2026-09-07-uncited-rules-blocks.md`, whose section H carries the probe,
the entry shape and every refusal with its scope) and rc-walk-fc has 500–829.

Where a finding of mine needs a paragraph outside 300–499 I cite it and leave the walking of that block
to its owner. Two of my entries lean on 815.1.c.2 and 827.1.c.3 for exactly that reason.

**The probe**, run against the live data files rather than the manager's 13:00 snapshot:

```
cited   = /\b(\d{3}(?:\.[0-9a-z]+)+)/g over data/combos.json + data/synergies.json
heading = /^[[:space:]]*(\d{3}(?:\.[0-9a-z]+)*)\.\s+(\S.*)$/ over the rules file
keep if !cited.has(id) && /Example:/ in the next 18 lines
rank by: does the example name a card from data/cards.json
```

Of the manager's 163 candidates in my range, **34 carry a worked example that names a pool card**. That
subset is the working queue, because a worked example is Riot telling you the interaction is real.

**The caret trap, confirmed for this file.** `grep "^NNN\."` and `grep -E "^ *NNN\."` both return zero
for the 89 headings that sit immediately after a form feed. Everything below was extracted with
`^[[:space:]]*`, which matches `\f`.

---

# Batch 1 — five entries, five uncited sub-rule clusters

| id | sub-rules opened | class |
|---|---|---|
| `tricksy-tentacles-yuumi-forced-defend-subset` | **355.11.b** | ENGINE |
| `ki-barrier-shen-kinkou-prevent-pool` | **437.1–437.3** (fifteen sub-rules) | ENGINE |
| `undercover-agent-karthus-empty-hand-draw` | **359.3.e.11** | ENGINE |
| `curator-steel-paws-activated-ability-seven` | **377.2 / 377.2.a** | ENGINE |
| `ashe-focused-scuttle-crab-hand-snapshot` | **424.3 / 424.3.a / 424.3.a.1** | ENGINE |

Staging file `/tmp/rc-walks/rc-walk-mid.json`, written with `set +o noclobber` and a session-prefixed
temp name, **appended, never overwritten**.

**Validation, over a merged copy — `data/combos.json` was never written by this session.**
`validateCombos`: **0 errors, 727 entries**. Duplicate ids: **none**. Duplicate card sets against the
whole catalogue: **none**. `generateVariants`: 1,575. The two `test/legend-lines.test.ts` checks
replicated over my five entries: **17 legend base codes checked, 0 defects**. `node .scratch/have.mjs`
run on all five card sets **before** any entry was written: *"no entry uses that card set or a subset
of it"* five times. None of the five is a loop-financed twin.

**Every rule reference and every quote checked mechanically.** 221 `NNN.x.y` references, all resolve;
57 quoted passages, all verbatim after normalising whitespace and curly quotes. Two flags raised by the
checker and both were **instrument artifacts, re-run and cleared**: five "missing" references were my
own filename `…-300-499.md` matching `499.md`, and one "not verbatim" was the apostrophe in
*"Fox-Fire's controller"* breaking a single-quote delimiter — the 929-character example is verbatim,
confirmed by direct substring test.

## 1. `tricksy-tentacles-yuumi-forced-defend-subset` — 355.11.b, a group target degrades to a SUBSET

**355.11.b, cited by nothing** (355.11 and 355.11.a both were):

> If the group of targets no longer collectively fulfill the targeting restriction as the spell or
> ability resolves, that spell or ability's controller can choose a subset of the original targets that
> fulfills the targeting requirement for the spell or ability to affect.

Riot's worked example is a pool card, `OGN-256 Fox-Fire`, and a defensive pump — and it ends by drawing
the boundary of the subset:

> They can't choose to affect units at the same battlefield that weren't initially chosen as targets.
> They can, however, choose to affect units that were initially chosen as targets that left the chosen
> battlefield before Fox-Fire resolved as long as those units are all located at the same battlefield.

So **a pump aimed at a total-Might restriction costs you one dragged body, never the spell**, and a
defensive move does not dodge it either as long as the survivors end up co-located. The pool prints
three group targets priced by summed Might (swept over `data/corpus_flat.txt`, 2026-09-09): `OGN-256
Fox-Fire`, `UNL-054 Tricksy Tentacles`, `VEN-107 Decree of Discord`. 355.12 is what makes each member a
target in the first place.

The line: Tricksy Tentacles drags up to 8 total Might of enemy bodies onto a battlefield **you** control,
so 450 and 190.3.a.1 apply Contested status and 464.2.c.1 makes **them** the Attacker on **your** turn;
464.2.e.1 places the Defender's triggers last and 340.1 resolves them first, so Yuumi's +3 Might and
[Tank] land before the damage step. 466.5 gives Control only to a player who did not already have it, so
the class is ENGINE and no point is scored.

## 2. `ki-barrier-shen-kinkou-prevent-pool` — 437.3, a Prevent Value is spent down and survives

**Fifteen sub-rules of the Prevent block were cited by nothing**: 437.1, .1.a, .1.a.1, .1.b, .1.b.1,
.1.b.1.a, .1.b.1.b, .1.b.2, .2, .2.a, .3, .3.a, .3.b, .3.c and 437.6. (437.4, 437.5, 437.5.a, 437.5.b
and 437.7 were already carried by `void-gate-ki-barrier-prevent-counts-bonus-damage`, rc-walk-fc / #191,
which this entry cross-references and does not duplicate.)

The obvious reading of `VEN-126 Ki Barrier` is that its Prevent 7 is a one-shot spent by whatever it
meets first. **It is not.** 437.2 replaces the damage with one reduced by the tracked Prevent Value;
437.3 reduces that value by the amount prevented; 437.3.b makes the reduced number the newly tracked
value; 437.3.a expires it only *"If the Prevent Value is 0 or less."* A Prevent 7 that eats a Deal 2 is
a Prevent 5 and keeps working — **a pool for the turn**.

That makes the pool's two bounded Prevents **opposite tools**, which no entry had said. 437.1.b.2:
*"Prevent will always apply to the next damage that would be dealt to a unit affected by the Prevent
action."* `SFD-194 Counter Strike` reads *"The next time that unit would be dealt damage this turn,
prevent it"* — one instance of **any size**, the answer to `OGS-022 Final Spark` (8, the pool's largest
single Deal) and spent entirely by a 1-damage ping. Ki Barrier is the answer to a spell with several
Deal actions: against `OGN-248 Icathian Rain`, which prints *"Deal 2 to a unit."* six times, it absorbs
2 + 2 + 2 and 1 of the fourth — **seven of twelve**, where Counter Strike absorbs two. Counter Strike is
also Signature (Jax), so 103.2.d.2 forces a legend on any list that runs it; Ki Barrier is not.

437.3.c — *"If the Prevent Value is 'All' then it remains 'All.'"* — is why the other two Prevent cards
are different in kind and why the 437.3 arithmetic is visible on exactly one card. Four Prevent rows in
the pool, swept 2026-09-09: `OGN-145 Unyielding Spirit`, `SFD-194 Counter Strike`, `VEN-025 Esteemed
Hierophant`, `VEN-126 Ki Barrier`; only the last is numbered.

On `OGN-241 Shen, Kinkou` the seven stops being single-target cover: 437.5.a sets lethal at Might 3 +
[Shield 2] + Prevent 7 = **12**, and 815.1.c.2 makes every other body of yours an invalid assignment
until Shen has it. Both cards are [Reaction], so the whole line is assembled on the opponent's turn.

## 3. `undercover-agent-karthus-empty-hand-draw` — 359.3.e.11, the discard is EFFECT text

**359.3.e.11, cited by nothing** (359.3.e.10 and 359.3.e.14.a both were):

> Instructions that can be partially followed are followed as much as possible and ignored otherwise.

and its example is this card's sentence — *"A player plays a spell that says 'Discard 2, then draw 2.'
If their hand is empty, the instruction to discard 2 will be ignored. They'll still draw 2."* **422.4
names the card outright**, ending *"Regardless of how many cards they discard, they then draw 2."*

The distinction that makes it work is one paragraph away: 422.3 refuses a discard **cost** that cannot
be completed, and this discard is not a cost — it sits after the [Deathknell] marker, which 808.1.c
makes *"functionally short for 'When I die, [Effect].'"* Nor is the draw linked to it under
359.3.e.14.a, because *"then draw 2"* references nothing the discard touched.

`OGN-236 Karthus, Eternal` doubles it. From an **empty** hand: four cards drawn, two held, two in the
trash. From a hand of two or more: four discarded and four drawn, hand size unchanged. **Chaos + Order
is exactly ONE legend name** — Heart of the Tempest (VEN-155 / VEN-197), census run against
`data/cards.json` — so this is a one-legend line with no second shell.

## 4. `curator-steel-paws-activated-ability-seven` — 377.2 and 377.2.a

**377.2 and 377.2.a were cited by nothing**; their sibling 377.2.b is the paragraph R30 was voted on.

> 377.2. Card text will refer to activating Activated Abilities with the word "use" or "play."

That is the paragraph that makes `VEN-145 Curator of the Sands`' own sentence legible — *"When you
**play** a unit, gear, or activated ability with Energy cost 7 or more"* — and 827.1 (*"Empower is an
Activated Ability keyword"*) with 827.1.c.1 supplies the other half. **Two catalogued Curator entries
read only her printed-cost half through 206 and neither opens this one.**

> 377.2.a. If "using" or "playing" an Activated Ability is part of a trigger condition, that condition
> is fulfilled when the Activated Ability resolves.

So the rebate lands **after** the payment: the two runes can never help pay for the ability that
produced them. That is the *engine offset by one event* shape this project already records for a 383.3.b
base cost and for 381 with 310.2, reached by a **third** route.

And it dictates the partner. 827.1.c.3 makes cost-altering text count *"when determining a card's
Empower cost for any reason"*. The pool has exactly **four** activated abilities reaching 7 Energy
(swept 2026-09-09): `VEN-043 Steel Paws` (flat 7), `VEN-046 Nasus, Ascended` (flat 8), and `VEN-032
Frostcoat Mother` and `VEN-050 Grumpy Rockbear`, both *"[Empower] 12 Energy. This ability costs 1 Energy
less for each rune you control."* 161.2.a fixes the Rune Deck at twelve and 315.3.b channels two a turn,
so **from the turn you control six runes those two stop triggering Curator** — the board state that lets
you afford them switches her off. All four sit inside her Calm/Mind identity; only the two flat ones are
hers to use.

## 5. `ashe-focused-scuttle-crab-hand-snapshot` — 424.3, 424.3.a, 424.3.a.1

**424.2.b, 424.3, 424.3.a, 424.3.a.1 and 424.4.a were all cited by nothing.** The teeth:

> 424.3.a.1. The Revealed state is applied to those cards. Any further cards added to that zone after
> the instruction has been executed do not become Revealed.

`UNL-169 Ashe, Focused` banishes *"a card revealed this way"*, so the choice is confined to the snapshot
424.3.a took (*"All cards currently in the specified zone"*) and a card drawn afterwards can never be
the target. The strip is exactly one card and cannot be re-aimed. 424.3's own example adds that a bare
reveal is **non-destructive and order-preserving** — *"return them to the top of the Main Deck in the
same order"* — so a reveal is not a mill and costs the revealer nothing; only the banish has a price.

The hole the family cannot see: 424.1 scopes revealing to a zone whose information is hidden, and
811.1.b hides a card **at a battlefield**, not in the hand — so **no hand reveal in the pool ever shows
a facedown card**. Swept 2026-09-09: six cards reveal an opponent's hand (`OGN-156 Sabotage`, `OGN-192
Mindsplitter`, `UNL-053 Scuttle Crab`, `UNL-135 Insightful Investigator`, `UNL-169 Ashe, Focused`,
`VEN-085 Decree of Strength`) and exactly **one** also opens the facedown: Scuttle Crab. The information
is bounded and small — 811.1.b allows one facedown per battlefield and 485.4 puts two on a Duel table —
which is why it is worth a card and not more. Calm + Order is **four** legend names, census run against
`data/cards.json`.

---

# 6. A correction owed to CLAUDE.md — the enemy-mover family's domains, MEASURED

Not applied by me: this lane does not own `CLAUDE.md` and the file is the user's accumulated record.
Reported to rc-manager5 with the measurement. **Three of its statements about that family are wrong at
the precision they were written**, measured against `data/cards.json` (authoritative for domains) and
confirmed against `data/corpus_flat.txt` on 2026-09-09:

| CLAUDE.md says | `cards.json` says |
|---|---|
| `OGN-043 Charm` **(Calm/Mind)** — written twice, in the #141 and #173 notes | **mono-`calm`** |
| `UNL-139 Bone Skewer` **(Fury/Chaos)** — the #173 note | **mono-`chaos`** |
| "`OGN-067 Blitzcrank, Impassive` … **the only one in mono-Calm**" — the #188 note | Charm, Iascylla and Blitzcrank are **all** mono-Calm, and `UNL-054 Tricksy Tentacles` is a fourth |

Measured row by row: `OGN-043 Charm` calm, `OGN-067 Blitzcrank, Impassive` calm, `UNL-050 Iascylla`
calm, `UNL-139 Bone Skewer` chaos, `VEN-148 Shadow Dash` calm/order, `UNL-141 Evelynn, Entrancing`
chaos, `UNL-054 Tricksy Tentacles` calm.

**The consequence is not cosmetic.** CLAUDE.md presents the family as spread across four identities and
tells the next session that *"a fifth entry in this family must say what it adds to all four."* Four of
the six are **mono-Calm**, so it is a **Calm** family and one Calm legend covers every one of them —
which is a deckbuilding fact of the opposite shape to the one the file states. This is the file's own
standing lesson biting a line inside it: a domain claim that was **typed** rather than **run** against
`cards.json`.

# 7. Blocks read and returned EMPTY, each with the scope of the refusal

Recorded so nobody re-reads them.

### 7.1 370.2 — the Zhonya's chain needs a card this pool does not print

370.2 (*"A Replacement Effect can only be applied once to an event, or to any Game Actions or events
that replace that event"*) has a worked example in which **two copies of Zhonya's Hourglass save each
other**: the first applies, kills its source, *"which creates an event the other can apply its
Replacement Effect to."* Its premise is a spell reading *"gear you control become 1 [M] gear units this
turn."* **No such card exists** — `grep -in "gear unit"` over `data/corpus_flat.txt` returns **zero**
rows, and the only near-miss is `SFD-073 Experimental Hexplate`'s *"I am a Mech"*, which is a **tag**
and not a type (the #191 type histogram found no card in the pool with two types). `OGN-077 Zhonya's
Hourglass` reads *"If a friendly **unit** would die"*, and a gear dying is not a unit dying, so the
second Hourglass never sees the first one's death.

**Scope: the current pool.** The rule is live and general; only the chaining example is dead letter.
Note also that the first instrument I reached for (`grep -inE "gear.{0,40}(become|are).{0,30}unit…"`)
**errored out of ugrep on complexity and printed nothing** — an empty result that was not a measurement.
Two working instruments were run before this paragraph was written.

### 7.2 417.1 – 417.7 — the Deal block's remaining rows are definitions or already carried

Eleven rows here are uncited but the block is spent. 417.1.a and 417.1.e.1 are already recorded in
CLAUDE.md (#191); 417.6.b.2.a, 417.6.b.3 and 417.6.b.4 are **cited** by entries. What is left is
definitional — 417.1.b/c/d (marking damage), 417.2, 417.5, 417.7 — with one row worth carrying:
**417.6.a**, *"If a game effect does not specify a source, the game effect describing the Deal action is
the source"*, whose example names `Void Seeker`. It is the exact complement of 417.6.b.3 (Challenge) and
together they are the whole of "is this spell damage or unit damage".
**Scope: new lines.** 417.6.a remains the better citation for the positive half.

### 7.3 427.3.a, 434.5, 364.3.a, 410.2.b, 355.16, 356.7, 358.4 — read, no line

- **427.3.a** — separate banish-and-play effects never share a pool, and an unmet condition leaves the
  card *"in banishment indefinitely."* Same content rc-walk-blocks walked as 395 / 397 (#187 batch 4).
  *Scope: new lines; it is the general form and the better first citation.*
- **434.5** — *"Attaching a card to another card does not modify either card's state"*, example: *"An
  exhausted equipment being attached to a unit does not ready the equipment."* True and thin.
  *Scope: no card turns on it today.*
- **364.3.a** — conditional passives are recognised by *"if"* or *"while"*. Definitional; rc-walk-blocks
  walked 363–366 in its batch 5.
- **410.2.b**, **355.16**, **356.7**, **358.4** — procedural rows. 356.7 and 357.2 were walked by
  rc-walk-blocks in its batch 15; 355.16 is 357.3's twin at the finalization step, with a hypothetical
  card rather than a printed one.

# 8. Running tally for this lane

- Opened at catalogue **715 + 180 synergy rules**; batch 1 stages **5** entries, all ENGINE.
- Sub-rules opened that nothing in `data/combos.json` or `data/synergies.json` had cited: **355.11.b,
  359.3.e.11, 377.2, 377.2.a, 424.3, 424.3.a, 424.3.a.1, 437.1, 437.1.a, 437.1.a.1, 437.1.b, 437.1.b.1,
  437.1.b.1.a, 437.1.b.1.b, 437.1.b.2, 437.2, 437.2.a, 437.3, 437.3.a, 437.3.b, 437.3.c** — 21 in all.
- Read and returned empty with the reason: **370.2** (scoped to the pool), **417.1–417.7** (scoped to
  new lines), **427.3.a / 434.5 / 364.3.a / 410.2.b / 355.16 / 356.7 / 358.4**.
- Next in the queue, all in range and all carrying an example that names a pool card: **369.2** (Burning
  Out and Preventing Damage are themselves Replacement Effects), **370.1.b.1** (Undertitan, an *"as"*
  replacement is the event **plus** the action), **359.3.e.4** and **359.3.e.12 / .12.a** (a target that
  left and came back is a new object; a check on an unavailable target returns null), **364.3.a's
  neighbours 364.1 / 364.3**, **416.2.a**, **422.2.a**, **465.2.c.7**, **443.2.a / 444.2**, **483.4 /
  483.6**, and the 489 (2v2) rows **489.5.a / 489.6 / 489.7 / 489.8.f / 489.8.g / 489.8.g.1**.

---

# 9. What rc-walk-blocks' section 57 handed over, and what I did with it

rc-walk-blocks read part of 300–499 before the lane split and wrote its readings into section 57 of
`docs/phase0/walks/2026-09-07-uncited-rules-blocks.md` (`26fe14a`), **staged as nothing**. I read it after
drafting and folded three of its rows in, re-deriving each against the rules file rather than taking the
report at face value:

- **355.11.b — the removal-versus-evacuation split.** Its sharp half is that a shrunken subset is partial
  credit for a removal or a drag and **total failure** for an evacuation, because 323.6 strips Control only
  from players *"without their Units occupying them"*. Verified against 323.6 and folded into entry 1,
  where it is the reason this line and `moonlight-affliction-tricksy-tentacles-evacuation` are opposites
  on the same spell. It also names an unstated failure mode in that entry and in
  `decree-of-discord-megatusk-evacuate-and-gank` — **not applied by me, this lane does not own
  `data/combos.json`.**
- **465.2.c.7 — a second Tank BODY is not redundant even though a second Tank GRANT is.** Re-read in the
  rules file: the paragraph is uncited, 815.2 (*"Multiple instances of Tank are redundant"*) **is** cited,
  and 465.2.c.7's worked example is two Tank bodies plus one plain body. Folded into entry 2: two Shens
  are a toll of 5 + 5, and 12 + 5 = 17 with Ki Barrier on one of them.
- **377.2.a — a countered ability never fires the trigger.** Re-derived against 419.4.b (cited) and folded
  into entry 4 as the paragraph's other consequence.

I also read `f4a0053` (the Duel's *"Battlefield Count: 2"* is a setup parameter, so 470's cap can be three
with the Baron Pit on the table, and 471.1.b.1 then taxes a conquer finisher). **It does not move any
number in this batch** — none of my five entries scores, so none is priced against a conquer ceiling.

Not taken, on rc-walk-blocks' own advice: **423.1.a.1** is already cited 72 times and the stun family is
mined. **356.3** (cost increases are applied before discounts) is uncited and is a real lead, but it sits
in another lane's touched work on `mystic-vortex-helm-suppression-tax` and I ran out of turn before it.

---

# HANDOFF — rc-walk-mid, stood down 2026-09-09, resumes SATURDAY 2026-09-12

`/tmp` does not survive a reboot and Saturday is three days away, so this section is the durable copy.
Whoever resumes is not me and will not have my context.

## H1. Lane, issue, staging

- **Issue #187**, "Walk: the Core Rules blocks this catalogue has never cited". Commits use `Refs #187`.
  **Do not open a new issue for the split and never write a qualified `Closes`** — a qualified `Closes` has
  already closed a whole issue on this repo when only half of it was done.
- Lane: **uncited Core Rules sub-rules 300–499 and nothing else.** rc-walk-blocks holds 100–299,
  rc-walk-fc holds 500–829. Cite outside the range freely; leave the walking of it to its owner.
- **Staging file: `/tmp/rc-walks/rc-walk-mid.json`, five entries, none merged as of this handoff.**
  Ids: `tricksy-tentacles-yuumi-forced-defend-subset`, `ki-barrier-shen-kinkou-prevent-pool`,
  `undercover-agent-karthus-empty-hand-draw`, `curator-steel-paws-activated-ability-seven`,
  `ashe-focused-scuttle-crab-hand-snapshot`. **Append, never overwrite**, and `set +o noclobber` on the
  same command line — a silent `cat >` failure has folded another lane's entries into a staging file here
  before.
- Nothing of mine is half-walked. There is no dropped draft.

## H2. Numbers, opened and closed at

- Opened at catalogue **715 entries + 180 synergy rules**; staged **5**, all ENGINE, so the catalogue is
  **720** once merged.
- Of the manager's **163** uncited candidates in 300–499 (computed 13:00, 2026-09-09), **34** carry a
  worked Example that names a card in `data/cards.json` — that subset is the queue.
- **21 sub-rules opened** that nothing in `data/combos.json` or `data/synergies.json` had cited: 355.11.b,
  359.3.e.11, 377.2, 377.2.a, 424.3, 424.3.a, 424.3.a.1, and the whole of 437.1 / .1.a / .1.a.1 / .1.b /
  .1.b.1 / .1.b.1.a / .1.b.1.b / .1.b.2 / .2 / .2.a / .3 / .3.a / .3.b / .3.c. Plus **465.2.c.7**, folded in
  from section 57.
- Validation over a merged copy (`data/combos.json` never written by this session): `validateCombos`
  **0 errors, 727 entries**; no duplicate id; no duplicate card set; `generateVariants` 1,575;
  `test/legend-lines.test.ts` checks replicated over the five, **17 legend base codes, 0 defects**;
  **221 rule references all resolve**; **61 quoted passages all verbatim**. `node .scratch/have.mjs` run on
  all five card sets before drafting **and again immediately before reporting**: clear both times.

## H3. Refusals, each WITH THE SCOPE IT WAS MADE IN

A refusal is only as wide as the reason given.

| block | refused because | scope |
|---|---|---|
| **370.2** | its Zhonya's-chain example needs a spell reading *"gear you control become 1 [M] gear units this turn"*; `grep -in "gear unit"` over `data/corpus_flat.txt` returns **zero** rows, and `SFD-073 Experimental Hexplate`'s *"I am a Mech"* is a **tag**, not a type | **the current pool.** The rule (a Replacement Effect applies once per event, and its own application creates a new event another can replace) is live and general; only the two-Hourglass chain is dead letter, because `OGN-077` reads *"If a friendly **unit** would die"* |
| **417.1 – 417.7** | the remaining uncited rows are definitional (417.1.b/c/d, 417.2, 417.5, 417.7); 417.1.a and 417.1.e.1 are already in CLAUDE.md, and 417.6.b.2.a / .b.3 / .b.4 are cited by entries | **new lines.** **417.6.a** (*"If a game effect does not specify a source, the game effect describing the Deal action is the source"*, example `Void Seeker`) is still the right first citation for the positive half of "spell damage or unit damage" |
| **427.3.a** | separate banish-and-play effects never share a pool; same content rc-walk-blocks walked as 395 / 397 | **new lines**; 427.3.a is the general form and the better first citation |
| **434.5** | *"An exhausted equipment being attached to a unit does not ready the equipment"* — true and thin | **today's pool**; no card turns on it |
| **364.3.a** | conditional passives are recognised by *"if"* or *"while"*; definitional, and rc-walk-blocks walked 363–366 | **new mechanics** |
| **410.2.b, 355.16, 356.7, 358.4** | procedural. 356.7 / 357.2 walked by rc-walk-blocks batch 15; 355.16 is 357.3's twin at the finalization step with a **hypothetical** card, not a printed one | **new lines**; 410.2.a/b is still the unifying Limited Action paragraph behind seven blocks |
| **423.1.a.1** | not opened at all — already cited 72 times, per rc-walk-blocks | **the stun family is mined** |

## H4. The exact next row to open

**`369.2`**, and it is in range, uncited, and carries two worked examples:

> Some Game Actions are themselves Replacement Effects.
> Example: Burning Out is a replacement effect.
> Example: Preventing Damage is a replacement effect.

Read it **with 370.2** (which caps a Replacement Effect at once per event) and with **431.5** and
**437.4**: the consequence rc-walk-blocks flagged and nobody has walked is that **a Burn Out cannot be
responded to**. This project's whole loop ledger is built on Burn Out being a cost you must not pay
(`lux-infinite-energy`'s "0 spare draws is a WALL"), and nobody has asked whether it can be interacted
with at all. That is the highest-value row left in 300–499.

Then, in order, all in range and all with an example naming a pool card:

1. **370.1.b / 370.1.b.1** — `SFD-175 Undertitan`; an *"as"* replacement is the event **plus** the action.
   This is also the paragraph behind a citation upgrade **four** Undertitan entries already owe (rc-walk-blocks §H5).
2. **356.3** — cost increases are applied **before** discounts (step 3 against 356.4), so a flat tax is
   fully refundable while 356.4.e's per-discount floor lets it stick. The pool's only three increase rows
   are `VEN-045`, `VEN-160`, `UNL-219`. Touches `mystic-vortex-helm-suppression-tax`.
3. **359.3.e.4** and **359.3.e.12 / .12.a** — a target that left a zone and came back is a **new object**;
   a check on an unavailable target returns null and all calculations on it are ignored.
4. **416.2.a**, **422.2.a**, **443.2.a / 444.2** (`UNL-079 Diana, Lunari`), **483.4 / 483.6** (`Sanction`).
5. The **489** rows — 489.5.a, 489.6, 489.7, 489.8.f, 489.8.g, 489.8.g.1. 2v2 is under-walked generally
   and `matchDeck` reads one list, so expect leads rather than entries.

## H5. Corrections owed elsewhere — NOT applied, because this lane owns neither file

1. **`CLAUDE.md`, the enemy-mover family.** Section 6 above, measured against `data/cards.json`:
   `OGN-043 Charm` is **mono-calm** (the file says Calm/Mind, twice), `UNL-139 Bone Skewer` is
   **mono-chaos** (the file says Fury/Chaos), and *"`OGN-067 Blitzcrank, Impassive` … the only one in
   mono-Calm"* is false — **four of the six movers are mono-Calm**, so it is a Calm family and one Calm
   legend covers all of them, which is the opposite shape to the one the file states.
2. **`moonlight-affliction-tricksy-tentacles-evacuation`** and
   **`decree-of-discord-megatusk-evacuate-and-gank`** should state the 355.11.b failure mode: a subset is
   partial credit for a removal and **total failure** for an evacuation (323.6).
3. **The two catalogued Curator entries** — `curator-printed-cost-rebate`,
   `curator-needlessly-large-yordle-printed-ten` — read only her printed-cost half through 206 and should
   cite **377.2**, the paragraph that makes *"play an activated ability"* mean anything at all.

## H6. Habits that paid this session

- **`grep -E "^[[:space:]]*NNN\."`.** A bare `^NNN\.` and a literal-space `^ *NNN\.` both return zero for
  the 89 headings after a form feed.
- **Re-run a surprising absence with a different instrument.** It fired twice today. My first sweep for a
  gear-becomes-unit card **errored out of ugrep on complexity and printed nothing** — an empty result that
  was not a measurement; two working instruments were run before the 370.2 refusal was written down. And
  the quote checker's two flags were both artifacts (my own filename matching `499.md`, and an apostrophe
  breaking a single-quote delimiter), cleared by direct substring test rather than by editing a correct
  quote.
- **Run `node .scratch/have.mjs` the moment you know the card set, and again before reporting.** Four lanes
  are reading one rules file into one working tree.
- **A sibling lane's report is a lead, not a citation.** All three rows I took from section 57 were re-read
  in the rules file before they entered an entry.

---

# RESUMED 2026-09-12 — batch 2, five entries, twenty-nine sub-rules opened

Session `rc-walk-mid` (successor), issue **#187**, manager `rc-manager5`. The handoff above was read in
full before anything else; its refusals are respected and none of them is re-opened. Catalogue was at
**736** when this batch was validated and staged — the predecessor's five are all merged.

| id | sub-rules opened | class | cards |
|---|---|---|---|
| `blast-cone-iascylla-responsible-move-stun` | **411.4, 411.5, 411.5.a, 446.2, 446.3, 446.3.a, 446.3.b, 446.3.c** | ENGINE | UNL-133 + UNL-050 |
| `shakedown-immortal-phoenix-reaction-add-at-finalization` | **444.1, 444.2, 444.2.a, 444.2.b, 444.2.c, 444.3, 444.3.a** | ENGINE | OGN-033 + OGN-037 |
| `deceiver-viktor-reflection-keeps-non-play-trigger` | **383.4.a.3, 383.4.a.4** | ENGINE | UNL-199 + OGN-117 |
| `progress-day-shadow-temple-deliberate-burn-out` | **369.2, 370.2, 431.3.a, 431.4, 431.4.a, 431.5, 413.4, 413.4.c** | ENGINE | VEN-165 + OGN-114 |
| `ride-the-wind-overzealous-fan-non-combat-showdown-conversion` | **316.8.b.1.a, 344.1, 313.1.a, 313.3, 316.8.c, 462.1** | ENGINE | OGN-173 + SFD-128 |

Staged to `/tmp/rc-walks/rc-walk-mid.json`, **appended** under `set +o noclobber` through a
session-prefixed temp file and an atomic `os.replace`. `data/combos.json` was never written by this
session.

**Validation, over a merged copy at catalogue 736.** `validateCombos`: **0 errors, 741 entries**.
Duplicate ids: **none**. Duplicate card sets (card set + `needs`): **none**. Subset/superset sweep of my
five card sets against all 736: **no hit in either direction**. `generateVariants`: **1,595**. The three
`test/legend-lines.test.ts` checks replicated over my five: **19 legend base codes, 0 defects**; no entry
uses a Signature card. **389 rule references, all resolve; 104 quoted passages, all verbatim** after
normalising whitespace and curly quotes.

## 8. `blast-cone-iascylla-responsible-move-stun` — 411.4, and Riot's example is the card

**411.4, cited by nothing:** *"If an ability triggers when “you” do something, it triggers when a Game
Action that you are responsible for occurs."* Its worked example opens *"A gear reads “When you move an
enemy unit, you may exhaust this to [Stun] it.”"* — that is `UNL-133 Blast Cone`, printed in the pool,
almost word for word. So the Cone's stun is a **responsibility test over every enemy move you cause**,
not something tied to the Cone's own play trigger, and **411.5 / 411.5.a** say why: *"Spells and
abilities have attribution for game actions, while players have responsibility."*

That splits the Cone's two sentences into two different kinds of ability. Sentence one is a **Play
Effect** (383.4.a) and fires once. Sentence two has no such condition and is rationed only by
415.3.a — one stun per turn, forever. `UNL-050 Iascylla` is the only mover in the pool that is free,
repeating and on your own turn.

**446.3 and all three of its sub-rules were uncited**, and 446.3.c is load-bearing across this whole
catalogue: *"Moving does not use the Chain, nor is it able to be Reacted to."* Every "evacuate, then
walk in" entry rests on it and reaches it the long way round through 323.10 and the Neutral Closed
State. 446.3.a/b (*"There is no state for Permanents between locations"*) close the companion question:
a body is never catchable in transit.

## 9. `shakedown-immortal-phoenix-reaction-add-at-finalization` — 444.2.c, a correction

**The whole of rule 444 was uncited.** 444.2.c: *"Players may activate abilities that Add resources with
the Reaction keyword at any time that they are instructed to Pay resources. Those abilities finalize and
resolve immediately, ignoring normal restrictions."*

**Every rune in the game is such an ability** — 164.2.a `[E]: [Reaction] — Add [1]`, 164.2.b
`Recycle this: [Reaction] — Add [C]`. This project's standing model is that a cost due at FINALIZATION
must be banked beforehand, because 312.2.c hands out no priority in a Closed State. That is true of
**priority** and false of **paying**: 444.2.c is a permission that does not need priority and says so.

It bites on the **opponent's turn**, where 167 has emptied your pool twice over. The line: a [Reaction]
Shakedown kills a body there; 428.5.c attributes the kill to the spell (without it no burn spell would
ever satisfy *"kill a unit with a spell"*, since 143.2.a kills in a Cleanup) and 428.5.c.1 makes you
responsible for it — the same responsibility clause as 411.4. The Phoenix's 1 Energy + 1 Fury Power is
then found on the spot.

444.2 also carries a second fact nobody has used: **a Pay instruction inside an effect is optional even
with no "you may"** — *"If they choose not to, the instruction is ignored"* — with 444.2.a and 444.2.b
splitting what declining costs you depending on whether the Pay is part of a cost.

## 10. `deceiver-viktor-reflection-keeps-non-play-trigger` — 383.4.a.4

`UNL-T06 Reflection` prints *"(I become a copy of something when played. I don't get that card's play
effects.)"* — a **defined term used on a token with no definition on the card**. 383.4.a supplies it and
**383.4.a.3 / 383.4.a.4 were uncited**: *"Abilities that trigger when another object is played are not
considered Play Effects."*

So the Reflection's drawback is narrower than it reads. It loses `"When you play me…"` and nothing else.
`OGN-117 Viktor, Innovator` is the extreme case, because a trigger on another object being played is his
**entire printed text** — the naive reading makes a Reflection of him a blank 0-Might body, and 383.4.a.4
makes it a second Viktor.

The windows line up on the phase order: 315.2.b.2 puts the Hold *after* 315.2.a, so a Reflection minted
at the Hold survives your turn and **all of the opponent's**, which is the only turn Viktor's trigger can
fire on. 350.2 with 185.2.a is the trap the family always walks into — Viktor says *"play a CARD"*, so
the Recruits he makes are invisible to his own trigger and there is no cascade.

## 11. `progress-day-shadow-temple-deliberate-burn-out` — 369.2, the handoff's own top row

**369.2, cited by nothing:** *"Some Game Actions are themselves Replacement Effects. Example: Burning Out
is a replacement effect. Example: Preventing Damage is a replacement effect."* 431.5 repeats it.

**The answer to the handoff's question is yes: a Burn Out cannot be responded to.** A Replacement Effect
intercedes *during* the execution of a game effect (369); 340.1 says of a resolving chain item *"Execute
its game effects in their entirety"*; and in the Draw Phase there is no chain at all, because 335 sends
you straight to *"the next substep, step, phase, or turn"*. There is no window anywhere in 431.2.

**And the order of 431.2's four steps is the payoff.** 431.2.b *"Recycles their trash into their Main
Deck"* comes **before** 431.2.d *"Completes the remainder of the action that caused them to burn out"* —
413.4.a/b/c say the same from the Draw side. So `OGN-114 Progress Day` on an empty deck draws four fresh
cards out of the trash you spent the game filling, for exactly one point (194.1.d).

**The bound, which is the part the loop ledger needs.** 431.3.a: *"Unless some effect intervenes, this
will result in them burning out repeatedly, giving 1 point to an opponent each time, until an opponent
passes the Victory Score and wins the game."* 431.3.b: *"Points gained after the first Burn Out being
processed in sequence cannot be replaced or prevented by any means."* A deliberate Burn Out with a **fat
trash** is a one-point toll; the identical action with an **empty trash** is the game. Inside a loop,
recycled cards go to the BOTTOM OF THE DECK (416.1) and never to the trash, so the trash is empty by
construction — **`lux-infinite-energy`'s "zero spare draws is a WALL" is 431.3.a and 431.3.b, not a point
you may choose to pay.** No entry had ever cited either paragraph.

431.4 / 431.4.a are why you cannot simply ask for it: *"Players may only burn out when Game Effects
direct them to do so."* Sweep over `data/corpus_flat.txt`, 2026-09-12: exactly **two** cards draw three
or more — `OGN-114 Progress Day` (four) and `SFD-087 Premonition` (three).

## 12. `ride-the-wind-overzealous-fan-non-combat-showdown-conversion` — 316.8.b.1.a

**316.8.b.1.a, cited by nothing:** *"These Showdowns are called Non-Combat Showdowns. During the
proceedings of a Non-Combat Showdown, units controlled by a different player may become present at the
Battlefield where the Showdown is ongoing. This will cause the Showdown to become a Combat Showdown in
the following cleanup."* **344.1** says it from the Showdown side and is also uncited.

**The bloodless conquer this catalogue uses offensively has a printed answer.** 348.2.a gives Control
only *"If only one player's Units remain at the Battlefield"*, so one defending body denies the Conquer
before any combat resolves. Then `SFD-128 Overzealous Fan` — Riot's own worked example at 204.3.a —
kills himself to send the attacker home, and 466.3.d returns No Result with nobody's units present.

**The destination is legal because this is a Duel, and that is a measurement.** Both paragraphs that
would forbid it are scoped by their own opening words: 447.2.a and 462.1 both begin *"In Modes of Play
with more than two players"*, 462.2 likewise, and 447.2.b is scoped to teammates. 485.1 puts two players
at a Duel table, so **the whole 462 block and the 447.2.a/b/c destination restrictions are dead letter in
Constructed** — recorded here so nobody walks them again.

## 13. Refusals, each WITH ITS SCOPE

| block | refused because | scope |
|---|---|---|
| **462.1, 462.2, 462.2.a, 462.2.b, 447.2.a, 447.2.b, 447.2.c** | every one opens *"In Modes of Play with more than two players"* or *"In Modes of Play with teammates"*; 485.1 puts two players at a Duel table | **Constructed and 2v2-as-two-decks.** They are live in a multiplayer mode this project does not model, and their absence is exactly what makes entry 12 legal — cited there for that reason |
| **439.2.c** | its distinction is *"Play a 1 [M] Recruit token"* against *"Create a 1 [M] Recruit token"*, and `grep -c "\bCreate\b"` over `data/corpus_flat.txt` returns **0** on 2026-09-12 — no card in the pool uses the Create wording | **today's pool.** The rule is real and would matter the moment a set prints "Create"; 355.2.a remains the citation for a token with no printed destination |
| **427.5, 428.6, 430.5, 413.3, 426.3, 415.2, 416.2.a, 422.2.a, 444.3** | formatting and definitional rows (*"This action is formatted as…"*, *"X is a state"*, *"must when instructed"*) | **new lines.** 444.3/444.3.a are cited in entry 9 as the Limited-Action frame for Pay, which is the only use they have |
| **479.2** | its worked example needs a passive *"Might increased to N"*, which CLAUDE.md already records as unprinted; re-checked and still unprinted | **today's pool**, and CLAUDE.md's existing refusal stands unchanged |
| **330.2, 421.3, 383.4.a.1, 383.4.b.1, 383.4.b.4, 383.4.c.3, 383.4.d.3** | naming and structure rows that add no mechanic on their own | **new lines**; several are cited as supporting text in the entries above |
| **355.5.b** | NOT refused — deferred. Its worked example is `OGN-289 Targon's Peak` and its content is that a delayed trigger's choices are made when the DELAYED trigger finalizes, not when the causing item does. That is a citation upgrade owed to `targons-peak-defy-delayed-ready`, whose 359.3.e.16 mechanism is different | **next batch** |

## 14. Measurement notes — three instrument failures, all caught

1. **`.scratch/have.mjs` Q2 is wrong with more than one base code.** Given `have.mjs UNL-133 UNL-050` it
   prints *"UNL-133 UNL-050: in no entry"* — it joins argv into one token — while single-arg calls are
   correct. Measured 2026-09-12: the ten cards in this batch show 0/2/1/2/3/1/2/1/5/2 entries by direct
   query and "in no entry" for all ten through the multi-arg call. **Q1's subset answer should not be
   trusted from a multi-arg call either**; the authority used here is the subset/superset sweep inside
   this lane's own validator, run over all 736 entries. Two lanes have reported this tool saving them a
   walk, so the failure mode is worth fixing rather than working around.
2. **A quote typed from CLAUDE.md rather than pasted from the rules file.** 464.2.c.3.a was written here
   as *"If a Unit or Player becomes present…"*; the real text is *"If a Unit **controlled by the Attacker
   or Defender** becomes present … , as appropriate for its controller."* CLAUDE.md's own version carries
   an ellipsis and is honest; dropping the ellipsis is what made it a paraphrase. Caught by the quote
   checker before staging, and the re-read changed entry 12's citation from 464.2.c.3.a to **464.2.c.3**,
   because the Fan is already present when the conversion cleanup opens the Combat.
3. **A count of citations is perishable.** `447.2.a` was uncited when entry 12 was drafted and cited by
   another lane's entry an hour later. The claim was rewritten to name the catalogue size it was measured
   at. The `499.md` false positive the predecessor recorded is still present — this document's own
   filename matches the rule-reference regex — and is still benign.

## 15. Corrections owed elsewhere — NOT applied, this lane owns neither file

1. **`CLAUDE.md`, the loop wall.** The file says a fifth draw is *"431.1.a, a point for the opponent
   (194.1.d)"*. Inside a loop the trash is empty by construction (416.1 recycles to the bottom of the
   DECK), so 431.3 applies and it is **431.3.a and 431.3.b** — repeated Burn Outs, unpreventable points,
   until the opponent wins. The wall is absolute, not a toll, and the two paragraphs that say so were
   cited by nothing in the project.
2. **`CLAUDE.md`, the "must be banked beforehand" sentence.** *"312.2.c is why you cannot go and find it
   … the Power must already be in the pool when the attack is declared; the answer is a 0-Energy Power
   source (`OGN-040 Seal of Rage` …)"* is corrected by **444.2.c**: a [Reaction] Add may be activated at
   the instant of payment, *"ignoring normal restrictions"*, and 164.2 makes every rune such an ability.
   The Seal is still good — it is free — but it is no longer necessary.
3. **`.scratch/have.mjs`** — the multi-arg Q2 bug in §14.1.

## 16. HANDOFF — next rows, in order

`rc-manager5` relayed rc-walk-blocks' combined survey (`.scratch-rules/survey.mjs lo hi`): the range holds
**1,264 headings and 700 uncited non-boilerplate rows** at catalogue 736, so **ranking matters far more
than counting**. The 369.2 lead is **closed by §11 above**. Next, highest first:

1. **465.2.c.1 / 465.2.c.1.a** — *"Assigning Damage is not Dealing Damage"* and *"When all Damage is
   assigned, it will be Dealt simultaneously."* Neighbour count 304, the highest in the range. The
   simultaneity half is the mechanism behind every `OGN-133 Flurry of Blades` refusal in CLAUDE.md.
2. **464.2.c.1.a / 464.2.c.1.b** — who holds Focus when a Showdown was already ongoing. Live: they forced
   a correction to a shipped entry. Directly relevant to §12, which assumes 345 + 313.3.
3. **383.4.e.1 / .e.2 / .e.3** — the sub-rules that DEFINE attack triggers, which this catalogue reasons
   about constantly and has never cited.
4. **355.10.b / 355.10.c** — two more things that are not a target, beside the .d/.e/.f already in use.
5. **323.9.a** — the standing half of the 323.10 un-staging finding.
6. **420.3.b** — *"The Effect is Moving those Units."* Composes with 446.3.c from §8.
7. **355.5.b** — the deferred Targon's Peak row from §13.

Method notes to carry: **echo the parsed range in any range-scoped probe** (that lane passed its range as
one quoted shell word and both ranges printed identical rows, which reads as a finding rather than a
bug); **run both probes**, since a heading whose siblings are all boilerplate never clears the neighbour
bar; and **re-run a surprising absence with a different instrument** — it fired again here, in §14.1.

---

# Batch 3 — 465.2.c.1.a, and a correction to the framing that asked for it

One entry, four uncited sub-rules, staged 2026-09-12 at catalogue **743** (batches 1 and 2 merged at
`6b1a300`). `rc-manager5` asked for **465.2.c.1.a**'s simultaneity half as the mechanism *"behind every
Flurry of Blades refusal in CLAUDE.md"*.

**It is not that mechanism, and the correction is the entry.** There are **two** paragraphs that make
damage simultaneous and they cover different sources:

- **370.1.a.2** (uncited) — *"An event can occur simultaneously with other events only when those events
  are all the result of the same Game Action or change in state occurring."* Its worked example is a
  **spell** killing two units at once *"because their deaths result from the same game action"*, and its
  second example is the negative case: *"Kill a friendly unit. If you do, kill an enemy unit…"* is **two**
  game actions and is **not** simultaneous. This is the paragraph under `OGN-133 Flurry of Blades`.
- **465.2.c.1.a** (uncited) — *"When all Damage is assigned, it will be Dealt simultaneously. These
  actions are not synonymous."* This is the paragraph under the **Combat Damage Step** and nothing else.

Attributing a spell sweep to the combat rule is the same class of slip as citing reminder text as rules
text: the conclusion is right and the authority is the wrong paragraph.

**465.2.c.1** (*"Assigning Damage is not Dealing Damage"*) and **417.3.a** (*"Assigning Damage causes
Damage to be dealt outside of being directed to Deal Damage"*) are the companions, both uncited. Together
with 417.1.a, which **is** cited, they say there is **no window between assignment and dealing** — so a
[Reaction] "in response to the damage" does not exist, and anything that changes a Might has to land
before 465.2.c assigns.

| id | sub-rules opened | class | cards |
|---|---|---|---|
| `keeper-of-masks-flurry-of-blades-centaur-simultaneous-fodder` | **370.1.a.2, 465.2.c.1, 465.2.c.1.a, 417.3.a** | ENGINE | UNL-081 + OGN-133 + UNL-068 |

## 17. The line, and why these three cards

Mind prints **no 1-Might fodder**: every Mind token in the pool is a 3 Might Sprite or Mech (swept over
`data/corpus_flat.txt`, 2026-09-12), so a 1-damage sweep kills none of your own. `UNL-081 Keeper of Masks`
is the exception — Might 1 himself, with two Reflections that **R27** leaves at **0 Might**, all three at a
battlefield for **one rainbow Power and no Energy** (811.1.b's tail, 811.1.d.1). 143.2.a kills all three on
one damage (`1 >= 1` and `1 >= 0`), and 816.1.b was going to kill them at your next Beginning Phase anyway,
so the fodder is free.

`UNL-068 Spectral Centaur` is the only payoff that collects the whole batch. **The same event pays three
different ways, measured over `data/corpus_flat.txt` on 2026-09-12 — five cards:** `UNL-068 Spectral
Centaur` and `UNL-129 Vicious Snapjaws` have no gate and pay **per body**; `SFD-169 Altar of Memories` is
rationed by **its own exhaust** and pays once however many die; `OGN-118 Wraith of Echoes` and `UNL-174
Shard of Undoing` say *"the first time … each turn"*, which **383.1.b** collapses to one execution.
Simultaneity is worth everything to the first pair and nothing to the other three.

`OGN-133 Flurry of Blades` is Body and the other two are Mind, so 103.1.b.1 forces **Body/Mind**, and the
census against `data/cards.json` returns exactly one name: **Defender of Tomorrow (VEN-149/VEN-194)**. A
forced shell, stated as such.

**383.4.a.4 falls on the losing side here**, which is worth recording beside §10 where it fell on the
winning side: the Reflections copy Keeper of Masks, whose only non-keyword text is a **Play Effect**, so
there is no cascade — three bodies, not seven.

**Validation, batch 3 alone against the live 743.** `validateCombos` **0 errors over 744**; no duplicate
id; no duplicate card set including `needs`; subset/superset sweep against all 743 clean both ways;
`generateVariants` 1,598; legend checks 1 base code, 0 defects. **462 rule references across all six of
this session's entries resolve; 122 quoted passages all verbatim.**

## 18. Method note — the checker caught what memory did not

Two quotes in this entry (383.1.b and 485.4) were typed from `CLAUDE.md` rather than pasted, and both
happened to be correct. That is luck, not method: the same shortcut produced §14.2's paraphrase of
464.2.c.3.a an hour earlier. **The mechanical check is what makes it safe to write quickly** — assert
every `NNN.x.y` exists and every single-quoted passage appears verbatim in the rules file, every batch,
before staging.

---

# Batch 4 — 383.4.e with 383.4.f: the missing half, not the missing paragraph

One entry, staged 2026-09-12 at catalogue **744** (batch 3 merged at `d33cbbf`).

**A correction to the brief first.** `rc-manager5` relayed that *"383.4.e.1, .e.2, .e.3 — the sub-rules
that DEFINE attack triggers — have never been cited."* Measured 2026-09-12: **`383.4.e`, `383.4.e.2`,
`383.4.e.2.a` and `383.4.e.2.b` ARE cited**, several times each, and CLAUDE.md carries the last two. What
is uncited is the naming boilerplate (`.e.1`, `.e.3`, `.f.1`, `.f.3`) and one substantive row, the defend
mirror **`383.4.f.2.b`**.

**So the finding is not a missing paragraph. It is a missing HALF.**

## 19. "When I attack or defend" is BOTH triggers, and six entries price only one

383.4.e defines an Attack Trigger as firing *"when a Unit or Player gains the Attacker designation for the
first time during a combat"*; 383.4.f is the identical sentence for the Defender designation. A card
worded **"When I attack or defend"** satisfies both, so it is an Attack Trigger *and* a Defend Trigger —
the same shape the rules already state for the other pair at **823.1.b**: *"Hunt is both a Conquer and a
Hold effect."*

The honest rate is therefore **twice per turn cycle**: once on your turn when the body attacks, once on
the opponent's turn when it defends. **Measured over the catalogue, 2026-09-12: six entries use an
attack-or-defend card and NOT ONE cites both paragraphs** — `ahri-foxfire-might-threshold`,
`rell-magnetic-recurve-bow-per-attack`, `sentinel-adept-weaponmaster-free-attach`,
`noxian-guillotine-recurve-bow-delayed-kill`, `blitzcrank-ezreal-dashing-forced-defence`,
`cataclysmic-duel-fiora-peerless-permanent-one-on-one`. Two cite `383.4.e` alone, and one is *named*
`per-attack`.

## 20. The player half is printed on one side of the game only

383.4.e.1 gives two shapes, *"When I attack…"* and *"When you attack…"*. **Measured three ways on
2026-09-12** — `grep -ic` over `data/corpus_flat.txt`, a widened `grep -oiE`, and a scan of
`data/cards.json` — **the pool contains ZERO cards worded "when you attack."** The defend side prints
four, and three of them are battlefields: `OGN-279 Fortified Position`, `OGN-285 Reaver's Row` (banned in
both formats, `data/legality.json`), `SFD-215 Ravenbloom Conservatory`, `SFD-126 Loyal Pup`.

That asymmetry explains two things this project records separately. Every attack trigger in the pool is
keyed to a **unit** and needs that unit present when 464.2.c.3 designates — the general form of the
attack-reinforcement problem (only `SFD-025 Rengar, Pouncing` and `OGN-107 Ava Achiever` lift it). The
defend side has a **player**-keyed trigger, which is why `SFD-126 Loyal Pup` need not be standing there,
and why a **battlefield** can carry a defend trigger at all: a battlefield is never a unit and never gains
the designation, so *"When you defend here"* works only through 383.4.f's player half read with 190.6.d.

| id | sub-rules opened | class | cards |
|---|---|---|---|
| `recurve-bow-loyal-pup-attack-or-defend-double-rate` | **383.4.e.1, 383.4.e.3, 383.4.f.1, 383.4.f.2.b, 383.4.f.3** | ENGINE | SFD-016 + SFD-126 |

**Validation against the live 744:** `validateCombos` **0 errors over 745**; no duplicate id; no duplicate
card set; subset/superset sweep clean both ways; `generateVariants` 1,599; legend checks 5 base codes and
4 name-beside-code pairs, 0 defects. Quotes 108, all verbatim — **the checker caught `818.1`, which I had
typed from memory as *"Equip is an Activated Ability that Attaches a card with the Equipment tag to a
unit"*; it actually reads *"Equip is an Activated Ability keyword."*** Third time this session that a
quote written from memory rather than pasted was wrong, and third time the check caught it before staging.

## 21. Refusals and upgrades from this block

| row | disposition | scope |
|---|---|---|
| **`OGN-279` + `SFD-215`** | REFUSED as a pairing. 485.4.a has each player provide three battlefields and 485.5 *"randomly selects"* which is used, so two of your own battlefields never share a board | **every format this project models** |
| **`383.4.e.1` / `.e.3` / `.f.1` / `.f.3`** | naming and wording boilerplate; cited in the entry above as supporting text, not walked on their own | **new lines** |
| **blinking a carrier to re-fire an attack trigger** | REFUSED. 383.4.e.2.a: the condition is checked *"once per combat, despite a Unit being able to gain and lose the Attacker designation multiple times in the same combat"*, with 383.4.f.2.a the mirror | **all attack and defend triggers** |

**CITATION UPGRADES OWED, flagged not applied** (this lane does not own `data/combos.json`): the six
attack-or-defend entries in §19 should cite `383.4.f` beside `383.4.e`, and
`rell-magnetic-recurve-bow-per-attack` prices the same gear on attacks alone. Separately,
`loyal-pup-jagged-cutlass-free-defensive-relay` walks the relocation without citing the paragraph that
makes it legal — `383.4.f`'s *"a Unit or Player"* is why the Pup need not be present.

---

# Batch 5 — the priority block was never cited, and an ETB chooses after it lands

Two entries, staged 2026-09-12 with the live catalogue moving from **749 to 752** under me (four lanes
share this tree). Session `rc-walk-mid`, second successor, issue **#187**, manager `rc-manager5`. The
handoff in §16 was read first; its refusals stand and none is re-opened. **`/tmp/rc-walks/rc-walk-mid.json`
was emptied to `[]` before anything was appended** — the twelve entries of batches 1–4 are all merged.

| id | sub-rules opened | class | cards |
|---|---|---|---|
| `morgana-apothecary-hextech-ray-doubling-ladder` | **355.5.b** | ENGINE | OGN-009 + VEN-017 + UNL-021 |
| `gemcraft-seer-nocturne-vision-replacement-deploy` | **370.1.b, 370.1.b.1, 370.2, 371.2, 312.1, 312.1.a, 312.1.b, 312.1.b.1, 419.1, 419.1.a, 419.2, 419.2.a, 410.1, 410.1.a, 410.1.b, 410.2.b, 316.5.a** | ENGINE | OGN-100 + OGN-194 |

## 22. The structural find: this catalogue has never cited the paragraph that says what Priority is FOR

Measured over `data/combos.json` + `data/synergies.json` on 2026-09-12, at catalogue 752:

| paragraph | citations |
|---|---|
| `312.2.a` (when a player RECEIVES priority) | **81** |
| `312.1` *"Priority is the singular exclusive right to take Discretionary Actions."* | **0** |
| `312.1.a`, `312.1.b`, `312.1.b.1` | **0** |
| `419.1`, `419.1.a`, `419.2` *"This is a Discretionary Action."*, `419.2.a` | **0** |
| `410.1`, `410.1.a`, `410.1.b`, `410.1.b.2`, `410.1.b.3`, `410.2.b` | **0** |
| `316.5.a` | **0** |
| `420.3` (the Standard Move IS a Discretionary Action) | 3, against **200** for `420.3.a` |

The project reasons about "you cannot do that outside your Main Phase" on almost every page and reaches
it through 312.2.a, 144.1.b and 316.5.b every time. The chain those derivations are shorthand for is:

- **419.2** — of Playing a card: *"This is a Discretionary Action."* (419.1.a: *"By default, a player can
  only Play cards from their hand or their Chosen Champion zone."*)
- **420.3** — *"The Standard Move inherent to Units is a Discretionary Action."*
- **421.2** — *"Hiding is a Discretionary Action."*
- **410.1.a** — *"A Discretionary Action is a Game Action that may be performed at any time during a
  player's turn during a Neutral Open State."*
- **312.1** — *"Priority is the singular exclusive right to take Discretionary Actions."*, with
  **312.1.b** *"If no player has Priority, no player can take Discretionary Actions."*

So playing a card, standard-moving and hiding are **one class of action with one timing rule**, and
`[Action]`, `[Reaction]` and `[Ambush]` are exceptions written against **410.1.a**, not against 312.2.a.

**And the escape clause is 312.1.b.1, which was cited by nothing:**

> Players can always take and make choices for Limited Actions when instructed, regardless of Priority.

Read with **410.2.a** (*"A Limited Action is a game action that a spell, ability, or circumstance of the
turn's progression causes the player to perform. A player cannot perform these actions at-will."*) and
**410.2.b**, that is the **general licence behind every "play it, ignoring its cost" effect in the pool**
— `OGN-107 Ava Achiever` throwing a [Hidden] card into a combat, `OGN-194 Nocturne` playing himself off
a look, `UNL-179 Rift Herald`'s Deathknell playing a unit on the turn it dies. This project has derived
that permission card by card; 312.1.b.1 states it once. It is the same shape as batch 2's **444.2.c**
finding (a [Reaction] Add may be activated at the instant of payment, *"ignoring normal restrictions"*):
**priority governs what you may do at will, and never what you are instructed to do.**

## 23. `morgana-apothecary-hextech-ray-doubling-ladder` — 355.5.b, an ETB chooses AFTER the body lands

**355.5** puts choices at the Make Relevant Choices step: *"If a card requires you to specifically choose
one or more Game Objects, that choice is made now."* **355.5.b**, cited by nothing, carves the exception:

> This does not include making choices for Triggered Abilities of permanents, or Delayed or Reflexive
> Triggers generated by the item being finalized, even if those abilities trigger when the chain item is
> played.

Its first worked example is a unit whose triggered ability reads *"When I'm played, kill a unit"*, and it
ends: *"The target will be chosen when the ability triggers."*

Two consequences, both general and neither stated anywhere in the catalogue:

1. **The body is already on the board when its own ETB chooses**, so it is a legal choice for its own
   trigger. `UNL-021 Grim Apothecary` (*"When you play me, you may return a friendly unit at a battlefield
   to its owner's hand"*) can return **himself**, which is what makes him an answer to targeted removal
   and not only a value bouncer.
2. **An opponent who reacts to the card BEING PLAYED is acting before the choice exists.** Their real
   window is after the trigger is finalized (312.2.c), by which time the target is locked at 337.1. Moving
   the intended victim in response to the play buys nothing; you simply pick again.

The line itself is arithmetic. `VEN-017 Morgana, Vindictive` deals *"damage to a unit equal to the damage
marked on it"* — a **doubling**, so from an opener of `d` the marked total after `k` plays is `d × 2^k`.
`OGN-009 Hextech Ray` is the cheapest 3 in Fury, and the largest printed Might in the pool is **12**,
which is **two names and not one** — `UNL-059 Master Yi, Unstoppable` and `UNL-147 Baron Nashor`
(`UNL-238` is its second printing), measured over `data/cards.json` — so **3 → 6 → 12** reaches every
unit in the game on the second Morgana. The Apothecary is what buys the second one, and 103.2.b caps him at three.

The bound is the phase order, and it closes the obvious extension: **combat damage can never be doubled.**
**465.3** reads *"2. Skip the FEPR process and cancel any outstanding tasks. Proceed to the Resolution
Step."* — no priority after 465.2.d deals combat damage — and the very next task is 466.1's Combat
Cleanup, whose 466.1.a.1 heals every unit. **143.3.b** gives exactly two healing times, 143.3.b.1 *"At the
end of each player's turn."* and 143.3.b.2 *"During a Combat Cleanup."*, so a spell's marked damage lives
for one turn and combat's does not survive its own combat.

**A measurement that corrects a CLAUDE.md inventory.** The file's sixteen-card list of removal that
reaches a base was swept with a `kill | deal N to (an) (enemy) unit` predicate. Morgana's amount is not a
numeral, so the predicate cannot see her — and her instruction carries no `at a battlefield` and no
`here`, so **she reaches an enemy unit in a base**. Re-swept 2026-09-12 for a Deal instruction naming
"a unit" with neither location clause, `data/corpus_flat.txt` returns **13 rows**, of which **ten** are
genuine enemy-facing Deals — `OGN-029`, `OGN-248`, `OGN-252`, `OGS-022`, `SFD-077`, `UNL-020`, `UNL-023`,
`UNL-067`, `UNL-073`, **`VEN-017`** — while `SFD-191 Rabadon's Deathcrown` (Bonus Damage, not a Deal) and
`UNL-019 Blighted Battleaxe` (*"deal 4 to me"*) are false positives, and `SFD-116 Yone, Blademaster` names
the base outright. **My own predicate has a false negative**: `UNL-026 Xerath, Freed` is excluded because
its *"at a battlefield"* is a restriction on USING the ability, not on the target. State the predicate
with the number.

## 24. `gemcraft-seer-nocturne-vision-replacement-deploy` — 370.1.b.1, and a correction to a shipped entry

**370.1.b.1**, cited by nothing, whose worked example is `SFD-175 Undertitan`:

> In the case of Replacement Effects that describe a game action to occur "as" an event occurs, the
> described event is replaced by that same event plus the game action being performed.

`OGN-194 Nocturne, Horrifying` reads *"As you look at or reveal me from the top of your deck, you may
banish me. If you do, you may play me for [rainbow]."* That clause is a **Replacement Effect and not a
Triggered Ability**, and `nocturne-stacked-deck-cheat` — the only entry that uses him — calls it *"the
trigger"* twice and cites **no paragraph for it at all** (its whole rule set is 103.1.b, 103.2.a, 103.2.b,
416.1). **CITATION UPGRADE AND CORRECTION OWED THERE, flagged not applied.** Three things follow from the
classification that would not follow from a trigger: it never becomes a Chain Item, so there is no window
between the look and the body arriving; no trigger multiplier in the pool reaches it (`OGN-236 Karthus`
doubles Deathknells, `UNL-087 Blue Sentinel` and `UNL-029 Red Brambleback` double hold and conquer
triggers); and **371.2** is what makes the *"you may"* legal.

`OGN-100 Gemcraft Seer` (*"Other friendly units have [Vision]"*) is what turns a one-shot look into a look
on every unit you play, including a unit played at [Ambush] or [Reaction] speed on the opponent's turn —
and that is where §22 cashes: the instructed play is a **Limited Action**, so **312.1.b.1** lets you take
it with no Priority. 355.2.a then puts Nocturne at your base **or a battlefield you control**, the
contested one included, and 319.6 with 323.2.a give a body that becomes present mid-combat its designation
at the next Cleanup — a 4 Might defender after the attackers were declared.

Two honest caps are written into the entry rather than hidden. **817.2.b**: *"If the player does not
recycle the top card and nothing else happens in between the triggers resolving, each instance of Vision
will see the same card."* — N units played is **one look with N re-rolls**, not N cards deep. And
**370.2**: a Replacement Effect applies once per event, so extra Nocturnes raise the hit rate and never
the number deployed per look.

The verb is why the family exists: Nocturne says *"look at OR reveal"* while Undertitan says only *"As I'm
revealed"*, and 436.1 defines Predict and 817.1.b defines [Vision] with *"look at"*. **Nocturne is the
printed exception to this project's standing "a predict never reveals" rule.**

## 25. Rows read this batch and returned without an entry, each with its scope

| row | disposition | scope |
|---|---|---|
| **355.10.b** | Read. Its content is real — *"Kill a unit at a battlefield"* targets the unit and **not** the battlefield, *"Kill all units at a battlefield"* targets **the battlefield and not any units**, and a destination is never a target — but **no card in the pool can read it**. Measured three ways on 2026-09-12: `grep -c target data/corpus_flat.txt` is **0**, a JS scan of the corpus for `/target/gi` is **0**, and a scan of all of `data/cards.json` is **0**. **The word "target" is printed on zero cards.** So the whole 355.10 block surfaces only through `809.1.c` ([Deflect], which taxes spells that *target* and charges per *choice*), `054.1` (*"can't be chosen"*) and the 359.3.e / 758 mistarget rules | **today's pool.** 355.10.b is the better first citation than 355.10.d for *"a battlefield-wide sweeper pays no Deflect"*, because it names the battlefield as the target rather than only excluding the units |
| **323.9.a** | Read, thin alone, one real consequence. *"The Combat remains Staged at that Battlefield as long as there are Units present from two opposing players there."* Read with **323.13** — the Turn Player chooses **one** staged Combat per Cleanup — this is the paragraph guaranteeing that the **un-chosen** Combat is still there after the first resolves. CLAUDE.md records 461.1 for *which* combat resolves first and has nothing for *why the other one survives* | **new lines and citation upgrades**: any two-front attack entry should cite 323.9.a beside 461.1. It is the standing half of the 323.10 un-staging finding, exactly as the handoff said |
| **420.3.b** | Read with 420.3 and 420.3.a. The word that does work is **"those"**: *"The Cost is Exhausting one or more Units"* / *"The Effect is Moving those Units."* — the cost-set and the effect-set are the **same set**, so a Standard Move can never exhaust one body to move another, and with 144.3 (a group move is one action) the group that pays is exactly the group that walks | **citation upgrade.** 200 entries cite 420.3.a and 3 cite 420.3; none cites 420.3.b, and none cites 410.1.a, which is where the Standard Move's *window* actually lives |
| **356.3** | Read. *"Apply cost increases."* at step 3, before **356.4** *"Apply discounts."* The consequence is a **limit, not a line**: an increase lands on the printed cost first, so it is the first thing any of the opponent's discounts eats, and 356.6 floors the result at 0. A tax is therefore worth its full face value only against an opponent whose discounts were not already overshooting; 356.4.e (*"If a discount applies a minimum cost, that minimum applies only to that discount"*) is what lets a floored discount waste itself instead | **citation upgrade owed to `mystic-vortex-helm-suppression-tax` and `helm-empowered-spell-tax`**, which price the pool's only three increase rows (`VEN-045`, `VEN-160`, `UNL-219`) and cite neither 356.3 nor 356.4 |
| **`UNL-219 Vaults of Helia`** | **The two existing refusals STAND and I did not re-open them.** `docs/phase0/walks/2026-09-07-uncatalogued-cards.md` §3.3 and `docs/phase0/walks/2026-09-07-orphan-remainder.md` both refuse it as a pure drawback, and §3.3's reason — *"A token-only deck ignores it, which makes it a blank rather than a synergy"* — is precisely the angle 356.3 and 185.3.a.1 would have suggested. Checked and left alone | **unchanged.** 356.3 supplies a second, independent reason: the tax lands before discounts, so it is the first thing anything refunds |
| **355.5.a** | Already cited 6 times; read only for its contrast with 355.5.b (*"Stun a unit at a battlefield" is a Choice / "Kill all gear" is not*) | **no action** |

## 26. Citation upgrades owed elsewhere — flagged, NOT applied (this lane owns neither file)

1. **`nocturne-stacked-deck-cheat` is wrong on a term.** It calls Nocturne's *"As you look at or reveal me"*
   clause a **trigger**. Under **370.1.b.1** it is a **Replacement Effect**; the entry should carry
   370.1.b, 370.1.b.1, 370.2 and 371.2, and the three consequences in §24.
2. **The five `SFD-175 Undertitan` entries** (`reksai-undertitan-reveal`, `void-rush-undertitan-accelerate`,
   `diana-lunari-showdown-scry`, `teemo-strategist-undertitan-mind-order-reveal`,
   `undertitan-pakaa-signpost-move-reveal`) should cite **370.1.b.1**, which names Undertitan in its own
   worked example — the debt rc-walk-blocks first flagged in its §H5.
3. **`targons-peak-defy-delayed-ready`** should cite **355.5.b**, whose *second* worked example is
   `OGN-289 Targon's Peak` by name: the choice of which runes to ready is made when the **delayed** trigger
   finalizes at the end of the turn, not when the conquer effect does.
4. **`mystic-vortex-helm-suppression-tax`** and **`helm-empowered-spell-tax`** should cite **356.3** with
   **356.4**, per §25.
5. **Every entry that prices a Standard Move** should have **410.1.a** available for the window and
   **420.3.b** for the cost-set/effect-set identity; and **312.1** with **312.1.b.1** is the general
   citation for "an instructed play needs no priority", which the catalogue currently derives per card.

## 27. Validation, batch 5 alone against the live catalogue

`data/combos.json` was **never written by this session.** Over a merged copy: `validateCombos` **0 errors
over 754 entries**; no duplicate id; no duplicate card set including `needs`; subset/superset sweep of both
card sets against every live entry, **no hit in either direction**; `generateVariants` **1,617**. Legend
checks replicated: **4 legend base codes resolved through `poolOf`, 0 defects**, and all eight codes named
in `gemcraft-seer-nocturne-vision-replacement-deploy` were generated from `data/cards.json` and re-verified
name by name. **61 distinct rule references, all resolve** (tokenizer and `^\s*` anchor taken from the
committed `test/rule-refs.test.ts` rather than rebuilt). **21 quoted passages checked from an EXPLICIT
list, 0 defects** after two repairs.

## 28. Method notes

1. **The quote check caught an elision with no ellipsis, again.** A notable quoted 355.5 as *"…that choice
   is made now'"* — dropping the closing period of the rules sentence. It reads as complete and is not.
   Fourth time this lane has caught a quote written quickly rather than pasted.
2. **The live catalogue moved 749 → 752 while this batch was being written**, so the subset sweep and the
   entry count were re-run immediately before reporting, not after writing. A count is perishable; the rule
   is not.
3. **A surprising absence was re-run with a different instrument.** "No card prints the word *target*" is
   the kind of claim this project has been burned by, so it was measured three ways (§25).
4. **`generateVariants(combos, cards)` takes the CardIndex, not the features list**, and `data/cards.json`
   is `{cards: […]}` while `data/features.json` is `{features: […]}` — use `loadCombos()` and
   `loadCardIndex()` from `src/load.ts` instead of re-reading the files, which is what a first draft did
   and what produced two "is not iterable" crashes.
5. **A /tmp script cannot import `../src`.** Scratch probes belong in `.scratch/` (gitignored, line 14 of
   `.gitignore`), not in `/tmp/rc-walks/`.

## 29. HANDOFF — next rows for this lane, in order

1. **`359.3.e.4` and `359.3.e.12` / `.12.a`** — uncited. *"If a target changes Zones to or from a Non-Board
   Zone and then returns to its original zone, it is no longer a legal target, because it's not treated as
   the same object."* with three worked examples. It is the paragraph under every bounce-as-an-answer line
   and under `UNL-021 Grim Apothecary` used defensively, which this batch deliberately did **not** build.
2. **`312.1` / `419.2` / `410.1` as an ENTRY rather than a §22 finding** — the block is opened here as
   supporting text in one entry; a line built on 312.1.b.1 (an instructed play on the opponent's turn that
   the opponent cannot answer because no Chain Item exists) has not been written.
3. **`416.2.a`**, **`422.2.a`**, **`443.2.a` / `444.2`** (`UNL-079 Diana, Lunari`), **`483.4` / `483.6`**
   (`Sanction`) — the handoff's batch-4 list, untouched.
4. The **489** (2v2) rows — 489.5.a, 489.6, 489.7, 489.8.f, 489.8.g, 489.8.g.1. Expect leads, not entries:
   `matchDeck` reads one list.
5. **`323.9.a` and `420.3.b`** are read (§25) and need only an entry that wants them.

---

# Batch 6 — a bounce is a counterspell, and the rules name a card this catalogue already builds on

One entry, staged 2026-09-12. Batch 5's two were merged by `rc-manager5` while this was being written
(live catalogue **755**), so the staging file was trimmed to the unmerged row before validating — the
trim is by ID against `data/combos.json`, not by memory of what was sent.

| id | sub-rules opened | class | cards |
|---|---|---|---|
| `windsinger-gust-bounce-as-a-counterspell` | **359.3.e.4, 359.3.e.12, 359.3.e.12.a** | ENGINE | SFD-138 + OGN-169 |

## 30. 359.3.e.4 — leaving the battlefield is what makes a bounce an answer

> If a target changes Zones to or from a Non-Board Zone and then returns to its original zone, it is no
> longer a legal target, because it's not treated as the same object.

Its three worked examples are the whole content, and the first is the general case:

> An enemy unit at a battlefield is no longer a legal target if it is no longer an enemy, no longer a
> unit, or no longer at a battlefield.

**359.3.e.5** (cited twice) then says what happens: *"If any of the spell's targets are no longer legal,
those game objects, players, or zones are unaffected by the spell as it resolves."*

So every removal spell in the pool that reads *"a unit **at a battlefield**"* is answered by moving the
body off the battlefield in response, and `SFD-138 Windsinger` does it for **zero Energy** (811.1.b plays
a hidden card *"ignoring its base cost"* and its tail grants it [Reaction]) while `OGN-169 Gust` does it
for **one**. It does **not** answer the other half — the ten Deal instructions and the longer list of
kills that name *"a unit"* with no location clause, measured in §23 — and the entry says so.

**The counterplay is printed in the same paragraph and costs one Might.** 359.3.e.4's second example:
*"A unit with 3 or less Might is no longer a legal target if it is no longer a unit or if its Might is
greater than 3."* Both of these cards carry that ceiling, so any +1 Might at [Reaction] speed makes THEM
mistarget, and 477.3.e.2.a applies increases before decreases so the pump always wins the race.

The third example is a standing warning: *"Something that's exhausted is no longer a legal target if it is
no longer exhausted."* The pool prints exactly two cards that name an exhausted friendly unit as a target
(`OGN-124 Arena Bar`, `UNL-201 Voidreaver`, the sweep CLAUDE.md already carries), and a ready in response
makes both mistarget.

## 31. 359.3.e.12 — and the Core Rules name `OGN-242 Baited Hook` in the example that blanks it

> If the spell checks information about a target that is no longer legal or a card or permanent whose
> location, zone, or status has changed such that that information is no longer available, that check
> returns "null" and all calculations based on it are ignored.

Riot's first worked example quotes Baited Hook's whole text and ends:

> Baited Hook's controller looks at the top 5 cards of their Main Deck, but can't choose any unit from
> among them.

An opponent returns the friendly unit to hand in response; it cannot be killed, its Might is null, the
whole payload evaporates, and the 1 Energy, the rainbow and the exhaust are already spent. **Three shipped
entries stand on that card — `baited-hook-sprite-queen-free-unit`, `vanguard-helm-baited-hook-buff-ladder`,
`baited-hook-galio-printed-might-fetch` — and none of them mentions the counterplay or cites the
paragraph.** The bodies a Baited Hook ladder starts on are small, which is exactly the range a 3 Might
bounce covers.

Its second worked example is `SFD-107 Strike Down` answered by `SFD-011 Angle Shot`, which is the
enemy-facing Angle Shot use CLAUDE.md flagged as uncatalogued and which another lane catalogued today as
`angle-shot-battleaxe-tryndamere-excess` — **that entry cites 26 paragraphs and 359.3.e.12 is not one of
them**, though the rules use its two cards as their own example.

**359.3.e.12.a** is the complement (*"...that information is accessible"*) and **359.3.e.13** is the other
direction — a spell that moves something to another zone **as its own** cost or effect may look back at
what it was — so a Baited Hook that resolves undisturbed reads the Might of the body it killed. Only an
**outside** zone change nulls it.

## 32. Citation upgrades owed from this batch — flagged, NOT applied

1. **`baited-hook-sprite-queen-free-unit`, `vanguard-helm-baited-hook-buff-ladder`,
   `baited-hook-galio-printed-might-fetch`** — 359.3.e.12 names their card and describes the one-Energy
   answer that blanks the whole ability. None cites it.
2. **`angle-shot-battleaxe-tryndamere-excess`** — 359.3.e.12's second worked example is Strike Down
   answered by Angle Shot, i.e. that entry's own detach mode, by name.
3. **`bone-skewer-vex-opponent-play-scheduler`** — the opponent playing a unit on YOUR turn is
   **312.1.b.1** (*"Players can always take and make choices for Limited Actions when instructed,
   regardless of Priority"*) read with 410.2.a; the entry derives it without the paragraph.
4. **`tideturner-mega-mech-swap` and `blade-dancer-tideturner-free-swap-and-ready`** — 811.1.d.2's escape
   clause names Tideturner, which CLAUDE.md records; separately, **355.5.b** is why the swap partner is
   chosen after Tideturner has landed.

## 33. Standing count for this lane

Across batches 5 and 6, **sub-rules opened that nothing in `data/combos.json` or `data/synergies.json`
had cited**: 355.5.b, 359.3.e.4, 359.3.e.12, 359.3.e.12.a, 370.1.b, 370.1.b.1, 370.2, 371.2, 312.1,
312.1.a, 312.1.b, 312.1.b.1, 316.5.a, 410.1, 410.1.a, 410.1.b, 410.2.b, 419.1, 419.1.a, 419.2, 419.2.a —
**21 in all**, of which the 312 / 410 / 419 block is the structural find of §22. Read and returned with
scope: 355.10.b, 323.9.a, 420.3.b, 356.3, and `UNL-219 Vaults of Helia` (refusals checked, they stand).
Citation upgrades flagged and not applied: **nine entries** across §26 and §32.

---

# Batch 7 — a findings batch with no entry, and it says so

Three rows read, **no entry staged**. Two are structural holes of the same shape as §22 — the catalogue
cites an INSTANCE hundreds of times and has never cited the DEFINITION — and the third refines an existing
refusal rather than overturning it. Staging file unchanged (one unmerged entry, batch 6's).

## 34. Rule 483 is the schema behind 485.4, and all of 483 is uncited

Measured over `data/combos.json` + `data/synergies.json`, 2026-09-12 at catalogue 755:

| paragraph | citations |
|---|---|
| `485.4` (*"Battlefield Count: 2"* for the Duel) | **274** |
| `485.4.a` (*"Each player provides three (3) Battlefields…"*) | **193** |
| `483.1` … `483.8`, every one | **0** |

**483 is where those field names are defined**, and 483.4 is the one that matters:

> **483.4.** Battlefield Count: Determines how many Battlefields are in play, contributed by players,
> during play.

CLAUDE.md's Baron Pit finding (#187 batch 16) argues from 485.4 read against 485.4.a that *"Battlefield
Count: 2"* is a **setup** parameter and not a per-turn cap, so `UNL-147 Baron Nashor`'s Baron Pit token
makes the Score ceiling three. **483.4 says it outright**: the Count is of battlefields *contributed by
players*. A battlefield token added by a card is contributed by a CARD, and 187.9 defines it in the rules
themselves. The derivation is right and now it has a paragraph.

**483.4.b** is the other half nobody has used — *"This may involve utilizing less Battlefields than there
are players."* — which is the general statement behind 485.4's 2 against 485.4.a's 3 each. **483.6**
(*"Format: Conditions to win or additional rules added over play."*) is the field 489.3's *"Victory Score:
11"* instantiates, i.e. the schema behind the 2v2 finding this project already carries.

**CITATION UPGRADE OWED, flagged not applied**, to the three Baron Pit entries —
`baron-pit-illaoi-third-battlefield-holds`, `baron-pit-fae-porter-reach-from-anywhere`,
`shady-spectacles-baron-copy` — and to the CLAUDE.md paragraph that states the finding.

## 35. The two halves of rule 410, now both measured

§22 opened the Discretionary half. The Limited half is the same size and is equally uncited:

| the Discretionary actions (410.1.a: your turn, Neutral Open State) | the Limited actions (410.2.b: only when instructed) |
|---|---|
| **419.2** Playing a card — *"This is a Discretionary Action."* (0 citations) | **410.2.b**'s own example: **Drawing** (0) |
| **420.3** the Standard Move (3, against 200 for 420.3.a) | **416.2** Recycling — *"This is a Limited Action."* (0), with **416.2.a** (0) |
| **421.2** Hiding (2) | **422.2** Discarding — *"Discarding is a Limited Action."* (0), with **422.2.a** (0) |
| | **443.3** Skipping (0), with **443.3.a** (0) |

Two consequences worth carrying, neither of them an entry on its own:

1. **Hiding is confined to your own Main Phase by 421.2 read with 410.1.a**, not by anything in 811.
   This project records that the hide and the play are a turn apart (811.1.b's tail) and never says why
   the hide itself cannot happen at [Reaction] speed. It is because Hiding is Discretionary.
2. **An instructed recycle or discard needs no priority and happens on anybody's turn**, by 312.1.b.1 read
   with 416.2.a and 422.2.a. That is the general form of what the catalogue asserts card by card — and it
   is the same sentence that licenses an instructed PLAY, which is §24's mechanism. Rule 410 is one
   distinction doing all of this work, and the project has been re-deriving it per card.

## 36. A refinement to rc-walk-blocks' 443 refusal, not an overturn

Its refusal reads *"exactly one card prints Skip, **zero cards trigger on the Draw Phase**"*, scoped to
new mechanics. Both halves are true and I re-measured them. **The refusal is scoped to the Draw PHASE, and
drawing is a different question.** Swept over `data/corpus_flat.txt` on 2026-09-12: **`UNL-074 Frigid
Jewel`** (*"When you draw your second card each turn, give a friendly unit +2 Might this turn"*) is the
pool's **only** card that triggers on drawing, and `VEN-022 Endless Riches` is the pool's only Skip
(*"Skip your Draw Phase"*). 315.4.b is the draw that phase performs, so **443.2** (*"When an event or
procedure of the turn is Skipped, it is replaced with nothing"*) and **443.2.a** (*"Anything that triggers
on the occurrence of the event or procedure of the turn does not trigger"*) make Endless Riches remove the
first of the Jewel's two draws. **443.4** adds that *"Skip is a Replacement Effect"*, so 370.2 caps it at
one application per event exactly as it caps Nocturne's clause in §24.

It is an anti-synergy note of one card against one card, which is why it stays a note. The refusal stands
with its scope widened by one sentence: **zero cards trigger on the Draw Phase, and exactly one triggers
on drawing.**

## 37. Why there is no entry in this batch

All three rows are **definitions of machinery the catalogue already uses correctly**. Rule 483 changes no
line; rule 410's two halves change no line; 443 reaches one card pair and the existing refusal covers it.
Manufacturing a card set to carry them would have produced exactly the shape this project warns about — a
row whose second card is doing no work. The deliverables are the three measurements, the citation upgrade
in §34 and the refusal refinement in §36.

---

# Batch 8 — a second findings batch: the Add model is universal, a "you may" is a blind commitment, and a refusal re-checked and confirmed

No entry staged. Staging file unchanged (one unmerged entry, batch 6's). The probe was rebuilt for this
batch and **echoes its parsed range on the first line of its own output** (`.scratch/mid-probe.mjs`, which
is gitignored): of **124** uncited headings carrying an Example in 300–499 at catalogue 755, **41 name a
card in `data/cards.json`**. That is the working queue for whoever holds this lane next.

## 38. 429.2.b is dead letter in this pool, and that makes the project's Add model UNIVERSAL

**429.2** (cited 49 times) and **429.2.a** (28) say a Triggered or Activated ability that Adds resources
*"resolve as soon as they are finalized"* and *"will resolve before any other outstanding items on the
chain are finalized."* **429.2.b, cited by nothing, is the exception:**

> Spells that Add resources will linger on the chain as normal when they are finalized.

So a spell that adds mana would be counterable and could not be used to pay for something already being
finalized, while an ability can (429.3, 444.2.c). **Measured 2026-09-12 over `data/corpus_flat.txt`: of
the pool's 233 spells, ZERO add resources.** The single regex hit is `UNL-073 Deadly Flourish`, and the
`[Add]` in it belongs to the reminder text of the Gold gear **token** it plays.

**Every Add in this pool is a Triggered or Activated Ability**, so 429.2's instant resolution is universal
here and there is no Add in the game that can be countered. That is worth more than an entry: it closes a
question the mana ledgers have never had to ask. *Scope: today's pool. The rule is live and would matter
the moment a set prints a spell that adds.*

**429.3's second worked example is the same finding batch 2 made from 444.2.c**, and it names a pool card:
*"A player moves two units to a battlefield where their opponent controls a Mageseeker Investigator.
Although they have no window of priority, they may activate Add Reactions to pay for the applied cost from
Mageseeker Investigator, and those abilities finalize and resolve immediately."* Two paragraphs, one
finding, both now recorded.

## 39. 383.3.e.2 — a leading "you may" is a BLIND commitment, and declining removes the trigger entirely

**383.3.e.2, 383.3.e.2.a and 383.3.e.2.b are all uncited:**

> **383.3.e.2.a.** During finalization of the Triggered Ability, the player who controls the Triggered
> Ability may choose to perform it.
> **383.3.e.2.b.** If they do not, it is removed from the chain.

Its worked example names `Stalwart Poro` and settles the once-per-turn half: declining does **not** spend a
*"Once each turn"* trigger. rc-walk-blocks reached that half in its batch 10 and refused the row because
none of the pool's four *"once each turn"* cards has the *"you may"* shape. **The general half is live and
nobody has stated it.** The decision is made **during finalization**, and 312.2.c withholds priority in a
Closed State *"until all pending chain items finish being finalized"* — so **every "you may pay X to Y"
trigger in the pool is committed to before you see a single response.** This project already records that
such a cost is PAID at finalization (204.3.a, 383.3.b.1); 383.3.e.2.a adds that the DECISION is made there
too. And 383.3.e.2.b means a declined trigger never becomes a resolved chain item at all, so nothing that
watches abilities resolving can see it.

## 40. 390.5.c.1 and 392 — a delayed ability follows what it affected into Banishment

**390.5, all of 390.5.a–c.2, 391 and 392 are cited by nothing.** 392 is the one with printed reminder text
in this pool:

> Delayed Abilities are not associated with Units or Gear; they are created by other Abilities or Spells.
> As such they are executed when their condition and/or specified time occurs regardless of whether the
> source of the Delayed Ability is still on the board or not.

`UNL-169 Ashe, Focused` prints *"When they hold, return it to their hand (even if I'm no longer on the
board)"* — **that parenthetical is reminder text for 392**, and 390.5.c.1 supplies the zone: *"If the
Linked Ability causes the affected Game Objects to change zone to a non-board zone, the appropriate zone
for the Delayed Linked Ability will be the zone the Game Object is moved to."* The banished card is in
Banishment, so that is where the delayed return's window lives, and killing Ashe does nothing to it.

**CITATION UPGRADE OWED to `ashe-focused-scuttle-crab-hand-snapshot`**, which is this lane's own batch-1
entry: it cites 424.1 through 424.4.a for the reveal and nothing at all for the delayed return that is
half the card. 391's two worked examples are `Ravenborn Tome` (a Delayed Passive) and `Noxian Guillotine`
(a Delayed Triggered), both printed in this pool and both uncited.

## 41. Refusals re-checked this batch, and both CONFIRMED

| block | verdict |
|---|---|
| **478 / 479 Dependency** | **CLAUDE.md's refusal is right and I re-read the whole block to check it.** 478.1.a–c are general criteria, but every worked example in 479, 479.1 and 479.2 needs a passive reading *"Units you control here have their Might increased to 5 [M]"*, and no card prints it. `OGN-058 Discipline` appears only as the OTHER half of the pair. The refusal stands verbatim, including its own instruction to re-read the block if a future set prints that shape |
| **416.5.a** | Confirmed as rc-walk-blocks left it: two cards recycled to the **Rune** Deck go back in the owner's chosen order while the Main Deck is random, and no card recycles two runes in one action |
| **417.1.b / .c / .d** | Confirmed definitional. One line is worth keeping for the damage entries: **417.1.c**, *"Damage is marked on each unit separately"*, is why a sweep that marks 1 on every body gives each of them its own counter rather than a shared pool — the fact `morgana-apothecary-hextech-ray-doubling-ladder` reads one body's counter relies on |
| **358.4** | Confirmed procedural, and already cited 9 times. **358.1** and **358.2** (*"Check that all chosen targets are legal."* / *"Check that all costs were paid."*) are uncited but are the PLAY-time checks, whose failure mode is 358.5 — *"the actions taken in this process are undone and the action is cancelled"* — which is a different outcome from a spell that resolves and mistargets (359.3.e.5). Two outcomes that are easy to conflate, recorded here with both citations |

## 42. Running totals for this lane

- **Entries staged across batches 5–8: three**, of which two are merged and one is staged.
- **Sub-rules opened that nothing had cited: 21** (§33), plus the rows named in §38–§40 as findings rather
  than entry citations: 429.2.b, 383.3.e.2, 383.3.e.2.a, 383.3.e.2.b, 390.5 and its five sub-rules, 391,
  392, 358.1, 358.2, 416.2, 416.2.a, 422.2, 422.2.a, 443.3, 443.3.a, 483.1–483.8, 437.6.a.
- **Citation upgrades flagged and not applied: thirteen entries** across §26, §32, §34 and §40.
- **Refusals re-checked and confirmed: four** (§41), plus `UNL-219` in §25 and the 443 refinement in §36.

---

# Batch 9 — the delayed-ability vein is upgrades and not rows, a precision correction I nearly shipped, and a self-audit of this lane's whole output

No entry staged. `rc-manager5` asked for `390.5.c.1` / `.c.2` on Ravenborn Tome and Noxian Guillotine **as
an entry**; the answer is below with the measurement, and it is no.

## 43. A LEDGER CORRECTION TO MY OWN REPORTS: the citation-upgrade debt is ZERO, not thirteen

Batches 5–8 reported a running total of "thirteen citation upgrades flagged and not applied". **That number
was wrong the moment it was written.** `rc-manager5` applies each upgrade the same turn it is flagged, in
commit messages this lane does not see, and as of `da022b5` every one is applied — including the 392 upgrade
to `ashe-focused-scuttle-crab-hand-snapshot` from §40. The standing rule this violates is the project's own:
**an upgrade recorded as owed and then silently applied is how a debt list stops being trustworthy.** The fix
is one line of method: **re-read `data/combos.json` before carrying any debt forward**, exactly as a count of
entries is re-run before it is reported. Flag an upgrade once; never carry it.

## 44. Why 390.5 / 391 / 392 yields no entry: every card in the vein is already catalogued

**Swept `data/corpus_flat.txt` on 2026-09-12 for the delayed-ability wording** (`the next spell`, `the next
time`, `the next card`, `the next unit`): the pool prints **eleven** such cards, and **every one of them is
already in at least one entry**:

| card | shape | entries |
|---|---|---|
| `OGN-021 Sun Disc` | Delayed Passive | `vayne-hunter-sun-disc-two-conquers` |
| `OGN-023 Unlicensed Armory` | Delayed Replacement | `jinx-rebel-unlicensed-armory-repeatable-shield` |
| `OGN-031 Raging Firebrand` | Delayed Passive | `raging-firebrand-thermo-beam-free-gear-wipe` |
| `OGN-032 Ravenborn Tome` | Delayed Passive (**391's own example**) | two |
| `OGN-254 Noxian Guillotine` | Delayed Triggered (**391's own example**) | two |
| `OGS-020 Highlander` | Delayed Replacement | `highlander-towering-combatant-spell-shield` |
| `SFD-078 Temporal Portal` | Delayed Passive | two |
| `SFD-194 Counter Strike` | Delayed Replacement | two |
| `UNL-052 Nami, Headstrong` | Delayed Triggered | two |
| `UNL-175 Tactical Retreat` | Delayed Replacement | two |
| `VEN-044 Astral Heron` | Delayed Passive | `astral-heron-premonition-draw` |

**Zero uncatalogued.** So the vein is real and its product is **citation upgrades and one precision
correction**, not rows. Manufacturing a twelfth pairing out of these eleven would have produced the shape
§37 refused. The upgrades are named in §46 and are flagged **once**, per §43.

## 45. THE PRECISION CORRECTION, AND I NEARLY SHIPPED THE MISAPPLICATION

I was about to cite **390.5** for `OGN-031 Raging Firebrand` and `OGN-032 Ravenborn Tome`. **That is wrong**,
and the paragraph says so in its own first sentence:

> **390.5.** Delayed Linked Abilities are Linked Abilities that are generated by another Ability and
> reference that Ability or Game Objects it affects **instead of a window of time**.

A delay written *"the next spell you play **this turn**"* references **a window of time**, so it is a plain
**Delayed Ability under 391** and 390.5 never engages. The 390.5 family is the other kind — a delay that
points at the **object** the first ability affected, which is `UNL-169 Ashe, Focused`'s *"When they hold,
return **it**"*. That is why 390.5.c.1 (the window follows the object into the non-board zone) is Ashe's
paragraph and nobody else's in this pool.

**The split, stated once so the next lane does not re-derive it:**

- **391** — delay keyed to a WINDOW OF TIME. Seven of the eleven: Sun Disc, Raging Firebrand, Ravenborn
  Tome, Temporal Portal, Astral Heron, Nami, and the Guillotine's *"the next time it takes damage this
  turn"* insofar as it also names a window.
- **390.5** — delay keyed to the OBJECT the generating ability affected. Ashe, and the four *"Choose a
  friendly unit. The next time **it** would die this turn"* shields (`OGN-023`, `OGS-020`, `SFD-194`,
  `UNL-175`), whose window is 390.5.a: *"If that Ability affects a Game Object, the Delayed Linked
  Ability's window will be as long as that Game Object is in an appropriate zone."*
- **392** — applies to BOTH: *"Delayed Abilities are not associated with Units or Gear … executed when
  their condition and/or specified time occurs regardless of whether the source of the Delayed Ability is
  still on the board or not."*

**And 390.5.a has teeth the shield family never states: a delayed "would die" shield is ended by a BOUNCE,
not only by removal.** The chosen unit leaves the board, its window closes, and 359.3.e.4 makes the body
that comes back a different object, so replaying it does not restore the shield. Four shield cards, four
entries, and not one of them says that the cheapest answer to a shield is `OGN-169 Gust` at one Energy —
which is this lane's own batch-6 entry reached from the opposite side.

## 46. Upgrades flagged ONCE (see §43 — not carried forward)

`jinx-rebel-unlicensed-armory-repeatable-shield`, `highlander-towering-combatant-spell-shield`,
`counter-strike-tianna-crownguard-in-place-save`, `vilemaw-counter-strike-ambush-undo-is-free`,
`spirit-wheel-tactical-retreat-reaction-draw`, `hidden-blade-tactical-retreat-linked-instruction-draw` —
**390.5.a** for the window and **392** for surviving the source. `raging-firebrand-thermo-beam-free-gear-wipe`,
the two Ravenborn Tome entries, `astral-heron-premonition-draw`, `vayne-hunter-sun-disc-two-conquers`,
`veiled-temple-temporal-portal-second-repeat`, `platewyrm-egg-defender-gate` — **391** and **392**, and NOT
390.5, per §45.

## 47. Two smaller findings from the same sweep

**"Enters ready" is not Accelerate's private problem.** CLAUDE.md records 805.6 and 805.6.a — Accelerate is a
delayed replacement and *"will not interact with, or trigger, abilities that are affected by units becoming
ready"* — and the pool's three become-ready payoffs are `OGN-143 Pirate's Haven`, `VEN-071 Fretful Feline`
and `VEN-088 Jayce, Hammer in Hand`, all mono-Body (re-measured 2026-09-12, still exactly three).
**`OGN-021 Sun Disc` grants the same property with no keyword at all** — *"The next unit you play this turn
enters ready"* — and 415.1 (Readying is marking a Game Object **already on the board**) closes it for the
same reason 805.6.a closes Accelerate. So the exclusion generalises: **no "enters ready" grant in the pool,
keyworded or not, ever fires a become-ready payoff**, and a Fury/Body line reaching for the Sun Disc to fire
one is refuted on its face.

**A −5 discount bought with a 6-Energy body is never Energy-positive.** `OGN-031 Raging Firebrand` costs
E6 + 1 Fury and takes exactly one spell down by 5, all in the same turn, so the pair always spends one more
Energy than the spell alone. It pays for the 4 Might body and for crossing a threshold (Thermo Beam to zero),
and its entry correctly claims neither more nor less — checked before writing this, because a discount that
costs more than it saves is the kind of claim this catalogue has had to correct before.

## 48. Self-audit of this lane's whole output, batches 1–6

Run over all **fifteen** entries this lane has produced since 2026-09-09 (fourteen merged, one staged):

- **975 rule-reference mentions, 0 unresolved**, using the tokenizer and `^\s*` anchor from the committed
  `test/rule-refs.test.ts`.
- **201 quoted spans inside `sources[].quote` fields whose `kind` is `riot`: 0 not verbatim** after
  normalising whitespace and curly quotes.
- **32 `uses[]` rows, 0 without a row in `data/corpus_flat.txt`.**

**And the audit's first instrument was wrong in exactly the way my own brief predicted.** I began by
extracting every single-quoted span of 45+ characters from the whole JSON of each entry and checking it
against the rules files. It reported **127 failures**, and every one was an artifact: an apostrophe in prose
opening a span (`s worked example is…`), a passage of **card text** that correctly lives in
`data/corpus_flat.txt` and not in the rules, or `windsinger-gust-bounce-as-a-counterspell` counted twice
because it is in both the merged catalogue and my staging file (the run printed *"16 of 15 expected"*, which
is what gave it away). The brief for this session says in so many words: *run an EXPLICIT list of the
passages an entry claims to quote, never a regex over the whole JSON.* I built the forbidden probe anyway
and it cost a full run. **The count that contradicts a small structural fact is the tell** — 127 failures
against 0 failures on the same entries an hour earlier.

## 49. Leads for `rc-syn17`, which is mining walk documents for SYNERGY RULES

Stated as anchor + partner predicate, since that lane's unit is a pattern and not a line:

1. **Anchor: each of the four delayed "would die" shields.** Partner predicate: any card that returns a
   friendly unit to hand or moves it to a non-board zone. **`excludes` with its reason: 390.5.a ends the
   shield's window when the chosen unit leaves the board, so your own bounce cancels your own shield.**
2. **Anchor: `OGN-021 Sun Disc`.** `excludes` the three become-ready payoffs, per §47 — an `excludes` rule
   with a rules reason rather than a text predicate.
3. **Anchor: `OGN-169 Gust` and `SFD-138 Windsinger`.** Partner predicate: any spell or ability that checks
   information about a target (`equal to`, `up to`, `with Might`), because 359.3.e.12 nulls the check. The
   worked example the rules themselves give is `OGN-242 Baited Hook`.
4. **Anchor: any "you may pay X to Y" trigger.** Not a pairing but a NOTE for every such rule: 383.3.e.2.a
   makes the decision at finalization, before any response exists.

---

# Batch 10 — the pool's only removal that neither targets nor chooses, and a CLAUDE.md quote that is a paraphrase

One entry, staged at catalogue **757** (batch 6's was merged while batch 9 was being written, so the
staging file was trimmed by ID again before validating).

| id | class | cards |
|---|---|---|
| `kings-edict-sandshifter-targeted-and-untargeted-order-removal` | ENGINE | OGN-237 + SFD-158 |

## 50. Both cards were in ZERO entries, which closes two of the eleven walkable leftovers

Census run over `poolOf(cards)` folded by name+type, 2026-09-12 at catalogue 756: **15 deckable name+type
groups are in no entry**, of which four are banned in both formats (`OGN-168 Fight or Flight`,
`SFD-122 Called Shot`, `OGN-182 Scrapheap`, `SFD-020 Draven, Vanquisher` — checked against
`data/legality.json`, whose `entries[].bases` is an array), leaving **eleven walkable**. This entry closes
two: `OGN-237 King's Edict` and `SFD-158 Sandshifter`. The nine that remain are `OGN-071 Party Favors`,
`OGN-171 Mystic Poro`, `OGN-175 Shipyard Skulker` (a printed blank), `SFD-096 Laurent Bladekeeper`,
`SFD-098 Sea Monkey`, `UNL-092 Demacian Diplomat`, `VEN-026 Field Musicians`, `VEN-048 Cloud Drake` and
`VEN-131 Decree of Unity` — named so the next lane can refute the count in one query.

## 51. 355.10.e, and the six bodies nothing else in the pool can kill

Swept 2026-09-12 for *"can't be chosen"*: **six cards** — `SFD-105 Ruin Runner`, `UNL-057 Alpha Wildclaw`,
`UNL-059 Master Yi, Unstoppable`, `UNL-147 Baron Nashor`, `VEN-031 Twilight Shroud` (grants it for a turn)
and `VEN-038 Akali, Silent` (*"unless I'm in combat"*). **054.1** makes every one absolute where 809.1.c
only prices [Deflect], and **355.10.e** is the hole: *"It is part of a set of objects chosen in whole or in
part by other players."*

**`OGN-237 King's Edict` is the only card in the pool whose 355.10.e set is ONE-SIDED.** Swept: `OGN-209
Cull the Weak` (*"Each player kills one of their units."*) and `UNL-164 Safety Inspector` (*"each player
must kill one of their units"*) both include the caster; the Edict says *"a unit you don't control"*. In a
Duel (485.1) that is one opponent choosing one of their own units, and the trailing *"that hasn't been
chosen for this spell"* is dead letter with two players.

The price is printed and the entry leads with it: **six Energy and two Order Power buys one kill aimed by
the player losing the unit.** It is never efficient removal and always the answer of last resort. And the
one reading-dependent claim — that a body which *"can't be chosen by enemy spells and abilities"* may still
be chosen by its OWN controller under an enemy spell's instruction — is stated **with its alternative**
rather than asserted; it is the position the catalogue already holds for 355.10.d and 355.10.e, and the
[Deflect] half needs no reading at all, because 809.1.c charges only *"for each time they choose"*.

**355.10.f is the neighbour and is not the same clause**: *"It is identified in an instruction that a
player “must” complete."* The Edict is the `.e` shape (a set chosen by other players); `UNL-170 Atakhan`'s
*"the defender must kill one of their units here"* is the `.f` shape. The catalogue has used the two
interchangeably.

The Sandshifter is the targeted half, and **359.3.e.4's second example is its printed weakness** — a +1
Might in response makes a *"3 Might or less"* kill mistarget — while **355.5.b** is why it picks late.

## 52. A CORRECTION TO CLAUDE.md: 355.10.e's example is a PARAPHRASE, not the card's wording

CLAUDE.md states: *"355.10.e is a set 'chosen in whole or in part by other players' and its printed example
is `OGN-209 Cull the Weak`'s exact wording (and the example spells out that it does not target)."*

Measured 2026-09-12:

- **355.10.e's example**: *"Each player kills a unit they control"*.
- **`OGN-209 Cull the Weak` prints**: *"Each player kills one of their units."*

Same effect, different words — so *"exact wording"* is wrong, and the rules do quote the card exactly
**somewhere else**: **411.1**'s example reads *"Cull the Weak is a spell that reads “Each player kills one
of their units.”"* Both strings verified against their own files. The fix is one word in CLAUDE.md
(`exact wording` → `the same effect in different words; 411.1 is where the rules quote the card`), and the
lesson is the one this project keeps paying: **a quote that reads right is the one to re-check**, and the
check that caught this compared the rules text to `corpus_flat.txt` rather than to memory.

## 53. Method — I built the forbidden probe a SECOND time in one session

§48 recorded that the regex-over-the-whole-JSON quote probe produced 127 false failures. Validating this
batch I reached for it again and it produced **17 of 25 spans "not verbatim"**, every one an apostrophe
artifact (*"t be chosen"*, *"s set is chosen by…"*), a passage of **card text** that belongs in
`corpus_flat.txt`, or a sentence quoted from **CLAUDE.md**, which is a secondary source by this project's
own rule. Replacing it with an **explicit list of the eight rules passages and the seven card passages this
entry claims**, each checked against the correct file, returned **15 of 15 correct except one** — and that
one was the real defect in §52. The explicit list is not merely safer; **it is the only version that finds
anything**, because a probe with a 68% false-positive rate is one nobody reads.

---

# Batch 11 — 359.2, the paragraph that says a permanent is on the board before its own text runs

One entry, staged at catalogue **758**. It closes a third of the eleven walkable uncatalogued cards
counted in §50 (two in batch 10, one here; **eight left**: `OGN-071`, `OGN-171`, `OGN-175`, `SFD-096`,
`SFD-098`, `UNL-092`, `VEN-048`, `VEN-131`).

| id | sub-rules opened | class | cards |
|---|---|---|---|
| `field-musicians-fiora-worthy-self-pump-arrival` | **359.2, 359.2.a, 359.2.b** | ENGINE | VEN-026 + SFD-180 |

## 54. 359.2 is the mechanism behind a consequence this lane has now used three times

> **359.2.** A Permanent leaves the Chain and becomes a Game Object.
> **359.2.a.** Any passive abilities become active.
> **359.2.b.** Execute all rules text on the card, from top to bottom.

All three were cited by nothing. **The body is on the board before its own rules text runs**, which is the
mechanism under the consequence 355.5.b states from the other side (*"The target will be chosen when the
ability triggers."*). Batch 5 used it for the Grim Apothecary returning himself, batch 6 for the Windsinger
being excluded only by the word *"another"*, and batch 10 for the Sandshifter picking late. **359.2.b is
the better first citation for all three**, because it is the step that puts the card on the board, and
355.5.b is the step that defers the choice to it.

## 55. Riot's worked example at 710 is this card's effect with this card's numbers

`VEN-026 Field Musicians` reads *"When you play me, give a unit +3 Might this turn."* 710's example:

> A unit with a base Might of 3 is targeted by a spell that reads "A unit gets +3 [M] this turn." As that
> spell resolves, its Might changes from 3 to 6, and it becomes Mighty. When that effect expires at the end
> of the turn, it will no longer be Mighty.

The Musicians are printed at Might **3**, so under 359.2 they are a legal choice for their own trigger and
the crossing 709 wants is on the card that just arrived. `SFD-180 Fiora, Worthy` then pays: *"When a unit
you control becomes [Mighty], you may pay 1 Order Power to ready it."*

**And the payoff is ARRIVAL, not Might.** 143.4 enters units exhausted and 144.2 with 420.3.a put the
exhaust on the Standard Move, so a body played this turn cannot normally walk to a battlefield until your
next Awakening (415.3.a). Fiora's is a **real** ready — 415.1 marks a Game Object already on the board,
which is exactly what an *"enters ready"* grant is not (805.6, 805.6.a, and §47's Sun Disc generalisation)
— so for one Order Power the Musicians reach a battlefield the turn they are played. **The Might expiring
at end of turn costs nothing, because it was only ever transport**; 315.2.b.2 Holds the battlefield at your
next Beginning Phase with a plain 3 Might body standing on it.

**What it adds to Fiora's thirteen other entries**, stated in the entry rather than left for a reviewer: it
is the only one where a single card supplies the crossing AND is the body that walks, so nothing else has
to be arranged first, and the only one where the pump expiring is not a loss. Fiora is the most
partner-rich trigger card in the pool, which this project records as **a warning flag rather than a
prize** — the mechanism is catalogued many times over and what earns a row is a partner that changes the
shape.

**The honest cost is a blind commitment**, and batch 8's finding pays for itself here: 383.3.e.2.a puts the
decision on Fiora's *"you may pay"* at FINALIZATION, 383.3.e.2.b removes the trigger if you decline, and
204.3.a makes the Power due there — with 312.2.c handing out no priority until every pending item has
finished finalizing. 444.2.c is the relief, since 164.2 gives every Basic Rune two [Reaction] abilities.

## 56. Validation and the quote method that is now standard for this lane

`validateCombos` **0 errors over 758**; no duplicate id; no duplicate card set including `needs`;
subset/superset sweep against every live entry clean both ways; `generateVariants` **1,630**; legend checks
**4 base codes, 0 defects**, with all eight Calm/Order codes generated from `data/cards.json`;
**22 distinct rule references, all resolve.**

Quotes were checked from an **explicit list split by source file** — eight rules passages against
`data/Riftbound-Core-Rules-2026-07-16.txt` and two card passages against `data/corpus_flat.txt`, **10 of 10
verbatim**. That split is the fix for §53: the regex probe failed twice this session precisely because it
sent card text to the rules file and prose apostrophes to both.

---

# Batch 12 — a board-independent XP faucet, and dispositions for the leftovers

One entry, staged at catalogue **759**. It closes a fourth walkable uncatalogued card; **seven remain**.

| id | class | cards |
|---|---|---|
| `demacian-diplomat-blood-rose-board-independent-xp` | ENGINE | UNL-092 + UNL-109 |

## 57. Every other XP source in Body is gated on the board

Swept 2026-09-12 over `data/cards.json` for *"gain N XP"*: **32 cards** gain XP. Within mono-Body,
`UNL-094 Gemhand Hunter`, `UNL-096 Hunter's Machete`, `UNL-100 Voracious Gromp`, `UNL-102 Crowd Favorite`,
`UNL-113 Master Yi, Tempered`, `UNL-117 Arachnoid Horror` and `UNL-119 Kha'Zix` all carry **[Hunt]**, which
823.1.c.1 pays only on a Conquer or a Hold; `UNL-095 Grim Resolve` needs a combat win; `UNL-107 Stare Down`
needs an enemy garrison for its main clause; `UNL-115 Nilah` needs a move; `UNL-116 Poppy, Paragon` needs
the opponent within 3 of the Victory Score. **`UNL-092 Demacian Diplomat` and `UNL-109 Blood Rose` ask
nothing of the board at all** — XP from cards leaving your hand.

That is the same shape as the project's own #200 finding that 29 of 71 finishers are Hold-gated, so *"the
stall that makes the line necessary is the stall that switches it off"*. A [Level] deck walled off the
board still levels on this pair, and on nothing else in its domain.

The entry applies this lane's batch-8 finding rather than restating it: Blood Rose's *"you may pay 1 Energy
to gain 1 XP"* is a cost at the start of a Triggered Ability's effect, so 204.3.a and 383.3.b.1 make it
payable **to finalize** and **383.3.e.2.a puts the DECISION there too**, with 383.3.e.2.b removing the
trigger if you decline — a blind commitment that is cheap here and expensive on a bigger trigger. 444.2.c
is the relief. And its own second sentence is the anti-synergy: *"Spend 3 XP, exhaust: Ready a unit"* is a
SINK, and 730.2 with 824.1.d switch off every [Level] rung the faucet was filling.

## 58. Dispositions for the seven leftovers, so the next lane starts from a clean slate

| card | disposition | scope |
|---|---|---|
| `OGN-071 Party Favors` (Calm E3) | **REFUSED.** *"Each other player chooses Cards or Runes. For each player that chooses Cards, you and that player each draw 1…"* — 355.10.e makes it untargeted, and in a Duel (485.1) it is one opponent choosing between two **symmetric** modes. They pick the one worth more to them; you get the same thing either way. A card whose reward the opponent selects, where both rewards are symmetric, is never a combo piece | **today's pool.** The rune mode is still worth knowing: a rune channelled exhausted is 1 Power immediately (164.2.b, whose cost is the recycle) and 1 Energy from your next Awakening (415.3.a) |
| `OGN-171 Mystic Poro` (Chaos E2 M2, [Vision]) | **LEAD, not staged.** It is the mono-Chaos feeder for the `OGN-194 Nocturne` replacement-effect line this lane merged as `gemcraft-seer-nocturne-vision-replacement-deploy`. The delta is real but narrow — one look per copy against a static that makes every unit a looker, bought by dropping a second domain — so it is a **manager decision** whether a mono-Chaos twin earns a row, not something to stage unilaterally | **the manager's call** |
| `OGN-175 Shipyard Skulker` (Chaos E3 M3, no text) | **REFUSED.** It is the Chaos member of the pool's five vanilla units — measured: `OGN-049 Playful Phantom` (calm), `OGN-088 Mega-Mech` (mind), `OGN-142 Mountain Drake` (body), `OGN-175` (chaos), `OGN-219 Vanguard Sergeant` (order), and **no Fury member**. CLAUDE.md's argument for the Phantom is that a blank body is perfect fuel for an exhaust-a-friendly cost; Chaos has no such cost. Swept: the only Chaos card that eats a friendly body is `UNL-142 Heedless Resurrection`, whose ceiling **reads the killed unit's cost**, so cheap vanilla fuel is actively WRONG there | **today's pool.** It becomes fuel the moment Chaos prints a cost that names a unit without reading its cost |
| `SFD-096 Laurent Bladekeeper` (Body E3 M3) | **SYNERGY LEAD.** Measured over `data/cards.json`: 36 units print [Ganking], eleven of them in Body, and Laurent is the **cheapest and the only one with no Power cost and no condition** — `UNL-115 Nilah` is E3 **P1**, `VEN-070 Brutal Hunter` is E3 P1 and only Ganks while Empowered, and the rest start at E4. A deckbuilding fact rather than a line | **new lines**; anchor for a rule wanting a Body battlefield-to-battlefield mover |
| `SFD-098 Sea Monkey` (Body E2 M2) | **REFUSED with a rules reason worth keeping.** His optional additional cost buffs HIMSELF, and 355.10.d with 355.10.d.1 — *"This exception applies solely to objects for which no choice is ever possible"* — means *"buff me"* chooses nothing. So he does **not** fire a *"when you choose a friendly unit"* payoff (`SFD-144 Spirit Wheel`, `SFD-195 Blade Dancer`), even though buffing is otherwise choosing under 702.2.a. That forecloses the first line anyone would try with him | **all self-targeting buffs**, not only this card |
| `UNL-092 Demacian Diplomat` | **CLOSED** by the entry above | — |
| `VEN-048 Cloud Drake` (Mind E6 M5) | **REFUSED as a worse member of a catalogued engine.** Measured: 19 Dragons in the pool and **exactly one in Mind**, this one. The Dragon discount package is `OGN-140 Herald of Scales` (Body) and `UNL-104 Gentle Gemdragon` (Body), already catalogued as `herald-of-scales-gemdragon-drakehound-net-positive` with `SFD-006 Eager Drakehound` (Fury, E3, the cheapest Dragon and the Energy-positive case). Putting the Cloud Drake in that package forces Mind + Body, which is **one legend name** — `VEN-149 / VEN-194 Defender of Tomorrow` — for a strictly more expensive Dragon | **today's pool** |
| `VEN-131 Decree of Unity` (Order E2 P1) | **LEAD.** *"Kill an enemy Chaos unit or gear."* It is one of the sixteen gear-removal printings the project measured for #200, and the only one gated on a DOMAIN rather than on a cost or a Might. Twelve BURST and CHAIN entries stand on attached Equipment; this answers the Chaos ones for 2 Energy and 1 Order Power | **new lines** |

## 59. HANDOFF — rc-walk-mid, batches 5–12, written for a successor who will not have my context

### H14. Lane, issue, staging

- **Issue #187**, sub-rules **300–499 and nothing else**. rc-walk-blocks holds 100–299, rc-walk-fc 500–829.
  Commits use `Refs #187`; never a qualified `Closes`.
- **Staging file `/tmp/rc-walks/rc-walk-mid.json`.** `rc-manager5` merges within the hour, so **trim it by
  ID against `data/combos.json` before every validation**, never by memory of what you sent. As of this
  handoff it holds **one** unmerged entry, `demacian-diplomat-blood-rose-board-independent-xp`.
- **Never carry a citation-upgrade debt forward.** The manager applies each one the same turn, in commit
  messages this lane does not see. Flag once, then re-read `data/combos.json`.

### H15. What batches 5–12 produced

Six entries staged (five merged at the time of writing), and **21 sub-rules opened as entry citations**:
355.5.b, 359.2, 359.2.a, 359.2.b, 359.3.e.4, 359.3.e.12, 359.3.e.12.a, 370.1.b, 370.1.b.1, 370.2, 371.2,
312.1, 312.1.a, 312.1.b, 312.1.b.1, 316.5.a, 410.1, 410.1.a, 410.1.b, 410.2.b, 419.1/419.1.a/419.2/419.2.a.
Plus the findings-only rows of §38–§40 and §54: 429.2.b, 383.3.e.2/.2.a/.2.b, 390.5 and its five sub-rules,
391, 392, 358.1, 358.2, 416.2/416.2.a, 422.2/422.2.a, 443.3/443.3.a, 483.1–483.8, 437.6.a.

**The two structural finds worth re-reading before anything else** are §22 (the whole of 312.1, 410.1 and
419.1–419.2 is uncited while 312.2.a is cited 81 times, and 312.1.b.1 is the general licence behind every
"play it, ignoring its cost" effect) and §38 (of 233 spells, ZERO add resources, so 429.2's instant
resolution is universal in this pool).

### H16. Refusals made in batches 5–12, each with its scope

355.10.b (no card prints the word *"target"* — measured three ways); 323.9.a and 420.3.b (read, thin alone,
citation upgrades only); 356.3 (a limit, not a line); `UNL-219 Vaults of Helia` (the two existing refusals
checked and standing); 478/479, 416.5.a, 417.1.b/c/d, 358.4 (re-checked and confirmed); the 443 refusal
**refined** — zero cards trigger on the Draw Phase and exactly one triggers on drawing; and the seven
leftovers in §58.

### H17. The exact next rows

1. **The probe**, rebuilt and gitignored at `.scratch/mid-probe.mjs`, **echoes its parsed range on its
   first output line** — run it as `node .scratch/mid-probe.mjs 300 499` with TWO separate arguments. At
   catalogue 757 it reported **124 uncited headings with an Example in range, 41 naming a pool card**.
2. Unopened and ranked: **373.2.a.1**, **394.1** (`SFD-090 The Zero Drive`), **417.6.b.2** (`OGN-017 Iron
   Ballista`), **475.1** (`SFD-180 Fiora, Victorious`), **358.1 with 358.2** (the play-time legality checks
   whose failure mode is 358.5's undo, a different outcome from 359.3.e.5's mistarget).
3. The **489** (2v2) rows — 489.5.a, 489.6, 489.7, 489.8.f, 489.8.g, 489.8.g.1. Expect leads: `matchDeck`
   reads one list.
4. The **seven leftover cards** of §58, of which two are marked LEAD and one is a manager decision.

### H18. Method that paid, and the two probes that did not

- **Quotes are checked from an EXPLICIT list SPLIT BY SOURCE FILE** — rules passages against the rules,
  card passages against `data/corpus_flat.txt`. The regex-over-the-whole-JSON probe failed **twice** in one
  session (127 false failures, then 17 of 25) because it sends card text to the rules file and prose
  apostrophes to both. The explicit list found the only real defect of the session (§52).
- **Rule references**: reuse the tokenizer and `^\s*` anchor from the committed `test/rule-refs.test.ts`.
  975 mentions across this lane's fifteen entries resolved with zero failures.
- **Validate against a MERGED COPY** and never write `data/combos.json`; use `loadCombos()` and
  `loadCardIndex()` from `src/load.ts` rather than re-reading the JSON, and note `generateVariants(combos,
  cards)` takes the CardIndex as its second argument.
- **Scratch probes belong in `.scratch/`** (gitignored), not `/tmp`, because a `/tmp` script cannot import
  `../src`.
- **A census must be run, never typed.** Every legend list, domain and count in batches 5–12 came out of
  `data/cards.json` or `data/corpus_flat.txt` in the same turn it was written.

### H19. The single most valuable sentence this lane produced, and a successor will not infer it

**359.2.b is the BOARD-STATE paragraph and 355.5.b is the DEFERRAL paragraph, and a claim about what an
ETB can see needs both.** Batches 5, 6 and 10 each cited 355.5.b alone for "the body is already on the
board when its own trigger chooses" — which is true, and incomplete: 355.5.b says only that the choice is
deferred to the trigger, and it is **359.2 / 359.2.b** (*"A Permanent leaves the Chain and becomes a Game
Object" / "Execute all rules text on the card, from top to bottom"*) that puts the card on the board first.
Batch 11 found it only because the author re-read three of their own entries in a row and noticed the same
half-citation in all three.

**That is the shape of a whole class of defects here: a citation that is TRUE and INCOMPLETE.** No
validator can see it — every reference resolves, every quote is verbatim, the conclusion is right — and
nothing catches it except the author re-reading their own work looking for the paragraph they did *not*
cite. Budget a pass for it at the end of every third or fourth batch, not at the end of the lane.

---

# Batch 13 — rule 489 read end to end: the 2v2 model this project carries is wrong in three places

No entry, and there cannot be one: `matchDeck` reads ONE list and 489.2 puts two independent decks on a
team, so a 2v2 line has no representation in `data/combos.json` today. What follows is a correction to the
project's stated 2v2 model, measured against the rules text. **489.3 (*"Victory Score: 11"*) is cited 42
times; 489.4, 489.4.a, 489.5 and all its sub-rules, 489.6, 489.6.a, 489.7, 489.8.c.1, 489.8.c.2, 489.8.d
and its sub-rules, 489.8.e, 489.8.f, 489.8.g and its sub-rules, 489.8.h and 489.8.i are cited ZERO times.**

## 60. CORRECTION ONE — the 2v2 board has THREE battlefields, so the rate rises with the target

> **489.4.** Battlefield Count: 3
> **489.4.a.** Each player provides three (3) Battlefields, included in their deck during deck building.

CLAUDE.md records that *"2v2 is section 489, its Victory Score is 11 … so every BURST and CHAIN in the
catalogue is authored for Constructed and is three points short in 2v2."* **That is half the picture.**
470 caps Scoring at once per Battlefield per turn and 315.2.b.2 Holds every battlefield you control, so a
three-battlefield board pays **three points a turn where a Duel pays two**. The target is 37% higher (11
against 8) and the Hold rate is **50% higher** — so an unopposed Hold curve is not slower in 2v2, it is
slightly faster. What is genuinely three points short is a **BURST**, which fires once and cannot borrow
the extra battlefield.

It is the same shape as the project's Baron Pit finding — a per-turn Score ceiling of three rather than two
— except that here it is printed in the format's own setup rather than manufactured by a card.

## 61. CORRECTION TWO — in 2v2 the player going FIRST never uses a battlefield at all

> **489.5.a.** Each player who is not going first randomly selects one (1) of their three (3) Battlefields.
> The other two are removed and will not be used for this game. The selected Battlefields are placed
> simultaneously between the players before play and will be used for this game.
> **489.5.b.** The player who is taking the first turn removes their Battlefields. They will not be used.

So the three battlefields on a 2v2 board come from the **three players who are not going first**, one each
at random. Every battlefield-dependent entry in this catalogue carries a 2v2 caveat of the form *"485.4.a,
485.5, 486.5 and 103.4.c as for every battlefield entry"* — and in 2v2 the honest statement is stronger:
**if you go first, your battlefield is removed before play and the entry is dead for that game**, and if you
do not, it is one in three. **489.8.i** adds a deckbuilding constraint the Duel does not have:
*"Teammates may not utilize the same Battlefields."* **489.8.h** is its twin: *"Teammates may not utilize
the same Champion Legend."*

## 62. CORRECTION THREE — "friendly" already reaches your teammate, so no card has to name an ally

> **489.8.e.** Friendly describes controlled Game Objects by a player or their Teammate.
> Example: "When you play me, ready a friendly unit" could target a player's own Units or their teammate's
> Units.

CLAUDE.md states: *"Only 4 cards in the pool name an ally ("teammate"/"partner" appear on none); the 'or an
ally holds' half of SFD-201 and UNL-193 is uncatalogued."* The count is right and **the conclusion drawn
from it is wrong**: a card does not have to name an ally to reach one. **Every "friendly unit" in the pool
— every buff, every ready, every would-die shield, every "friendly unit at a battlefield" — reaches the
teammate's board in 2v2 by 489.8.e**, which roughly doubles the legal object set of a large fraction of the
catalogue. The four cards that name an ally are the exceptions that reach something *"friendly"* does not
(a teammate's HOLD, which is an event and not a Game Object).

The limits are printed beside it and are exactly what you would want to know: **489.8.c** *"Control is not
shared"*, with **489.8.c.1** (you may not Hide at a battlefield your teammate controls) and **489.8.c.2**
(you may not issue Standard Movement to their Units). So in 2v2 you may buff, ready and save your
teammate's bodies, and you may not move them or hide behind their board.

## 63. Four more rows worth carrying

- **489.8.d.3** — *"If a spell or ability references or checks a player gaining or losing points, that
  ability checks whether that player's team gains or loses points."* So `OGN-028 Draven, Showboat` reads the
  TEAM total in 2v2, and the five *"within 3 points of the Victory Score"* cards read 8 of 11 rather than
  5 of 8. The project records the threshold moving with the format; this is the paragraph that says whose
  score it is.
- **489.8.g.1** — the Final Point from a Conquer must Score **each** Battlefield in the same turn,
  *"excepting any that were occupied by their ally during the scoring step of that turn's Beginning Phase."*
  With three battlefields that is a strictly harder version of 471.1.b.1, and the ally exception is the
  only relief.
- **489.8.b** — battlefields your teammate controlled during your Beginning Phase are **disqualified** from
  being scored by your team that turn, with Riot's own example naming the trick it exists to stop: *"A
  player may not induce their partner to retreat, and then conquer a Battlefield their teammate was
  controlling."*
- **489.7** — the player going first does not draw in their first Draw Phase and the player going last
  channels an extra rune. The turn-clock model this project built for Constructed (`npm run adversarial --
  --turns`) is priced on the Duel's first-turn rule and would need this row to speak about 2v2 at all.

## 64. Why this is a findings batch and what would make it an entry

`matchDeck` reads one decklist, 489.2 puts two independent decks on a team and 103.1.b applies per deck, so
**a team line cannot be expressed in `data/combos.json` as it stands.** The prerequisite for ever mining
this block is a representation decision — a second `uses` set, or a `teamUses`, or a separate file — which
belongs to the manager and not to a walk. Until then 489 yields **caveats that belong in existing entries**,
which is what §60–§63 are.

---

# Batch 14 — the Mystic Poro decision, taken by the manager and priced before it was written

One entry, staged at catalogue **760**, at `rc-manager5`'s direction after §58 raised it as a manager
decision rather than staging it unilaterally. **It closes the fifth walkable uncatalogued card; six remain**
(`OGN-071`, `OGN-175`, `SFD-096`, `SFD-098`, `VEN-048`, `VEN-131` — four of them refused in §58 and two
kept as synergy leads).

| id | class | cards |
|---|---|---|
| `mystic-poro-nocturne-mono-chaos-look-package` | ENGINE | OGN-171 + OGN-194 |

## 65. The arithmetic came first, because the instruction was "price it or refuse it"

Three `OGN-171 Mystic Poro` are **three one-card looks in a game** — [Vision] is on the Poro alone, where
`OGN-100 Gemcraft Seer` prints *"Other friendly units have [Vision]"*, a static. 103.2 counts the Chosen
Champion inside the 40 and 103.2.a.1 sets it aside before play, so the Main Deck is **39** and holds three
Nocturnes. Modelled as three uniform one-card looks with the misses recycled to the bottom (416.1):

> 1 − (36/39)(35/38)(34/37) = **0.219**

The same model over the eight or so units a Gemcraft Seer's static looks through in a game gives **0.508**.
**So this package converts a Nocturne roughly HALF as often as `gemcraft-seer-nocturne-vision-replacement-deploy`.**
That is stated in the entry's own NAME, in the `ferrous-forerunner` shape the project asks for:
*"THREE LOOKS AGAINST A STATIC, AND THAT IS THE ENTRY'S REAL CONTENT"*.

**It is still a row, and the reason is identity rather than throughput.** The Seer is mono-Mind, so that
entry forces one of exactly four Mind/Chaos legend names; this package is mono-Chaos end to end and rides
in **any** Chaos pair, asking nothing of the rest of the deck. It is the forced-shell argument inverted —
elsewhere a Colourless card is the only route *into* an identity, and here dropping a domain is the point.
And the 22% is payable because the Poros are not dead cards: a 2 Energy 2 Might body with a free scry is a
card a Chaos deck runs for the body, and 431.1.c with 431.1.c.1 keep the look and the recycle free of Burn
Out even with the deck empty. The Nocturnes are the copies that cost something.

**THE BACK-LINK IS OWED AND WAS CHECKED RATHER THAN ASSUMED.** This entry names
`gemcraft-seer-nocturne-vision-replacement-deploy`; that entry does **not** name this one — grepped over
`data/combos.json` on 2026-09-12, `OGN-171` appears in it zero times. A cross-reference that exists in one
direction only is the defect two lanes shipped this week, and it is one grep to check. **Flagged once**,
per §43.

---

# Batch 15 — the zone paragraph eight entries stand on and none cites, and the Limited/Discretionary census closed

No entry. The card vein in 300–499 is spent (every card the probe's remaining rows name is catalogued, checked
one by one below), so this batch is **one whole-family H19 defect, one first-citation that is already live in a
shipped entry, one structural census, and four refusals with their scope.** Staging file trimmed to **zero**
before validation: `mystic-poro-nocturne-mono-chaos-look-package` merged at catalogue **761**, and the back-link
flagged in §65 was applied by the manager (`gemcraft-seer-nocturne-vision-replacement-deploy` now names it —
grepped, not assumed). **Citation-upgrade debt carried into this batch: ZERO**, per §43.

## 66. THE DEFECT — eight entries let a passive ability work from inside the Main Deck and none cites the paragraph that allows it

`SFD-175 Undertitan` (*"As I'm revealed from your deck, [Add] :rb_energy_2:."*) and `OGN-194 Nocturne,
Horrifying` (*"As you look at or reveal me from the top of your deck, you may banish me."*) are the pool's two
cards whose text does work while the card is **in the Main Deck**. Measured over `data/combos.json` at catalogue
761 and re-measured at **762** immediately before reporting, **eight entries** stand on that: `reksai-undertitan-reveal`, `nocturne-stacked-deck-cheat`,
`void-rush-undertitan-accelerate`, `diana-lunari-showdown-scry`, `teemo-strategist-undertitan-mind-order-reveal`,
`undertitan-pakaa-signpost-move-reveal`, `gemcraft-seer-nocturne-vision-replacement-deploy`,
`mystic-poro-nocturne-mono-chaos-look-package`.

All eight cite **370.1.b.1** — the paragraph that makes an *"as"* clause a Replacement Effect rather than a
trigger, and whose worked example is Undertitan by name. That citation is **true and incomplete**, which is
exactly the class §H19 predicted. It answers *what kind of ability this is* and says nothing about *why an
ability on a card nobody has played is doing anything at all.* Four paragraphs answer that, and **two of them
are cited by nothing in this project**:

> **365.1.** Passive Abilities of Permanents are typically only active while on the Board.

That is the DEFAULT, and on its own it kills the whole family. Cited seven times in the catalogue, never here.

> **366.1.** Passive Abilities of cards in zones that are outside of the Board will self-describe their context.
> Example: The passive ability "Play me only during an opponent's turn." applies in any zone from which that
> card can be played.
> Example: Undying Legion has a passive ability that reads “[Legion][>] You may play me from your trash for
> [3][C].” That passive ability only applies when Undying Legion is in the trash.

That is the OVERRIDE, and *"from your deck"* / *"from the top of your deck"* is the self-description. **It is
cited by exactly TWO entries out of 761** (`void-drone-sarcophagus-zone-discount`,
`immortal-phoenix-falling-star-cleanup-attribution`), neither of them in this family — and the card it names in
its own worked example, `UNL-025 Undying Legion`, is catalogued in `battering-ram-undying-legion-cards-played`,
which cites neither 366.1 nor 812.2.

> **368.1.** Passive Abilities can be Replacement Effects.

**Zero citations.** This is the hinge, and it is the paragraph a reader most needs: it is what makes 366.1 rather
than **385.1 / 385.2** the applicable rule. This project has 385.1 and 385.2 — *Triggered* Abilities outside the
Board — at 16 and 42 citations and has never had their passive counterpart, so the natural move for the next
reader is to reach for 385 and get the wrong block. 368.1 closes that in one sentence.

> **424.1.a.2.** Cards remain in the zone they are being Revealed from.
> Example: If a card is being Revealed from the top of a player's Main Deck, it is still the top card of that
> player's Main Deck.

That is why the self-described context is still satisfied **at the instant the passive fires**: revealing is not
a zone change (424.1.a: *"Revealed is a temporary state and is not a zone"*), so the Undertitan is on top of the
deck while it Adds. Cited six times overall and in **two** of the eight (`diana-lunari-showdown-scry`,
`teemo-strategist-undertitan-mind-order-reveal`).

**NO VERDICT IS WRONG, AND I CHECKED RATHER THAN ASSUMED.** The two cards demand opposite enablers — Undertitan
pays on a REVEAL only, Nocturne on a LOOK as well — and 424.1 (*"Revealing is the act of presenting a card to
all players"*) against 436.1 / 817.1.b (Predict and Vision are defined with *"look at"*) is the split. Every
Undertitan enabler in the catalogue genuinely reveals: `SFD-170 Rek'Sai, Swarm Queen`, `SFD-188 Void Rush`,
`VEN-033 Pakaa Protector`, `OGN-121 Teemo, Strategist`, `UNL-079 Diana, Lunari`. Every Nocturne enabler looks:
`OGN-100 Gemcraft Seer` and `OGN-171 Mystic Poro` ([Vision]) and `OGN-183 Stacked Deck` (*"Look at the top 3"*).
**The authors got the distinction right without the paragraph** — which is precisely why no validator could ever
have surfaced this.

**UPGRADE, FLAGGED ONCE (§43):** add 368.1 + 366.1 against 365.1 to all eight, and 424.1.a.2 to the six that
lack it.

## 67. 373.2.a.1 IS NOT DEAD LETTER, AND ITS ONE CASE IS ALREADY SHIPPED

I was ready to refuse 373.2.a.1 as having no pool case — a Replacement Effect replacing an event that is part of
*another* Replacement Effect sounds like a rules-engine paragraph. Swept `data/corpus_flat.txt` for the
would-die / would-be-dealt family (nine cards: `OGN-023`, `OGN-077`, `OGN-269`, `OGS-020`, `SFD-051`, `SFD-173`,
`UNL-007`, `UNL-175`, `UNL-206`) and none of them nests. **The case is on the reveal side and it is inside an
entry this lane's own family audit had just opened.**

> **SFD-018 Void Hatchling** | Unit | Fury | E2 M2 | *"If you would reveal cards from a deck, look at the top
> card first. You may recycle it. Then reveal those cards."*

That is a Replacement Effect on a reveal, and *"then reveal those cards"* is a Game Action inside its own
instruction. When an Undertitan is among them, Undertitan's replacement applies **to a Game Action that is part
of another Replacement Effect** — the literal text of 373.2.a.1:

> **373.2.a.1.** A Replacement Effect that replaces an event or Game Action that is part of another Replacement
> Effect will not interrupt the sequence of the replaced Replacement Effect’s application.

And the companion that makes it terminate, whose own worked example is a nested pair of Zhonya's Hourglasses:

> **370.2.** A Replacement Effect can only be applied once to an event, **or to any Game Actions or events that
> replace that event.**

`void-rush-undertitan-accelerate` runs `SFD-018` and `SFD-175` **in the same `uses[]`** and cites neither. The
entry's arithmetic is unaffected; what it lacks is the paragraph that says its own two enablers compose rather
than pre-empt each other. Without 373.2.a.1 a reader can argue the honest opposite — that Void Hatchling
*replaced* the reveal event, so the Undertitan was never revealed and the [Add] never happens — and nothing in
the entry refutes it. **UPGRADE, FLAGGED ONCE: 373.2.a.1 + 370.2 to `void-rush-undertitan-accelerate`, which is
this project's first citation of 373.2.a.1.**

## 68. A UNIT CANNOT BE COUNTERED, BY CONSTRUCTION — and the eleven counters in the pool prove it

Three paragraphs, none of the last two cited:

> **359.2.** A Permanent leaves the Chain and becomes a Game Object.
> **359.3.a.** This card becomes a Finalized Chain Item.
> **359.3.c.** Other players have an opportunity to play Reactions before the resolution of spells.

The pre-resolution Reaction window is scoped **to spells by its own words**, and 359.2 is why: a permanent is
never a Finalized Chain Item at all. 329.2 makes a Chain Item Pending only until the Check Legality step (358),
312.2.c withholds priority until every pending item finishes finalizing, and 330 ends the Chain the moment the
last item leaves it — so **playing a unit with no triggered ability gives the opponent no priority whatsoever**:
the state goes Closed while the card is Pending, the body arrives at 359.2, the Chain empties, and 310.1's
Neutral Open State returns priority to the turn player under 312.2.a. This is the denial lens (#100) read from
the other side: that lens says the opponent's only windows are Closed States and Showdowns, and 359.2 is the
paragraph that says **a unit play creates neither.**

Measured over `data/cards.json`, folded by name+type: **ELEVEN counter printings, and all eleven carry
[Reaction]** — `OGN-045 Defy`, `OGN-064 Wind Wall`, `SFD-045 Not So Fast`, `SFD-136 Hard Bargain`,
`SFD-206 Riposte`, `UNL-044 Flurry of Feathers`, `UNL-106 Repulse`, `UNL-131 Abandon`,
`UNL-190 Lilting Lullaby`, `VEN-039 Crumbling Sands`, `VEN-152 Rebuttal`. **Nine say "a spell". Exactly two say
"spell or ability"** — `SFD-045` (*"Counter an enemy spell or ability that chooses a friendly unit or gear"*)
and `UNL-106` (*"Counter an enemy spell or ability that chooses it and no other friendly unit"*). **ZERO name a
unit, a permanent or a card.**

Two consequences the catalogue has never stated:

- **An ETB-removal BODY is strictly harder to answer than a removal SPELL, and the gap is nine cards to two.**
  A removal spell is answerable by all eleven; the body cannot be stopped from arriving at all, and only its
  triggered ability is exposed — to the two cards above, and only when the trigger *chooses* something. Twelve
  BURST and CHAIN entries stand on attached Equipment and the #200 lane priced their answers in gear removal;
  nobody has priced the counter axis, where the answer is one card wide.
- **`VEN-069 Mel, Newly Awakened`'s *"Your spells and abilities can't be countered"* covers everything that was
  ever exposed and nothing that was not** — a unit never needed the clause, which is why she words it that way
  and why `VEN-015 Decree of Rage`, the pool's only other *"can't be countered"*, is a spell.

Domain note, members named: six of the eleven are Calm (`OGN-045`, `OGN-064`, `SFD-045`, `UNL-044`, `VEN-039`
mono-Calm; `UNL-190` Calm/Mind), two Chaos (`SFD-136`, `UNL-131`), and one each Body (`UNL-106`), Body/Order
(`SFD-206`) and Mind/Chaos (`VEN-152`). **Counterplay is a Calm mechanic**, on the same axis as the stun lens
(#61) and the denial lens (#100).

## 69. THE LIMITED / DISCRETIONARY CENSUS IS CLOSED, AND THERE ARE EXACTLY THREE DISCRETIONARY ACTIONS

§35 opened the two halves of rule 410 and listed a handful of members. The census can be closed outright,
because rules **413–446 label every Game Action in the game** with one of two sentences. Swept the rules file
for *"is a Limited Action"* / *"is a Discretionary Action"*: **34 classification lines, and exactly THREE are
Discretionary.**

- **Discretionary (3):** **419.2** playing a card, **420.3** the Standard Move inherent to Units, **421.2**
  Hiding.
- **Limited (31):** 413.2 Draw, 414.3, 415.3 Ready, 416.2 Recycle, **417.3 Deal Damage**, 418.3 Heal, 420.2 Move,
  422.2 Discard, 423.2 Stun, 424.2 Reveal, **425.2 Counter**, 426.2 Buff, **427.4 Banish**, 428.3 Kill, 429.4 Add,
  430.4 Channel, 431.4 Burn Out, 432.2 Double, 433.2 Swap, 434.2 Attach, 435.2 Detach, 436.2 Predict,
  **437.6 Prevent**, 438.2 Replace, 439.6 Create, 440.3 Burn, 441.3 Empower, 442.2 Disempower, 443.3 Skip,
  444.3 Pay, 446 Move.

Read with the paragraphs §22 opened — **312.1** (*"Priority is the singular exclusive right to take
Discretionary Actions"*), **410.1.a** (a Discretionary Action may be performed at any time during a player's
turn in a Neutral Open State) and **312.1.b.1** (*"Players can always take and make choices for Limited Actions
when instructed, regardless of Priority"*) — the whole timing model of this game collapses to one sentence:

> **There are three things a player may ever do AT WILL, and every keyword that widens timing ([Action],
> [Reaction], [Ambush]) is written against one of them. Everything else — thirty-one actions, from Drawing to
> Killing to Paying to Revealing to Countering — happens whenever an effect instructs it, on anybody's turn,
> with no priority and no keyword.**

That is the general form of at least six things this project derived card by card: an instructed recycle or
discard on the opponent's turn (§35), an instructed Deal on the opponent's turn, 444.2.c's rune payment inside a
finalization, Hiding being confined to your own Main Phase by 421.2 and nothing in 811, the Standard Move's
Main-Phase-only confinement, and the fact that a *move by effect* (420.2, Limited) escapes every restriction the
*Standard Move* (420.3, Discretionary) carries. **Eighteen of the 34 classification lines are cited by nothing**
— 413.2, 416.2, 418.3, 422.2, 423.2, 425.2, 427.4, 428.3, 429.4, 433.2, 434.2, 435.2, 436.2, 438.2, 439.6,
440.3, 442.2, 443.3 — but the value is the classification, not the lines, and the negative half is worth
stating because it is printed identically four times: 416.2.a, 422.2.a, 427.4.a, 437.6.a and 443.3.a all read
*"only when directed to by Game Effects"* / *"when instructed to do so"*. **No player may ever recycle, discard,
banish, prevent damage or skip anything as a voluntary act.**

## 70. Refusals from this batch, each with its scope

| row | disposition | scope |
|---|---|---|
| **475.1** (*"Layers only serve to structure the application and order that Game Effects apply to Game Objects to maintain consistency"*) | **REFUSED.** Purely organisational; it makes no claim a line can stand on, and the layer paragraphs that do (476.1, 476.3, 477.3.b, 477.3.e.2.a) are cited 60, 10, and heavily. Its probe hit is an artifact of 476.3's Fiora, Victorious example sitting below it | permanent — this is a schema sentence |
| **394.1 + 396** (Linked Abilities) | **UPGRADE ONLY, flagged once.** `zero-drive-riptide-rex-banish-recursion` already carries 395 and 397 from the 2026-09-09 pass. 396 (*"Linked Abilities can contain component Abilities of any type"*) is why a granted **triggered** Deathknell and a printed **activated** ability form one set at all, and 394.1 is why *"units banished with this"* is a legal cross-reference. Neither changes the verdict | the two *"banished with this"* cards, `SFD-090` and `UNL-148` |
| **358.1 + 358.2 + 358.5** | **UPGRADE ONLY.** The Check Legality step is targets, costs, illegal state (358.3) and timing permission (358.4), and 358.5 is its failure mode — *"the actions taken in this process are undone and the action is cancelled"*, i.e. an illegal attempt costs nothing, which is a different outcome from 359.3.e.5's mistarget where the cost is already spent. 358.3 and 358.4 are cited 10 and 9 times and 358.3.a has its own entry (`here-to-help-warden-fizzle-still-played`). In a Duel 358.3's own example is unreachable: it is the three-player battlefield state, and 485.1 puts two players on the table | the Duel; live in 2v2 (489) |
| **`SFD-018 Void Hatchling` reading *"a deck"* rather than *"your deck"*** | **REFUSED as dead letter.** The clause is *"If **you** would reveal cards from a deck"*, and 053.1 makes *"you"* the controller, so the actor must be you. Swept: the pool's only card that reveals from another player's deck is `OGN-025 Blind Fury`, and it reads *"Each **opponent** reveals the top card of their Main Deck"* — the opponent is the actor, so the Hatchling never applies. The widening goes live the moment a set prints *"reveal the top card of an opponent's deck"* with you as the actor | today's pool |

## 71. Cards the probe's remaining rows name, all checked and all catalogued

This is why there is no entry. `OGN-017 Iron Ballista` (417.6.b.2, already cited 8×), `OGN-024 Void Seeker`,
`OGN-009 Hextech Ray`, `OGN-178 Undercover Agent`, `UNL-163 Mageseeker Investigator`
(`mageseeker-investigator-mass-move-tax`), `UNL-079 Diana, Lunari` (`diana-lunari-showdown-scry`),
`OGN-052 Stalwart Poro` (`taric-block-stalwart-poro-shield-stack`), `SFD-111 Here to Help` (two entries,
including one built on 358.3.a's own worked example), `SFD-090 The Zero Drive`, `UNL-025 Undying Legion`,
`OGN-037 Immortal Phoenix` (three entries), `OGN-183 Stacked Deck`, `SFD-018 Void Hatchling`,
`OGN-059 Eclipse Herald` (five). **Eleven of the eleven counter printings are catalogued too** — checked while
measuring §68. The 300–499 range still has rules to open; it has no cards left to open them on.

## 72. A correction owed to CLAUDE.md — [Action] reaches a Showdown, so the Rengar uniqueness does not rest on the keyword

CLAUDE.md records, from the #173 walk: *"exactly ONE survives the narrower question of arriving inside an
ALREADY-OPENED combat, and the reason is worth more than either number: 343.1.a bars an ordinary card from a
Showdown State, so that arrival needs [Reaction] and only Rengar, Pouncing has it."* The **count is right and
the stated reason is one paragraph short.**

> **806.1.c.1.** On Cards: "This can be played during showdowns on any player's turn."

So [Action] reaches a Showdown too, and `SFD-111 Here to Help` — [Action], E2 P1 — plays a unit from hand into
an open Showdown on the opponent's turn, which is exactly the shape the sentence says needs [Reaction]. The
reason Rengar is still alone on the **attacking** side is not the keyword but **355.2.a**: Here to Help says
*"to a battlefield you control"*, and 464.2.c.2 makes the battlefield you are attacking one you do not. The
finer split, which the catalogue already owns and which is what really separates the two cards, is **358.4**:
in a Showdown **Open** state the card being played is the one that closes it, so [Action] suffices; once the
state is **Closed** only [Reaction] gets in. So Here to Help deploys a blocker after attackers are declared and
can never be held as an answer to the removal spell already on the Chain; Rengar can. **Both entries stand;
the sentence in CLAUDE.md wants "355.2.a" where it says "[Reaction]".**

## 73. Validation and running totals

Quotes checked from an **explicit list split by source file**, per §H18: 23 rules passages against
`data/Riftbound-Core-Rules-2026-07-16.txt` (extracted with the `^[[:space:]]*` anchor, never a bare `^ *`, per
the form-feed trap) and 6 card passages against `data/corpus_flat.txt`. Every rule reference in this section
resolves. No entry staged, so no `generateVariants` run was owed; `data/combos.json` was not written.

**Lane totals after batch 15:** nine entries staged across batches 5–15, ~45 previously uncited sub-rules
opened, and this batch adds **368.1, 373.2.a.1, 359.3.a, 359.3.c, 417.3, 427.4, 427.4.a, 437.6.a, 425.2,
394.1 and 396** to the opened list, with 365.1 / 366.1 / 424.1.a.2 / 370.2 re-aimed at a family that had never
had them.

---

# Batch 16 — the Game Action blocks: what the buff cap does to the rest of the sentence, and the Create branch nothing in this pool reaches

No entry, and for the reason §71 gives. Three findings out of the 413–446 action blocks, two of them with
exhaustive member lists, plus two refusals with scope. Catalogue **762** at the start of the batch.

## 74. 426.1.c SPLITS EVERY BUFF PAYOFF IN TWO, AND RIOT'S OWN EXAMPLE IS `SFD-047 SIMIAN ANCESTOR`'S SENTENCE

**This catalogue cites 702.3 four hundred and eighty-eight times and 702.3.a a hundred and thirty-one** — the
one-buff-per-unit cap is the most-cited fact in the project. **426.1.c, the paragraph that says what the cap
does to the rest of the sentence the buff was written in, is cited ONCE** (`blade-dancer-caitlyn-choose-ready`).

> **426.1.c.** Units with Buff Counters can still be chosen for actions that Buff units, but will not be Buffed
> as part of the execution.
> Example: A spell reads "Buff a unit. Then, if it was buffed this way, draw a card." It will place a buff
> counter on a unit that has no buff counter, and then its controller will draw a card. However, if the player
> chooses a unit with a buff counter on it already, then the card will not be drawn.
> Example: A unit reads “When you buff me, ready me.” If that unit is chosen for a game effect that would buff
> it and it is already buffed, its ability will not trigger.

**The second worked example is `SFD-047 Simian Ancestor` verbatim** — *"When you buff me, ready me."* — and
`simian-ancestor-arena-bar-wallop-ready` does not cite it. That entry reaches the right verdict through
702.3.a (*"if a Buff is added … on a Unit that already has a Buff, it is not placed instead"*), which is the
CAP; 426.1.c is the CONSEQUENCE, and it is the paragraph a reader checking that entry actually wants.

**The split, swept over `data/cards.json` and folded by name+type, and both families are tiny and exhaustive:**

- **"When you BUFF" — TWO cards, and 426.1.c kills both on an already-buffed body.** `OGN-152 Mistfall`
  (Body gear, *"When you buff a friendly unit, you may pay :rb_rune_body: and exhaust this to ready it"*) and
  `SFD-047 Simian Ancestor` (Calm, M5).
- **"When you CHOOSE" — FOUR cards, and 426.1.c keeps every one of them ALIVE**, because the already-buffed
  unit *"can still be chosen"*. `SFD-057 Irelia, Fervent` (Calm, *"When you choose or ready me"*),
  `SFD-142 Jae Medarda` (Chaos, *"When you choose me with a spell"* — spells only, per the #169 walk),
  `SFD-144 Spirit Wheel` (Chaos gear) and `SFD-195 Blade Dancer` (Calm/Chaos legend), the last two reading
  *"When you choose a friendly unit"*.

So on a board where every body already carries a buff, **a mass buffer stops being a buffer and becomes a pure
"choose N friendly units" engine**: every buff does nothing, every Mistfall and Simian trigger goes dark, and
every Spirit Wheel and Blade Dancer trigger fires exactly as before. CLAUDE.md records the positive half
(*"buffing is CHOOSING (702.2.a), so every mass buffer fires Blade Dancer / Irelia Fervent"*); 426.1.c is what
makes that survive saturation, which is the only board state where the distinction is worth anything.

**Two riders on the same paragraph.** 809.1.c prices [Deflect] *"for each time they choose"*, so you pay the
tax for choosing an already-buffed enemy — or your own Irelia — and receive no buff for it; and **426.1.b.2 is
the rules' own slot for the Lee Sin exception** (*"Some effects may grant a Game Object permission to be Buffed
multiple times. Such an effect ignores this restriction"*), which is a cleaner source than the rule-002
argument CLAUDE.md currently uses for `OGN-078 Lee Sin, Ascetic`'s *"I can have any number of buffs"*.
`lee-sin-buff-bank` already cites it; CLAUDE.md's sentence does not.

**UPGRADE, FLAGGED ONCE: 426.1.c to `simian-ancestor-arena-bar-wallop-ready`.**

## 75. NOTHING IN THIS POOL IS CREATED, SO EVERY TOKEN IS PLAYED — and that closes an escape hatch

> **439.1.** Creating is the act of producing a Game Object that previously did not exist in the game.
> **439.2.c.** The zone a Game Object is created to can be specified implicitly by the Game Action being
> performed.
> Example: “Play a 1 [M] Recruit token” specifies a zone where the token is Created to implicitly, by
> specifying the token is played and thus created on the chain.
> Example: “Create a 1 [M] Recruit token” does not specify a zone, so the Recruit token will be created at any
> location on the board that it can be played to.

Two arrivals, two different rule sets: a **played** token is on the Chain, is bound by 355.2.a (the base or a
battlefield you control) and is a play for 350.2, so it fires *"when you play a unit"*; a **Created** token
skips the Chain, takes 439.2.b.1's destination and fires nothing.

Measured over `data/corpus_flat.txt` on 2026-09-12: **the word "create" appears ZERO times in the pool**, and
**76 lines print "play a … token"**. The single card that puts a Game Object on the board without playing it
is `UNL-147 Baron Nashor` — *"As you play me, add the Baron Pit battlefield token to the board if it's not
there already"* — which is a BATTLEFIELD, so 439.2.b.5 rather than 439.2.b.1, and 355.2.a was never about it.

**So: every unit and gear token in this pool is PLAYED, with no exception.** The catalogue uses the positive
half of that constantly (350.2 + 185.2.b for a token firing a play trigger) and has never stated that it is
universal, which is what makes it an `excludes` fact rather than a coincidence: **no token can dodge a
prohibition or a watcher that bites on PLAYING.** `OGN-070 Mageseeker Warden` — the pool's biggest denial card,
whose text is a prohibition on playing units anywhere but the base — has no token-shaped leak, and the twenty-six
[Accelerate] units CLAUDE.md names as its one leak remain its only one. 439.2.b.1 goes live the moment a set
prints the word.

## 76. 413.1.a IS WHY SIX CARDS MAY DRAW A CARD THAT IS NOT ON TOP

> **413.1.a.** Unless specified otherwise, drawing takes cards from the top of the Main Deck.

Uncited, and six catalogued cards need it. Swept over `data/cards.json` for a draw of something already named
(*"draw it / them / one of them / that card / any you…"*): **ten printings, and they split exactly in two.**

- **Four draw the top card anyway**, so 413.1.a is not load-bearing for them: `SFD-041 Apprentice Smith` and
  `VEN-033 Pakaa Protector` (*"reveal the top card … if it's a gear/unit, draw it"*), `UNL-079 Diana, Lunari`,
  and `SFD-188 Void Rush` (*"Draw any you didn't banish"* — the top two).
- **Six draw from a SPECIFIED position inside a looked-at set**, which only 413.1.a permits: `SFD-058 Ornn,
  Blacksmith` (top 4), `UNL-032 Double Trouble` (top 3, with [Repeat]), `UNL-051 Ivern, Nurturer` (top 3),
  `UNL-064 Fate Weaver` (top 4), `UNL-179 Rift Herald` (top 3) and `VEN-156 Lightning Rush` (top 3).

The price is printed on all six and is the same: **416.1 / 416.1.a send the rest to the BOTTOM** (or, for
Lightning Rush, to the trash), so a look-and-draw is a one-for-one that costs deck ORDER, where a
top-reveal-and-draw costs nothing. Read with 431.1.c and 431.1.c.1 — looking and revealing never Burn Out, and
neither does the zone change that follows — this is the family that is safe in an empty-deck shell, and
413.1.a is what says the card you take is the one you saw.

## 77. Refusals from this batch, with scope

| row | disposition | scope |
|---|---|---|
| **427.3 + 427.3.a** (cards may refer to cards banished by the same object; separate instances do not share a pool) | **REFUSED as dead letter for the shape the rule illustrates.** Riot's example is *"Banish the top card of your Main Deck. When you conquer this turn, draw it"* — swept, **zero** cards in the pool print *"banish the top"*. The pool's only two *"banished with this"* cards, `SFD-090 The Zero Drive` and `UNL-148 Cursed Sarcophagus`, are already governed by 395 and 397, which say the same thing about Linked Abilities and name the Zero Drive in their own examples | today's pool; 427.3.a goes live the moment a set prints a delayed banish-and-retrieve |
| **425.2 + 425.2.a + 425.3** (the Counter action block) | **UPGRADE-ONLY.** 425.3's format line — *"Counter [a card or ability on the chain]"* — is a second, independent confirmation of §68: a Counter can only name something **on the chain**, and 359.2 takes a permanent off it. Nothing here needs a new row; the finding is §68's and the paragraph is worth one line inside it | permanent |
| **439.2.b.1 through 439.2.b.5, 439.3, 439.4** | **REFUSED with §75's measurement.** They describe where a Created object goes, and this pool Creates exactly one thing, a battlefield token | today's pool |
| **413.2.a / 413.2.b / 413.3, 414.1.a, 426.2 / 426.2.a / 426.3** | **REFUSED as format and procedure lines.** They restate the Limited-Action classification §69 already closed, or give the printed formatting of an instruction | permanent |

## 78. Validation

Quotes checked from an explicit list split by source file: 12 rules passages against
`data/Riftbound-Core-Rules-2026-07-16.txt` and 8 card passages against `data/corpus_flat.txt`, zero failures.
All rule references resolve under the form-feed-safe `^[[:space:]]*` anchor. Every count in this batch was run
in the turn it was written — the two buff families over `data/cards.json` folded by name+type, the "create"
and "play a … token" counts over `data/corpus_flat.txt`, and the 488 / 131 / 1 citation counts over
`data/combos.json` plus `data/synergies.json`. No entry staged; `data/combos.json` not written.

---

# Batch 17 — the gear pool is disjoint on the exhaust, which is why 718.2 has one target and five readiers have none

No entry. One structural measurement with four consequences, one first citation of the paragraph that decides
whether an attach CHOOSES, and one sourcing correction. Catalogue **762**.

## 79. NO EQUIPMENT IN THIS POOL TAPS, AND NO TAPPING GEAR IS EQUIPMENT

Measured over `data/cards.json`, folded by name+type, 2026-09-12: **107 gear names. 39 are Equipment and
ZERO of the 39 carry an exhaust symbol anywhere in their text. Of the 68 non-Equipment gear, 40 do.** The two
halves of the gear pool are disjoint on the exhaust, and Riot appears to have designed them that way.

Four things follow, and the first is the largest.

**(1) 718.2 and 721.2 have exactly ONE Equipment target in this pool.** Those two paragraphs — *"While in this
state, the card's printed Rules Text is Inactive"* and *"Inactive Abilities do not trigger, do not apply, and
cannot be activated"* — are cited 110 and 66 times here. What they can actually silence on an attached
Equipment is only whatever printed Rules Text it has besides its own [Equip], and swept over the 39, exactly
one Equipment has any: **`SFD-090 The Zero Drive`**, whose *":rb_energy_3::rb_rune_mind:, Banish this: Play all
units banished with this, ignoring their costs. (Use only if unattached.)"* is the reason that reminder is
printed on that card and on no other. (`UNL-188 Hextech Gauntlets` looks like a second hit and is not — its
second sentence is `[Effect]` text, a Conquer trigger, which 718.3 appends to the carrier rather than
silencing.)

**(2) CLAUDE.md's rule is enforced by the pool rather than by the designer.** The file records, from the
removal of `prize-of-progress-hexplate-equip-shuttle`, that *"a repeatable activated ability of a gear must be
a gear that is NEVER attached."* True — and the stronger statement is that **there was never a shuttle to
build**, because no Equipment in this pool has a repeatable activated ability to shuttle. The removed entry
was reaching for a card shape Riot has not printed.

**(3) No Equipment in this pool is ever EXHAUSTED, so every gear-readier is aimed at the other half.**
359.2.d — *"If it is a Non-Unit Gear, it enters the Board Ready at the player's Base"* — and 143.4 exhausts
units only; swept, **zero cards exhaust a gear you control** (every `exhaust` on a gear line in
`corpus_flat.txt` is that gear's own cost symbol). With (1), an Equipment has no cost to spend and nothing
spends it for you. So, re-swept rather than typed: **three cards ready a GEAR by name** — `VEN-149 Defender of
Tomorrow` (*"Ready a gear"* / *"Ready 2 gear"*), `VEN-150 Acceleration Gate` and `SFD-221 Veiled Temple` — and
**two more can reach one as "something exhausted"**, `OGN-162 Miss Fortune, Captain` (*"you may ready
something else that's exhausted"*) and `VEN-068 Jayce, Brilliant Inventor` (*"ready something besides me
that's exhausted"*), both of which name the exhausted state in their own text and therefore have **no legal
Equipment target at all**. `SFD-221 Veiled Temple` is the card that proves the split from the other side: its
two clauses are *"you may ready a friendly gear. If it's an Equipment, you may detach it"* — a ready half that
can only ever matter for the 40 tapping gear, and a detach half that is the only half addressed to the 39.

**(4) 434.5's worked example is dead letter here** — *"An exhausted equipment being attached to a unit does
not ready the equipment"* — since no Equipment in this pool reaches an attach in the exhausted state. The
paragraph's general half (*"Attaching a card to another card does not modify either card's state except in
making those cards Attached and the Top-Most Card, and causing the Attached card to change locations"*) is
still the source for the fact that an attach is not a state reset.

**THE SYNERGY LAYER ALREADY IMPLEMENTS THIS, AND I CHECKED RATHER THAN ASSUMED.** Both gear-ready rules in
`data/synergies.json` — `defender-of-tomorrow-readies-gear` and `veiled-temple-readies-gear` — carry
`partner.textMatches: ":rb_exhaust::"` with `types: ["gear"]`, so neither can match an Equipment. No defect,
and the measurement supplies the REASON the predicate is right, which the rules currently state only as a
predicate. `pit-crew-gear-ready` does match Equipment and is correct to: `OGN-091 Pit Crew` reads *"When you
play a gear, ready me"*, a PLAY trigger, and playing an Equipment is playing a gear.

## 80. 434.3 AND 435.3 ARE THE GENERAL HOME OF THE [Equip]-vs-[Weaponmaster] CHOOSING SPLIT

> **434.3.** Attaching cards does not inherently choose or specify a target. However, Game Effects that Attach
> cards may do so.
> **435.3.** Detaching cards does not inherently choose, or specify a target. However, Game Effects that
> Detach cards may.

Both cited by nothing. This project derives the consequence card by card from the two keyword paragraphs —
818.1.b.1 (the [Equip] ability's choice IS a Target, cited 7 times) against 821.1.c.6 (*"The Equip ability is
not activated this way, and the unit with the Weaponmaster ability is not chosen"*, cited 11) — and 434.3 is
the sentence that says why those two can differ at all: **the attach ACTION never chooses; only the effect
performing it can.**

The precision worth carrying: **a [Weaponmaster] attach chooses the GEAR and not the UNIT.** 821.1.c is
*"you may choose a Card you control with the Equipment tag"*, and 821.1.c.6 removes only the unit from the
choosing. So a paid [Equip] fires a *"when you choose a friendly unit"* payoff — `SFD-144 Spirit Wheel` and
`SFD-195 Blade Dancer`, the pair §74 swept — and a Weaponmaster attach fires neither, because the pool prints
**no** *"when you choose a gear"* payoff at all. That is the exact shape of the batch-16 buff split: the
action is the same and the choosing is not.

## 81. 435.1.c IS THE ONE-SENTENCE SOURCE FOR A REFINEMENT CLAUDE.md SOURCES TO THREE PARAGRAPHS

> **435.1.c.** The card being Detached has its Effect Text become Inactive and its Rules Text cease being
> Inactive.

CLAUDE.md records the [Equip] one-way door and its 2026-09-07 refinement — *"it is a one-way door out of the
ATTACHMENT, not out of the game"* — and sources that to 718.1 read with 719.5 and 457.1. 435.1.c says it
outright and in the Detach block where a reader would look for it, together with its two companions 435.1.d
(the carrier loses the appended Effect Text) and **435.1.e** (the carrier loses the Might Bonus, cited by
nothing). The four cards that can perform a detach, swept: `SFD-011 Angle Shot` ([Reaction], either direction,
and it cantrips), `SFD-107 Strike Down` (a mandatory detach after the damage), `SFD-193 Grandmaster at Arms`
(re-attach, which 434.1.f makes a detach in the same action) and `SFD-221 Veiled Temple`. **435.1.a.1**, also
uncited, is the cheap guard on all four: *"Game effects that instruct a player to Detach a card that is
currently not Attached to anything will do nothing."*

## 82. Refusals from this batch, with scope

| row | disposition | scope |
|---|---|---|
| **434.5's worked example** | **REFUSED as dead letter** on §79(3)'s measurement: no Equipment in this pool is ever exhausted. The paragraph's general half stands as the source for "an attach is not a state reset" | today's pool |
| **434.2 / 434.2.a, 435.2 / 435.2.a** | **REFUSED**, they restate the Limited-Action classification §69 closed | permanent |
| **435.1.b / 435.1.b.1 / 435.1.b.2** | **REFUSED as physical-representation lines** (where to place the card, when a card stops being a Top-Most Card) | permanent |

## 83. Validation

Quotes from an explicit list split by source file: 8 rules passages against
`data/Riftbound-Core-Rules-2026-07-16.txt` and 7 card passages against `data/corpus_flat.txt`, zero failures.
Every count re-run in the turn it was written — the 107 / 39 / 0 / 68 / 40 gear census and the "exhaust a
gear" sweep over `data/cards.json` and `data/corpus_flat.txt`, the five readiers re-swept rather than copied
from CLAUDE.md (and the count corrected in shape: three name a gear, two name "something exhausted"), and the
two synergy predicates read out of `data/synergies.json`. No entry staged; `data/combos.json` not written.

---

# Batch 18 — why the Undertitan could not have been written as a trigger, and the thirteen spells that never brick

No entry. Two findings, the first of which closes §66 and the second of which names the members of a class
CLAUDE.md defines only negatively. Catalogue **762**.

## 84. 384 IS CITED BY NOTHING WHILE ITS EXCEPTION IS CITED 82 TIMES — and together they explain a design choice

Exactly the shape §66 found on the passive side, one block along. Measured on 2026-09-12: **384, 384.1 and
384.2 have ZERO citations, while 385.1 and 385.2 have 32 and 50.** This project cites the exception eighty-two
times and has never cited the rule it is an exception to.

> **384.2.** Triggered Abilities of Permanents are only able to have their Conditions evaluated while on the
> Board.
> **385.1.** Triggered Abilities on cards outside of the Board rely on the Information Level of the zone they
> are in.
> **385.2.** Triggered Abilities outside of the Board will self-describe their context.

Set that beside the passive pair from §66 and **one word is different, and it is load-bearing**: 365.1 says a
passive is *"**typically** only active while on the Board"*, 384.2 says a trigger's Condition can *"**only**"*
be evaluated there. The passive default is soft and the triggered default is hard, and the trigger's escape
hatch carries a condition the passive's does not — **385.1's Information Level**, whose reference table is
also uncited:

> **355.10.a.1.** Public zones are Battlefield Zones, Bases, Trashes, Legend Zones, Champion Zones, and
> Facedown Zones.

**The Main Deck and the Hand are not on that list.** So the answer to a question §66 raised and did not
answer — why is `SFD-175 Undertitan`'s *"As I'm revealed from your deck, [Add] :rb_energy_2:"* written as a
replacement effect rather than as the obvious *"When I'm revealed from your deck"* trigger? — is that **it
could not have been a trigger.** 384.2 bars the Condition from being evaluated off the board, 385.2's escape
requires the ability to self-describe its context, and 385.1 makes that escape depend on the Information
Level of the zone — and the Main Deck is secret. A replacement effect is a PASSIVE (368.1), and the passive
escape at 366.1 asks only for self-description with no Information Level clause. The card is written the only
way it could be.

**Swept over `data/cards.json` for cards whose own text acts on themselves in a non-board zone, folded by
name+type — twenty printings, and the split is exactly along that line:**

- **Triggered, and both are in the TRASH, which 355.10.a.1 makes public:** `OGN-037 Immortal Phoenix`
  (*"When you kill a unit with a spell, you may pay :rb_energy_1::rb_rune_fury: to play me from your trash"*)
  and `OGN-252 Super Mega Death Rocket!` (*"When you conquer, you may discard 1 to return this from your
  trash to your hand"*), which is 385.2's own worked example.
- **Passive, also in the trash:** `UNL-025 Undying Legion`, which is 366.1's own worked example, plus the
  **fourteen [Flow] spells** whose reminder reads *"You may play this from your trash for its Flow cost"* —
  `VEN-003`, `VEN-012`, `VEN-031`, `VEN-049`, `VEN-051`, `VEN-081`, `VEN-100`, `VEN-105`, `VEN-116`,
  `VEN-127`, `VEN-140`, `VEN-148`, `VEN-154`, `VEN-156` — where 366.2 / 366.2.a (a cost-altering passive
  applies in any zone the card can be played from) is what makes a Flow cost payable from a zone the card is
  not played from normally. `UNL-186 Death from Below` is the hybrid: a reflexive trigger (387.1) on the
  board that grants the trash play.
- **Passive replacement, in the MAIN DECK, and only these two:** `SFD-175 Undertitan` and `OGN-194 Nocturne,
  Horrifying`.
- **From the HAND: nothing at all.** Every *"from your hand"* instruction in the pool belongs to a card
  already on the board.

**The rule that falls out is worth more than the census: the trash can carry a trigger, the deck can only
carry a passive, and the hand carries neither.** That is an `excludes` fact for any future line — and it is
also the reason the two deck-dwelling cards are immune to every trigger multiplier in the pool, which the
Undertitan entries already state from the 370.1.b.1 side without knowing why the card had to be built that
way.

## 85. 355.13 NAMES THE SPELLS THAT CAN NEVER BRICK, AND CLAUDE.md DEFINES THAT CLASS ONLY BY ITS FAILURES

> **355.13.** If a card specifies that a player chooses “any number” or “up to” some number of Game Objects to
> be affected, they may choose any number of available targets, including zero. If they choose zero, the spell
> or ability can be played without any targets.

Zero citations. CLAUDE.md carries the negative half of this as one of its hardest-won lessons, from the #166
cross-audit: *"filler must be unconditionally castable, and a targeting spell never is"* — the case being
`swain-shurelya-double-conquer`, whose spell leg was `VEN-061 Decree of Insight` (*"Give an enemy Body unit
−5 Might"*), unplayable against a board with no Body unit, so all six Swain points evaporated. 358.1 checks
that all chosen targets are legal and 358.5 undoes the attempt, which is why that leg simply cannot be played.
**355.13 is the paragraph that names the exception class, and the class has members.**

Swept over `data/cards.json` for spells printing *"up to"* or *"any number of"*: **seventeen printings.**
**Thirteen are unconditionally castable** because every choice in them carries the qualifier (or, for
`UNL-103 Disposal Order`, because its second mode is *"Draw 1"*):

`OGN-105 Singularity` (Mind E6 P2), **`OGN-224 Salvage` (Order E2 P1)**, `OGN-256 Fox-Fire` (Calm/Mind E3),
`OGN-264 Guerilla Warfare` (Mind/Chaos E2 P1), `OGS-011 Flash` (Chaos E2), `SFD-043 Emperor's Divide`
(Calm E2), `SFD-080 Bellows Breath` (Mind E1 P1), `SFD-198 Arise!` (Calm/Order E6 P1),
`UNL-054 Tricksy Tentacles` (Calm E4 P1), `UNL-103 Disposal Order` (Body E2),
`VEN-103 Shadows of the Past` (Chaos E3 P1), `VEN-107 Decree of Discord` (Chaos E1 P1),
`VEN-150 Acceleration Gate` (Mind/Body E3 P1).

**And FOUR contain an "up to" and still brick, which is the trap the lesson is really about** — the tell is
whether any choice in the sentence lacks the qualifier: `SFD-023 Piercing Light` (*"Deal 2 to **a unit** at a
battlefield**, then** deal 2 to up to one other unit"*), `SFD-184 Relentless Pursuit` (*"**Move a friendly
unit.** You may attach up to one Equipment…"*), `UNL-198 Moonfall` (*"**Choose a battlefield where you have
units.** You may move up to one enemy unit…"*) and `VEN-140 Shuriken Flip` (*"Deal 2 to up to one enemy unit
at a battlefield, **then move a friendly unit**"*).

**The best filler in the pool is `OGN-224 Salvage`, and it is a second thing at the same time.** Order, 2
Energy + 1 Power: *"You may kill up to one gear. Draw 1."* By 355.13 it is castable into an empty board, by
419.4.b it counts as a card played for every non-triggered check even if every instruction fizzles, and it
replaces itself. It is also one of the sixteen gear-removal printings #200 measured against the twelve BURST
and CHAIN entries that stand on attached Equipment — so the cheapest unconditional filler in Order is the
same card as that identity's answer to an Equipment finisher. Every one of the seventeen is already in at
least one entry, so there is no row here; what there is, is a list a future author can pick from instead of
re-deriving the lesson from a broken leg.

## 86. Refusals from this batch, with scope

| row | disposition | scope |
|---|---|---|
| **355.11 / 355.11.a / 355.11.b** | **ALREADY CITED** — 20, 5 and 12 times, and `OGN-256 Fox-Fire` (Riot's worked example: a group-target that no longer collectively fulfils its restriction shrinks to a legal subset rather than fizzling) is in two entries. Checked because the probe surfaces it; no action | — |
| **355.15 / 355.16** | **REFUSED as process lines.** 355.16 (*"A player may not make choices during this step that will deterministically result in illegal choices"*) has a self-kill additional cost as its example and no pool card reproduces the shape | today's pool |
| **384.1, 424.2 / 424.2.a, 429.4 / 429.4.a / 429.5, 442.2 / 442.2.a, 423.2 / 423.2.a** | **REFUSED**, Limited-Action classification and formatting lines that §69 closed | permanent |

## 87. Validation

Explicit list split by source: 7 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 8 card
passages against `data/corpus_flat.txt`, zero failures. All rule references resolve. Every count run in the
turn it was written: the 0/0/0 against 32/50 citation split, the twenty self-referring printings folded by
name+type, the seventeen *"up to"* spells with their costs and entry counts read out of `data/cards.json` and
`data/combos.json`. No entry staged; `data/combos.json` not written.

---

# Batch 19 — 411.6 is the paragraph behind every "with me" check, and 411.6.a is why The Zero Drive works at all

No entry. One finding with three upgrades attached, one correction to a count in CLAUDE.md, and one short
note. Catalogue **762**.

## 88. THE "WITH" CHECK HAS ITS OWN PARAGRAPH, IT HAS THREE CARDS, AND BOTH HALVES ARE UNCITED

> **411.6.** Some Game Effects may check if a player has performed a Game Action “with” a Game Object. In
> order to do so, the Game Action must be attributed to the spell or ability of that Game Object if
> necessary, the player must control that spell or the ability of that Game Object, and the player must be
> responsible for the Game Action.
> **411.6.a.** Abilities granted via the effect text of a Game Object to another Game Object count for both
> Game Objects for this purpose.

**Both cited by nothing**, while their neighbours 411.4 and 411.5 are cited 11 and 10 times. Swept over
`data/corpus_flat.txt` for a check of the form *"with me"* / *"with this"*: **three cards.**

| card | the clause |
|---|---|
| `SFD-090 The Zero Drive` (Mind gear) | *"Play all units banished **with this**, ignoring their costs."* |
| `UNL-148 Cursed Sarcophagus` (Chaos gear) | *"Play a unit banished **with this**."* |
| **`UNL-181 Virtuoso` (Fury/Mind legend)** | *"if there are four spells banished **with me**, put each in its trash, channel 4 runes, and draw 1."* |

**A COUNT IN CLAUDE.md IS SHORT BY ONE, and in the way that file itself warns about.** It reads: *"`SFD-090
The Zero Drive` and `UNL-148 Cursed Sarcophagus` — the pool's only two `banished with this` cards."* That is
**true at the precision of the literal phrase and false at the precision of the concept**, because 053.1
makes a legend say *"me"* where a gear says *"this"*, so a grep for the phrase cannot see the third. The
project's own standing rule is that a uniqueness claim survives only at the precision it was measured at;
this is that rule biting a sentence in the file rather than in an entry. **The honest form is: three cards
check whether an action was performed with them, two gear and one legend, and 411.6 is the paragraph for
all three.**

**411.6.a IS WHAT MAKES THE ZERO DRIVE WORK AT ALL, and no entry says so.** The Drive's *"[Deathknell] —
Banish me"* is **Effect Text**, which 718.3 appends to the CARRIER; 053.1 and 136.2.d make that *"me"* the
carrier, so the banish is performed by an ability sitting on a unit, not on the gear. Read on its own, the
Drive's cash-out then has nothing to look at — the carrier banished itself. **411.6.a is the sentence that
closes it**: an ability granted via the Effect Text of one Game Object to another *"count[s] for both Game
Objects for this purpose"*, so the banish is *"with this"* for the Drive. `zero-drive-riptide-rex-banish-
recursion` derives the whole line correctly and cites 718.3, 719.5, 323.4, 395 and 397 — and the one
paragraph that licenses its central noun phrase is not among them.

**AND 397 WITH 411.6 IS WHY VIRTUOSO CANNOT BE FED FROM OUTSIDE.** Swept: the pool has **27 non-[Flow]
cards that banish something** and fourteen [Flow] spells that banish themselves under 829.1.b. Not one of
them can contribute to Virtuoso's four, because 411.6 requires the banish to be attributed to *that Game
Object's* ability and 397 says a component Linked Ability *"may only interact with Game Objects affected by
the Abilities it is Linked with"*. Neither of Virtuoso's two entries — `virtuoso-banish-channel` and
`jhin-virtuoso-ekko-malzahar-vi` — cites 394, 395, 396, 397 or 411.6, and the obvious first move with that
legend is to reach for a second banisher.

**THE CONTRAST WORTH CARRYING IS TWO WORDS WIDE.** Virtuoso's *"banished **with me**"* is 411.6 and is as
narrow as a check can be; `VEN-143 Master of Shadows`'s *"When **you** banish a card you own, empower me"* is
**411.4** — *"If an ability triggers when 'you' do something, it triggers when a Game Action that you are
responsible for occurs"* — and so is fed by every one of those 27 sources and by every [Flow] replay.
`master-of-shadows-banish-rummage` does not cite 411.4 either.

**UPGRADES, FLAGGED ONCE:** 411.6.a to `zero-drive-riptide-rex-banish-recursion`; 394.1, 395, 396, 397 and
411.6 to both Virtuoso entries; 411.4 to `master-of-shadows-banish-rummage`.

## 89. 421.3 — a facedown card has no properties of its own

> **421.3.** Cards that are facedown at Battlefields have their gameplay properties and permissions defined
> by the effect that put them there.
> Example: Cards that players Hide with the Hidden keyword can be Played for [0] on any subsequent turn as a
> property of the Hidden keyword.

Uncited, while 421.1 and 421.2 have three each. It is the paragraph under every [Hidden] finding this project
has made — the turn-apart play (811.1.b), the target confinement (811.1.d.2), the free play, the [Reaction]
the tail of 811.1.b grants — and it states the reason in one line: **a facedown card contributes nothing of
its own; every permission comes from the effect that hid it.** That is also the cleanest statement of why
R33 went the way it did (a prohibition on revealing blocks the play, because the play is a permission
granted by the hiding effect and not a property of the card), and why `OGN-107 Ava Achiever` — who plays a
[Hidden] card **from hand**, so 421.3 never engages — is unrestricted where a hidden play is confined. One
line in an existing entry, not a row.

## 90. Validation

Explicit list split by source: 5 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 4 card
passages against `data/corpus_flat.txt`, zero failures. All rule references resolve. Counts run in the turn
they were written: the three *"with me / with this"* cards and the 27 non-[Flow] banishers over
`data/cards.json`, and the 0 / 0 / 11 / 10 citation split plus the five entry-level citation checks over
`data/combos.json`. No entry staged; `data/combos.json` not written.

---

# Batch 20 — the H19 pass over my own batches 15–19, and the two failure modes of a play

No entry. §H19 says to budget a pass for the true-and-incomplete citation at the end of every third or
fourth batch; this is that pass over this successor's five, and it found **two real gaps in my own work**,
one of which is a paragraph cited by a single entry in the whole catalogue. Plus one contrast the Counter
block supplies for free. Catalogue **762**.

## 91. SELF-CATCH ONE — the Undertitan family also wants 370.3, and I prescribed four paragraphs without it

§66 prescribed 365.1 (the default), 366.1 (the override), 368.1 (the classifier) and 424.1.a.2 (the card is
still in the deck while revealed), and §84 added 384.2 / 385.1 / 355.10.a.1 to explain why the clause could
not have been a trigger. **All of that is about PASSIVE abilities in general. There is a paragraph about
REPLACEMENT EFFECTS and zones specifically, and I did not cite it:**

> **370.3.** If a Game Object has a Replacement Effect that is active in a specific zone, it is evaluated and
> subsequently applied if it enters that zone before an event occurs that it could replace.
> Example: A unit that reads "if a unit you control would die, you may banish me from your trash instead. If
> you do, heal that unit, exhaust it and recall it." The first unit dies simultaneously with a 1 [M] Recruit
> token. It does not enter the trash before the Recruit dies, so it will not be able to replace its death.

Measured 2026-09-12: **370.3 is cited by exactly ONE entry in 762** — `soraka-wanderer-vanguard-armory-
simultaneous-save` — and by none of the eight in the Undertitan/Nocturne family. It is the narrowest and most
on-point of the five: 366.1 says a passive outside the board self-describes its context, and 370.3 says a
replacement effect can be **active in a zone** and is applied if the object **entered that zone first**. For
a card that has been sitting in the Main Deck since the game began the timing half is free, which is exactly
why it is easy to leave out — and its worked example is the case where the timing half BITES, so the
paragraph is worth having on the family before someone builds a line that puts an Undertitan into the deck
mid-resolution.

**This is §H19 firing on the batch that discovered §H19's shape.** The citation I gave was true, the
conclusion was right, every reference resolved, every quote was verbatim — and the most specific paragraph
was missing. **UPGRADE, FLAGGED ONCE: add 370.3 to the eight, beside 366.1.**

## 92. SELF-CATCH TWO — §88 quoted 411.6 without citing the paragraph that defines the word it turns on

411.6 requires that *"the Game Action must be **attributed** to the spell or ability of that Game Object"*.
**Attribution is a defined term two paragraphs up, and §88 did not cite it:**

> **411.5.** Note that certain Game Actions, specifically Kill, can be attributed to spells and abilities.
> This attribution is not the same as the responsibility a given player has for the kill game action.
> **411.5.a.** Spells and abilities have attribution for game actions, while players have responsibility.

Cited 10 and 6 times, and they are the whole reason 411.6's three conditions are three rather than one — the
action must be attributed to the OBJECT (411.5.a), controlled by the player, and the player must be
RESPONSIBLE for it (411.1). Without 411.5.a a reader has no way to tell 411.6's first condition from its
third. **UPGRADE, FLAGGED ONCE: 411.5.a rides with 411.6 wherever it goes** — the two Virtuoso entries and
`zero-drive-riptide-rex-banish-recursion`.

## 93. THE TWO FAILURE MODES OF A PLAY ARE OPPOSITE, AND 425.1.c IS THE HALF §68 DID NOT HAVE

§68 established that a unit is uncounterable because 359.2 takes a permanent off the Chain. The Counter block
supplies the companion, and the pair is worth stating together because the outcomes are inverted:

> **358.5.** If any of the above checks fail, the actions taken in this process are undone and the action is
> cancelled.
> **425.1.c.** Countering does not refund any costs paid to play a card, activate an ability, or trigger an
> ability.

**A play that fails LEGALITY costs nothing** — targets released, costs unpaid, the attempt rewound.
**A play that is COUNTERED costs everything** — the Energy, the Power, the discard, the killed friendly unit
that an Optional Additional Cost ate at 357.2. So the whole risk of a costed play sits in exactly the window
§68 measured, and only spells have one. 425.1.c is cited 18 times, 358.5 once; the two have never been put
side by side, and the sentence a reader wants is that **the eleven counters can only ever reach the category
of card that has a window to be reached in.**

The third member of the block is the one that keeps the accounting straight in both directions:

> **425.1.b.** A card that is Countered is not considered to have been played for abilities that trigger on
> cards being played.

Cited 9 times, and it is the Counter-side statement of the split CLAUDE.md already carries from 419.4 — a
non-triggered check counts a card that was merely Finalized (419.4.b, Battering Ram counting a countered
spell) while a *"when you play"* TRIGGER does not (419.4.a.1). Same split, two blocks, and 425.1.b is the one
that says it about Countering by name.

## 94. What the self-audit did NOT find, recorded so the next pass does not re-run it

- §67's 373.2.a.1 + 370.2 pairing on `void-rush-undertitan-accelerate`: re-read against 370.1.b and 373.2,
  complete.
- §69's Limited/Discretionary census: the 34 classification lines were swept from the rules file rather than
  listed from memory, and 312.1 / 410.1.a / 312.1.b.1 are all three cited in the finding.
- §74's buff split: 702.3 / 702.3.a / 426.1.b.1 / 426.1.b.2 / 426.1.c / 702.2.a / 809.1.c all present.
- §79's gear census: 359.2.d, 143.4, 718.2, 721.2, 718.3 present; the negative sweep was run twice, once over
  `cards.json` and once over `corpus_flat.txt`.
- §85's seventeen *"up to"* spells: 355.13 with 358.1 / 358.5 for why a required target bricks, and 419.4.b
  for why a fizzled Salvage still counts as a card played. **355.12 is the companion I checked and did not
  need** — *"If a spell specifies that a player may perform a Game Action on some number of Game Objects,
  then all choices are considered targeted and chosen independently of the decision to perform the Game
  Action"* — it makes Salvage's gear a target chosen at finalization whether or not you go through with the
  kill, which matters for 809.1.c and not for castability. Cited twice; recorded here rather than added.

**The shape of both catches is the same and is worth stating once more: I cited the GENERAL paragraph and
missed the SPECIFIC one.** 366.1 is about passives and 370.3 is about replacement effects; 411.1 is about
responsibility and 411.5.a is about attribution. In both cases the general paragraph was enough to reach the
right answer, which is precisely why nothing flagged it.

## 95. Validation

Explicit list split by source: 8 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt`, zero card
passages this batch, zero failures. All rule references resolve. The 370.3 count (one entry in 762, none in
the family of eight) and the 425.1.b / 425.1.c / 358.5 counts were run in the turn they were written. No
entry staged; `data/combos.json` not written.

---

# Batch 21 — the whole process for playing an ABILITY is uncited, and it says which parts of a trigger you may refuse

No entry. The largest single uncited block this lane has found. Catalogue **762**; candidates surfaced by a
second probe, `.scratch/mid-probe-wide.mjs` (gitignored), which takes the uncited headings in range that
carry a mechanic verb and **no** Example — the complement of `mid-probe.mjs`, which only ever showed rows
Riot chose to illustrate. It reports **138** such rows in 300–499.

## 96. RULES 401–406 ARE THE ABILITY'S OWN PLAY PROCESS, AND THIS PROJECT HAS NEVER CITED ONE LINE OF IT

The rules carry two parallel processes: **349–359, Playing Cards**, which this catalogue cites in hundreds of
places, and **401–406, playing an Ability**, whose steps mirror it one for one — 401 activate or trigger,
402 make choices, 403 determine total cost, 404 pay costs, 405 check legality, 406 proceed with play.
Measured 2026-09-12: **402.3, 402.4, 402.4.a, 402.4.b, 403.1.b, 403.1.b.1, 404.1, 404.2, 404.2.a, 406.3,
406.4 and 406.5 are cited ZERO times each.** Every ability finding in this project has been derived from the
CARD process or from 383, and the block written for abilities has never been opened.

## 97. THE THREE DECISION POINTS OF A TRIGGERED ABILITY, EACH WITH A DIFFERENT ANSWER

This is what the block buys, and the asymmetry is the whole finding: **you may refuse to PAY a trigger, and
you may not refuse to CHOOSE for it.**

> **402.4.b.** If there are legal options to choose, the ability’s controller must choose them. They may not
> decline this stage of playing a Trigger.
> **404.2.** At this stage, players may decline to pay for Triggered Abilities that have incurred a cost. If
> they do, the ability will cease being a Pending Item and be removed from the Chain. It never becomes a
> Finalized Chain Item.

Set beside the decision point the predecessor opened at §39 — 383.3.e.2.a puts a leading *"you may"*'s choice
**during finalization**, before anybody has priority — a triggered ability has three gates and three
different rules:

| gate | rule | may you decline? |
|---|---|---|
| a leading *"you may"* | 383.3.e.2.a / 383.3.e.2.b | **yes**, and blind — 312.2.c gives nobody priority first |
| the CHOICE stage (targets, modes) | **402.4.b** | **no** — "They may not decline this stage" |
| the COST stage | **404.2** | **yes**, and the trigger is removed and never finalizes |

So a mandatory downside trigger cannot be dodged by declining to choose — if there is a legal option you
must take it — while any trigger that carries a cost can simply be walked away from at 404. And the two ways
a trigger leaves the chain without resolving are both marked, in identical words, as **not** a counter:

> **402.4.a.** This is not an Ability being countered.
> **404.2.a.** This is not an Ability being countered.

That matters because 425.1.b makes a **countered** card *"not considered to have been played for abilities
that trigger on cards being played"* — a distinction §93 opened. A declined or optionless trigger is a third
category again: it is not countered, so nothing that watches Countering sees it, and 425.1.c's no-refund rule
never applies because nothing was paid.

Two more from the same block, both of which restate facts this project derived elsewhere and both of which
sit where a reader would look for them:

> **402.3.** If legal options are not available for an Activated Ability, it is not legal to activate it.
> **403.1.b.1.** If a Triggered Ability has a cost within instructions (e.g. “[do X] to [do Y]”), the cost is
> taken as the base cost.

403.1.b.1 is the **third** statement in the rules of the fact this catalogue cites constantly through 383.3.b
and 204.3.a, and it carries its own cross-reference to 740.2.a. 402.3 is the activated-ability mirror of the
#166 lesson about a spell with no legal target: an activated ability with no legal option is simply not
activatable, where a TRIGGERED one goes on the chain first and is then removed by 402.4.

## 98. 406.4 COMPLETES §68 — THE OBJECT WITH NO WINDOW IS A PERMANENT, NOT "A NON-SPELL"

§68 established that a unit cannot be countered because 359.2 takes a permanent off the Chain while 359.3.a
makes a spell a Finalized Chain Item and 359.3.c gives the pre-resolution Reaction window. The ability
process says the same of abilities:

> **406.2.** This Ability becomes a Finalized Chain Item.
> **406.4.** Other players have an opportunity to play Reactions before the resolution of spells and
> abilities.

**So the correct statement is narrower than §68's and the sweep already agreed with it:** spells and
abilities both linger and both have a window; **only a PERMANENT has none.** That is exactly why the pool's
eleven counters split nine *"a spell"* to two *"spell or ability"* (`SFD-045 Not So Fast`, `UNL-106 Repulse`)
and why not one of them names a unit — there is nothing on the chain to name. 401.1's own tail is the
companion: an ability's chain item *"will not have a card representing it"* and **still creates a Closed
State**, which is why an ETB trigger hands the opponent the window its own body never did.

## 99. 303.2 IS THE MISSING HALF OF THIS PROJECT'S SIMULTANEITY MODEL

> **303.1.** Game Actions of any nature are performed one at a time and are executed completely.
> **303.2.** Game Actions cannot be performed simultaneously for any reason.
> **303.2.a.** If one or more game actions, game effects, or Triggered Abilities are activated simultaneously,
> then Turn Order is referenced to organize the sequence of actions, starting with the current Turn Player.

All three uncited. CLAUDE.md records, from the #187/300-499 lane, that **370.1.a.2** is the spell case for
simultaneity — *"An event can occur simultaneously with other events only when those events are all the
result of the same Game Action"* — and that 465.2.c.1.a is the combat case, and that reaching the right
conclusion through the wrong paragraph is a real failure class. **303.2 is the other half of that model and
states it from the opposite side: ACTIONS are never simultaneous; only EVENTS can be.** One Game Action, many
simultaneous events — which is precisely why `OGN-133 Flurry of Blades` kills every 1-Might body at once and
why *"Kill a friendly unit. If you do, kill an enemy unit"* does not. 303.2.a is the tiebreak nobody has
cited either: when things genuinely coincide, **Turn Order decides, starting with the Turn Player**, which is
the same ordering 373.1 gives for replacement effects with different controllers.

## 100. 331.1.a AND 331.1.b ARE THE CLOSED-STATE BAR, AND THEY UPGRADE §72

> **331.1.a.** Cards of all Categories, by default, cannot be played during a Closed State.
> **331.1.b.** Card abilities, by default, cannot be played during a Closed State.

Both uncited, and they are the exact twin of 343.1.a / 343.1.b, which bar the same things during a **Showdown
State** and are cited five times. **There are two separate default bars, not one**, and that is the clean
basis for the split §72 derived from 358.4's worked example: **[Reaction] answers both** (813.1.c.1, *"can be
played during Closed States on any player's turn"*), **[Action] answers only the Showdown one** (806.1.c.1,
*"This can be played during showdowns on any player's turn"*). So `SFD-111 Here to Help` can deploy a blocker
into an open Showdown and can never be held as an answer to a removal spell already on the Chain, while
`SFD-025 Rengar, Pouncing` can do both — and the reason is two paragraphs rather than one example.
**Self-upgrade to §72**, which stated the conclusion and sourced it to 358.4 alone.

## 101. Validation

Explicit list split by source: 14 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt`, zero
card passages, zero failures. All rule references resolve. The twelve zero-citation counts in §96, the
five in §99–§100 and 343.1.a's five were run in the turn they were written. The wide probe is
`.scratch/mid-probe-wide.mjs`, gitignored, and it echoes its parsed range on its first line like its sibling.
No entry staged; `data/combos.json` not written.

---

# Batch 22 — where 356.2 meets 425.1.c: the counter window is paid for in the costs that are not mana

No entry. Written to the manager's direction after §93, which asked whether 356.2.b is what licenses the
Optional Additional Cost that 425.1.c never refunds. **It is, the two blocks meet at exactly six cards, and
the pool puts the other eleven out of reach.** Catalogue **762**.

## 102. LEDGER CORRECTION TO MYSELF, AND THE FIX IS AN INSTRUMENT RATHER THAN A RULE

My §95 report listed batches 15, 16 and 19 as "outstanding with the manager". **All of it had been applied**
— re-read out of `data/combos.json` rather than out of my context: all eight of the Undertitan family now
carry 368.1, 366.1, 370.3 and 424.1.a.2; `void-rush-undertitan-accelerate` carries 373.2.a.1;
`simian-ancestor-arena-bar-wallop-ready` carries 426.1.c; `zero-drive-riptide-rex-banish-recursion` carries
411.6.a and 411.5.a; both Virtuoso entries carry 411.6; `master-of-shadows-banish-rummage` carries 411.4.
**Zero outstanding.**

§43 recorded this failure with the opposite sign — a predecessor carrying applied items forward as debt — and
the cause is identical in both directions: **the ledger lived in a context window and the truth lives in the
file.** Stating the rule again would not fix it, because §43 already states it. So the fix is an instrument:
`.scratch/mid-ledger.mjs` (gitignored) takes `entry-id:rule` pairs and prints APPLIED / OUTSTANDING / NO SUCH
ENTRY, re-reading `data/combos.json` and echoing its entry count on the first line. **Every ledger line in
every future report from this lane is generated by that command in the same turn it is sent, never typed.**

## 103. 356.2 LICENSES THE COST, 357.2 PAYS IT, AND 425.1.c GIVES NONE OF IT BACK

> **356.2.a.1.** Some Additional Costs specified by Passive Abilities on the card being played or another card
> are Mandatory, and must be paid to complete playing the card. They use the phrase "as an additional cost"
> and don't include the word "may."
> **356.2.b.1.** Some Optional Costs … are Non-Mandatory, and must be paid only if the player made the choice
> to pay them in step 2. They use the phrase "as an additional cost" and the word "may."

356.2.b.1's worked example spells out an ORDERING that no entry has cited as one: the intention is declared
in **step 2** (355, Make Relevant Choices), the cost is applied at **356.2**, any discount it buys lands at
**356.4**, and the resource actually leaves you at **357.2**. Read with 355.15 (*"These choices cannot be
changed after this step"*) that looks like a commitment made blind — and it costs nothing, because §68
established that **312.2.c gives nobody priority anywhere between step 2 and step 5**. The exposure is not
inside the play; it is after it:

> **425.1.c.** Countering does not refund any costs paid to play a card, activate an ability, or trigger an
> ability.

**So the price of the counter window is measured in the costs you cannot re-earn**, and Energy and Power are
usually not among them, because on your own turn they were going to be spent anyway. What does not come back
is a killed friendly unit, a discarded card, a spent buff and spent XP.

## 104. SEVENTEEN NON-MANA ADDITIONAL COSTS, AND ELEVEN OF THEM ARE ON CARDS THAT CANNOT BE COUNTERED

Swept over `data/cards.json`, folded by name+type, for an *"as an additional cost"* clause whose payment is
not Energy or Power: **seventeen cards. ELEVEN are UNITS and SIX are SPELLS.** By §68 and §98 a permanent
never becomes a Finalized Chain Item and never has a pre-resolution window, so **the eleven can never lose
their cost to a counter at all** — `OGN-002 Brazen Buccaneer`, `OGN-208 Cruel Patron`, `OGN-231 Commander
Ledros` (*"you may kill any number of friendly units"*, the largest irreversible cost in the pool),
`SFD-044 Legion Quartermaster`, `SFD-079 Bard, Mercurial`, `SFD-160 Zaun Punk`, `UNL-164 Safety Inspector`,
`UNL-166 Stalking Wolf`, `UNL-170 Atakhan`, `UNL-178 Poppy, Defender of the Meek`, `VEN-023 Zed, From the
Shadows`. That is a real design fact and not a coincidence: **Riot put the biggest sacrifice costs on the
card type with no counter window.**

**The exposed set is exactly six, and it is small enough to name:**

| spell | cost | mandatory? | timing |
|---|---|---|---|
| `OGN-146 Wallop` (Body E2) | spend a buff | optional | [Action] |
| `OGN-207 Call to Glory` (Order E3) | spend a buff | optional | [Reaction] |
| `UNL-140 Conscription` (Chaos E5 P2) | **spend 5 XP** | optional | — |
| `UNL-142 Heedless Resurrection` (Chaos E2 P1) | kill a friendly unit | **mandatory** | [Reaction] |
| `UNL-173 Sacrifice` (Order E1) | kill a friendly [Mighty] unit | **mandatory** | [Reaction] |
| `VEN-008 Ruthless Strike` (Fury E3) | discard 1 | optional | [Action] |

**`UNL-140 Conscription` is the worst blowout in the pool, and it is the only one whose resource has a
THRESHOLD behind it.** 730.2 makes spending XP *"reduce the value of XP marked on the Player"* and 824.1.d
makes a [Level N] ability Inactive *"as soon as the controlling player has less than [N] XP"* — so a
countered Conscription costs the card, the 5 XP, **and every Level rung it drops you below**, for nothing.
A buff, a card and a body are each a flat loss; XP is the one that takes other cards down with it.

**Two of the six cannot opt out, and they are exposed in the worst window.** `UNL-142` and `UNL-173` are both
[Reaction] spells with a MANDATORY kill, so they are cast into a Closed State — which is where all eleven of
the pool's counters live, every one of them carrying [Reaction] (measured in §68). The other four can simply
**decline the optional cost**: 356.2.b.1 makes that a step-2 choice, so Wallop, Conscription and Ruthless
Strike can each be played for mana alone and lose only mana to a counter. That is the mitigation the rules
themselves provide, and it is the reason the optional/mandatory distinction is worth citing rather than
paraphrasing.

**Measured: of the twenty entries that use those six spells, exactly ONE mentions a counter at all** —
`repulse-heedless-resurrection-shrink-target-count`, and its subject is the target count rather than the
cost. This is not an upgrade to flag on twenty entries; it is a fact about the format that belongs wherever
the project states the counter picture, because it is the other half of §68: **a counter is cheap against a
unit (it cannot be aimed at one) and expensive against exactly six spells.**

## 105. WHY THE REPLACEMENT QUESTION AND THE COUNTER QUESTION NEVER MEET IN THIS POOL

The manager's note pointed at 357.2.a, *"Costs that are replaced with other events by replacement effects are
still considered paid"*, whose worked example is `OGN-208 Cruel Patron` paying its kill-a-friendly-unit cost
into an `OGN-077 Zhonya's Hourglass`. Checked: **Cruel Patron is a UNIT**, and so are all eleven of the
sacrifice-cost cards above. So in today's pool the two questions are disjoint — **a cost that can be replaced
is on a card that can never be countered**, and the six counterable costs are a buff, a card, 5 XP and two
kills, of which only the kills could interact with a would-die replacement and neither card is a permanent
whose cost 357.2.a would preserve. Nothing to reconcile; recorded so the next reader does not go looking.

Companion from the same step, cited five times and worth naming here because it is the card-side twin of a
paragraph this project leans on heavily: **357.1.a** — *"During this step, the card's controller can use
activated abilities with the Reaction tag that Add resources to add Energy and Power to pay the card's
costs"* — is 444.2.c (cited 32 times) written for Playing Cards rather than for costs in general. The two
say the same thing about runes, and a reader chasing the Power Nexus finding from the card side will land on
357.1.a first.

## 106. Validation

Explicit list split by source: 9 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 6 card
passages against `data/corpus_flat.txt`, zero failures. All rule references resolve. Counts run in the turn
they were written: the seventeen non-mana additional costs folded by name+type over `data/cards.json`, the
eleven/six split by type, the twenty entries using the six and the one that mentions a counter, and every
citation count quoted. The §102 ledger was produced by `.scratch/mid-ledger.mjs`, not from memory. No entry
staged; `data/combos.json` not written.

---

# Batch 23 — 337.2 is one sentence this project has cited two thirds of, in two unrelated places, 118 times

No entry. Three findings from the HOT FEPR block, all from the wide probe's no-Example rows. Catalogue
**762**.

## 107. THE THREE CHAIN ITEMS THAT SKIP THE PRIORITY WINDOW ARE ONE RULE, AND NOBODY HAS CITED IT

> **337.2.** If, after finalizing the Chain Item, that item is a Unit, Gear, or an ability that Adds
> resources, it resolves immediately—Move to Step 4: Resolve.

**Cited ZERO times.** The sentence has three subjects and this project has derived two of them separately:

- the **Add** third is 429.2 / 429.2.a, cited **77** and **28** times — *"an ability that Adds resources
  resolves as soon as it is finalized, before any other outstanding chain item"*, which the catalogue uses
  to price every Add timing and which #200 batch 4 built the whole 444.2.c reopening on;
- the **Unit** third is §68, derived here from 359.2 (cited **41** times) — a permanent leaves the Chain and
  never becomes a Finalized Chain Item, so no counter can name it and no window exists;
- the **Gear** third §68 stated only implicitly, as "a permanent".

**118 citations across two unrelated arguments, and they are one rule.** What 337.2 adds beyond tidiness is
the REASON, stated in its own words: these three are exactly the chain items that **skip Step 2 (Execute)
and Step 3 (Pass) and go straight to Step 4 (Resolve)** — and Step 2 is where 338.1 hands the player with
Priority the right to respond. Nobody is ever offered a window while one of the three is on the chain,
because the process does not reach the step at which windows exist.

**The corollary is immediately usable and cuts both ways.** Anything the catalogue knows about one third is
true of the other two: an Add cannot be countered for the same reason a unit cannot (there is nothing on the
chain to name when anyone has priority); and §98's *"the object with no window is a PERMANENT"* should read
**"a Unit, a Gear, or an ability that Adds resources"** — which is also why `OGN-040 Seal of Rage` and the
rest of the 0-Energy Power base print *"(Abilities that add resources can't be reacted to.)"* as reminder
text. That parenthetical is 337.2, not 429.2.

## 108. THE FRAME IS CALLED HOT FEPR, IT IS UNCITED, AND ITS TWO HALVES RUN IN OPPOSITE DIRECTIONS

> **334.** Whenever a Player takes one or more actions that incur Tasks they should refer to the process of
> HOT FEPR: Handle Outstanding Tasks; then Finalize, Execute, Pass, Resolve.

334, 334.1, 334.2 and 334.2.a are all cited by nothing, as are 335.1, 337.1.a, 337.3, 340.2 and 340.3. The
catalogue has been reasoning inside this loop for months — 335 is cited 14 times and 340.1 ninety-four — and
has never named it. Three of its lines are worth having by number:

> **337.1.b.** Chain Items are Finalized in the order they were appended to the Chain.
> **340.1.** The newest Finalized Chain Item resolves.

**Finalize oldest-first; resolve newest-first.** 340.1 is cited 94 times and 337.1.b twice, and the pair is
the whole reason for this project's standing rule that *"a trigger you want to resolve FIRST must be placed
LAST under 383.3.d"* — with the other half, which the catalogue uses without citing: a base cost under
383.3.b / 204.3.a is paid at FINALIZATION, and finalization runs the other way, so **simultaneous triggers
are paid for in placement order and resolve in reverse placement order.** That is why a resource produced by
one trigger of an event can satisfy a 205 game action in another (resolution) and can never fund a base cost
in another (finalization) — a fact CLAUDE.md records three times from 383.3.b and never from 337.1.b.

> **337.1.a.** Finalizing an item to the chain does not pass Priority.

Zero citations, and it is the one-sentence source for 312.2.c, which this catalogue quotes constantly as
*"all pending chain items finish being finalized"*. 312.2.c says when priority arrives; 337.1.a says why it
does not arrive earlier.

## 109. 335.1 — IN A SHOWDOWN IT IS FOCUS, NOT THE TURN PLAYER, THAT RECEIVES PRIORITY

> **335.** If there are no Outstanding Tasks, no pending Chain Items, no ongoing Showdown or Combat, and it
> is the Main Phase, the Turn Player receives priority.
> **335.1.** If there are no Outstanding Tasks, no pending Chain Items, and there is an ongoing Showdown, the
> player with Focus receives priority.

335 is cited 14 times and **335.1 zero**, which means the catalogue has the Main-Phase half of the rule and
not the Showdown half. Read with 345 (*"As a Showdown begins, the player who applied Contested status to the
Battlefield gains Focus"*, which this project already uses) and 464.2.c.1 (the Attacker is the player whose
units applied Contested), the chain is complete: **inside a combat showdown the ATTACKER acts first, and
335.1 is the paragraph that turns Focus into priority.** The catalogue states the conclusion from 345 alone.

**340.2.a is the other end of it and is also uncited:** *"If this occurs during a Showdown and the chain
wasn't initiated by a triggered ability or an ability that Adds resources, focus passes to the next player
in turn order."* So Focus alternates after each resolved chain — **except** when the chain was started by a
trigger or an Add, which is 337.2's third subject appearing again from the Focus side.

## 110. Refusals from this batch, with scope

| row | disposition | scope |
|---|---|---|
| **355.9.a.2 / 355.9.a.3 / 355.9.a.5, 355.9.c** | **UPGRADE-ONLY, and worth one line in the counter picture.** 355.9.a.2 (*"'Spell' and 'ability' refer to objects on the Chain unless specified otherwise"*) is §68 reached from the targeting side — the eleven counters can only ever name a chain object — and 355.9.c's first example (*"A spell that says 'Counter a spell' cannot target itself"*) closes the degenerate case. Its second example is the useful half and is uncited: *"An ability of a permanent can target that permanent, because abilities and their sources are separate objects."* 355.9.a.3 and 355.9.a.5 pair with 355.10.a.1's public-zone list to make a facedown card and the Chosen Champion legal targets, which `OGN-181 Pack of Wonders` (*"Return another friendly gear, unit, or facedown card to its owner's hand"*) is the pool's only user of | permanent |
| **390.4** | **UPGRADE-ONLY.** *"Delayed Passive Abilities are Passive Abilities that are applicable only during a specified window of time"* is the DEFINITION whose example §45 already used at 391 (`OGN-032 Ravenborn Tome`). The 391 / 390.5 split §45 drew stands; 390.4 names the passive sub-kind | permanent |
| **333, 336, 338.1.a.1–.a.7, 339.1, 339.2** | **REFUSED as process plumbing** — the definition of a Task, the entry condition to FEPR, and the mechanical steps of passing priority. 338.1.a.1 restates 331.1.a, which §100 already opened | permanent |

## 111. Validation

Explicit list split by source: 10 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 1
card passage against `data/corpus_flat.txt`, zero failures. All rule references resolve. Every citation
count in §107–§109 was run in the turn it was written, and the 0-against-77-and-28-and-41 split in §107 is
the number the finding stands on. No entry staged; `data/combos.json` not written.

---

# Batch 24 — the Cleanup block: eight triggers, a loop, and the one sentence that says nobody gets priority inside one

No entry. Catalogue **762**. This catalogue cites **323 eight hundred and fifty-seven times** and **319 a
hundred and seventeen**, and the paragraphs that say when a Cleanup happens, what you may do during one and
how it ends are largely uncited: **318, 319.2, 319.4, 319.7, 320.1, 322 and 322.1 are ZERO.**

## 112. 320.1 IS THE SENTENCE FOR "NO PRIORITY INSIDE A CLEANUP", WHICH THIS PROJECT DERIVES FROM THREE OTHERS

> **320.** While a Cleanup is occurring, Chain Items cannot be Finalized or Resolved.
> **320.1.** New Pending Items can be added, but Finalized Items cannot be executed and Priority and Focus
> are not passed or awarded.

320 is cited five times and **320.1 zero**. CLAUDE.md states the consequence in the #117 walk — *"a 'when X,
you may pay N to …' trigger that fires inside a Cleanup (no priority there: 312.2 / 453 / 323.12) must have
the Energy banked beforehand"* — assembling it from three paragraphs, none of which is about Cleanups.
**320.1 is about Cleanups and says it in one clause.**

It also says the half those three do not: **new Pending Items CAN be added.** So a Cleanup is not a frozen
window — triggers pile onto the Chain throughout it — it is a window in which nobody is ever *offered* the
chance to respond to them. That is the same shape as §107's 337.2: the items exist, the step that hands out
priority is simply never reached.

And the escape is the one #200 batch 4 already found: **444.2.c is not priority**, so a rune still pays into
a finalization that happens inside a Cleanup (*"Players may activate abilities that Add resources with the
Reaction keyword at any time that they are instructed to Pay resources"*). The honest combined statement is
therefore **"no priority in a Cleanup, and that was never what paying required"** — the #117 sentence's
conclusion has already been corrected once on exactly this point, and 320.1 is where the corrected version
belongs.

## 113. RULE 319 IS THE COMPLETE LIST OF WHEN A CLEANUP HAPPENS, AND THE WIDEST ENTRY IS UNCITED

Eight triggers, and the catalogue's usage is lopsided: 319.6 (*"After any number of Game Objects enter or
leave the Board"*) is cited **79** times and 319.8 (*"After a Move is completed"*) **23**, while

> **319.7.** After the status of any number of Game Objects changes for any reason

is cited **zero** — and it is the widest of the eight. Exhausting a unit, readying it, stunning it,
Empowering it, and applying Contested (190.3.a.1) are all status changes with no zone change and no move, so
each one makes a Cleanup an Outstanding Task by 319.7 and by nothing else in the list. That matters wherever
this project's arguments turn on a Cleanup arriving: the evacuation family needs 323.6 to strip Control *"in
the same Cleanup"*, and the Cleanup that runs it is 319.7's when the trigger was a status change rather than
319.6's or 319.8's. 319.2 (*"After the game transitions between Phases"*) and 319.4 (*"After a Pending Item
becomes a Finalized Item on the Chain"*) are the other two uncited entries; 319.4 is why a Cleanup sits
between finalizing and resolving at all.

## 114. 322 IS THE CLEANUP LOOP, AND THE PROJECT HAS ONLY ITS SPECIAL CASE

> **322.** If an event occurs during a Cleanup that qualifies for a Cleanup, another Cleanup will occur
> immediately after the first completes, repeating until a Cleanup occurs with no new change in the game's
> state.
> **322.1.** These new Cleanups are themselves Outstanding Tasks.

Both zero. CLAUDE.md carries **324.2** — *"If events during a Special Cleanup require another Cleanup, a
normal Cleanup is invoked"* — which the #62 recall family needed, and 324.2 is the SPECIAL case of 322.
**322 is the general loop and the reason any chain of consequences terminates**: a death causes a status
change, which qualifies for a Cleanup, which may cause another, *"repeating until a Cleanup occurs with no
new change in the game's state"*. Its companion in the other direction is **321** (cited 10) with **321.1**
(cited 5): a Cleanup cannot interrupt a resolving Chain Item; it queues as an Outstanding Task instead. So
the two rules fence each other — **resolution never yields to a Cleanup, and a Cleanup never yields to
resolution** — and together they are why 370.1.a.2's simultaneity and 303.2's one-action-at-a-time can both
be true (§99).

## 115. Why there is no entry, and what the wide probe has left

Every consequence in this batch is a timing fact that applies to the whole catalogue rather than to a card
set, which is the shape the 300–499 range has produced since §71: the cards in range are all catalogued and
what remains is structure. The wide probe's unexamined rows, for whoever takes this lane next: **316.7 and
316.8** (how a Combat is initiated and when a Showdown is Staged), **323.2** (assigning and removing the
Attacker/Defender designation inside a Cleanup), **330.2** (a card played while a Chain already exists is
placed on the existing Chain), **348.2**'s Non-Combat Showdown children under a parent cited 361 times, and
**355.14.d–.i**, the Splitting block, of which CLAUDE.md already has 355.14.b/c/e/f/g/h.1/i.

## 116. Validation

Explicit list split by source: 8 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt`, zero
card passages, zero failures. All rule references resolve. The citation counts — 857 for 323, 117 for 319,
79 and 23 for 319.6 and 319.8, and zero for 318, 319.2, 319.4, 319.7, 320.1, 322 and 322.1 — were run in
the turn they were written. No entry staged; `data/combos.json` not written.

---

# Batch 25 — the bloodless conquer has exactly three answers, and the attacker holds the window that shuts all three out

No entry, and this one is a near miss: the mechanism already has an entry
(`ride-the-wind-overzealous-fan-non-combat-showdown-conversion`, built on 316.8.b.1.a). What that entry does
not have, and what **forty-six of the forty-seven entries standing on 344.2 do not have either**, is the
member list on both sides. Catalogue **762**.

## 117. THE RISK, ALREADY CATALOGUED ONCE, AND THE 47-TO-1 SPLIT

> **316.8.b.1.a.** These Showdowns are called Non-Combat Showdowns. During the proceedings of a Non-Combat
> Showdown, units controlled by a different player may become present at the Battlefield where the Showdown
> is ongoing. This will cause the Showdown to become a Combat Showdown in the following cleanup.

Measured 2026-09-12: **47 entries cite 344.2** — the paragraph that hands a player a Showdown, and then
348.2.a a Conquer, for walking onto an open battlefield with nobody to fight. **Exactly ONE of the 47 cites
316.8.b.1.a**, and it is the entry built to exploit the conversion rather than to defend against it. So
forty-six lines in this catalogue treat a bloodless conquer as a fait accompli, and the rule says it is not.
316.8.c is the permission: *"A Showdown is a structured Window of Opportunity where Players may play cards
and activate abilities with Action or Reaction."*

## 118. THE ANSWER SET IS THREE CARDS, MEASURED, AND ALL THREE ARE [Action]

To convert, the defender must put a unit **they** control at the battlefield where your Showdown is ongoing,
from outside it, at Action or Reaction speed. Swept over `data/cards.json`, folded by name+type, for an
[Action] or [Reaction] card carrying an instruction that moves a unit its own controller controls: **twelve
printings**, of which nine send the unit **to base** or require a body already at the destination
(`SFD-043`, `SFD-050`, `SFD-082`, `UNL-045`, `UNL-198`, `VEN-112`, `VEN-139`, `OGS-011`, `SFD-053`). **Three
can put a body on an arbitrary battlefield:**

| card | domains | cost | speed | clause |
|---|---|---|---|---|
| `OGN-173 Ride the Wind` | Chaos | E2 P1 | [Action] | *"Move a friendly unit and ready it."* |
| `OGN-262 Zenith Blade` | Calm/Order | E3 P2 | [Action] | *"Stun an enemy unit at a battlefield. You may move a friendly unit to that enemy unit's battlefield."* |
| `SFD-184 Relentless Pursuit` | Fury/Body | E2 P1 | [Action] | *"Move a friendly unit."* |

None is banned or restricted (checked against `data/legality.json`). 449 with 420.2.b is why an unrestricted
*"move a friendly unit"* reaches any battlefield: a move by EFFECT is not bound by 144.4 and carries no
exhaust (420.3.a puts that on the Standard Move alone).

**Two things follow that no entry states.** First, **the three span Chaos, Calm/Order and Fury/Body, so no
identity is safe** — a defender in almost any legend pair can be holding one. Second, and better,
**`OGN-262 Zenith Blade` is strictly the worst one to walk into**, because it stuns your body on the way in:
423.1.b removes a stunned unit's contribution to the combat damage step, so the combat it just created is
one your lone attacker cannot win, and 423.1.c keeps its full Might as the price to kill it. You lose the
unit and the battlefield to one 3-Energy card.

**[Ambush] is not a fourth answer**, and the reason is worth recording: 822.1.b grants *"you may play me as a
[Reaction] to a battlefield where you have units"*, and the defining feature of the board a Non-Combat
Showdown opens on is that the defender has no units there. The whole [Ambush] cycle is shut out by its own
permission.

## 119. AND THE ATTACKER HOLDS THE FIRST WINDOW, SO ALL THREE CAN BE SHUT OUT FOR ONE ENERGY

All three answers are **[Action]** and none is [Reaction]. 358.4 is what makes that decisive:

> **358.4.** Check that the card has the appropriate permissions to be played at this timing.
> Example: If the state is Showdown Closed and the card was the one that Closed the state, ensure that it has
> [Action] or [Reaction].
> Example: If the state is Closed and the card wasn't the one that Closed the state, ensure that it has
> [Reaction].

So an [Action] card gets into a Showdown **only while the state is still open** — only as the card that
closes it. Once any chain item exists there, the window is [Reaction]-only and all three answers are locked
out. And the attacker moves first, by two paragraphs this lane opened in §109: **345** gives Focus to the
player who applied Contested status, and **335.1** gives priority to the player with Focus.

**Therefore: play any [Action] card into the Showdown yourself and the bloodless conquer becomes
unanswerable.** The pool prints **91 [Action] cards** and the floor is 1 Energy — `OGN-004 Cleave` (Fury),
`OGN-183 Stacked Deck` (Chaos), `SFD-135 Factory Recall` (Chaos), `SFD-003 Blood Rush` (Fury), and best of
all **`OGN-179 Acceptable Losses`** (Chaos, E1, *"Each player kills one of their gear"*), which in a list
running no gear is a **1-Energy blank** by 055.1 — *"If all of a card's instructions are impossible, it is
still played and resolved, but nothing happens"* — and therefore does nothing except close the state.
CLAUDE.md already records that card as a one-sided gear wipe in a gearless deck; this is a second, unrelated
job for the same property.

That is the shape worth carrying: **the risk is real, it is three cards wide, and the tax to remove it
entirely is one Energy and one card slot — which is a cheaper insurance premium than any of the
forty-six lines currently pays, because none of them pays it at all.**

## 120. Why this is a finding and not an entry

The mechanism already has `ride-the-wind-overzealous-fan-non-combat-showdown-conversion`, and a second row
on the same paragraph would be the strict-subset shape the merge script refuses. What belongs in the
catalogue is a **notable on that entry naming its two siblings** (`OGN-262`, `SFD-184`) and the [Action]-only
shut-out, and a line wherever the evacuation family is described — this is a fact about forty-six entries,
not about one card set. **UPGRADE, FLAGGED ONCE.**

## 121. An instrument note, because I made the error this project keeps naming

My first sweep for the answer set required the phrase *"move … to <destination>"* and returned 45 rows that
did **not include `OGN-173 Ride the Wind`** — the very card the existing entry is built on — because its
text is *"Move a friendly unit and ready it"* with no destination clause at all. A move by effect names no
destination precisely when it is least restricted, so **a destination-shaped regex is blindest exactly where
the answer lives.** Caught because the count disagreed with a card I already knew belonged in it. Re-swept
on the actor rather than the destination (*moves a unit its own controller controls*), the set is twelve and
the answer is three.

## 122. Validation

Explicit list split by source: 4 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 6 card
passages against `data/corpus_flat.txt`, zero failures. All rule references resolve. Counts run in the turn
they were written: 47 entries citing 344.2 and the one citing 316.8.b.1.a over `data/combos.json`; the
twelve-then-three mover sweep and the 91 [Action] cards with their cost floor over `data/cards.json`; and
the ban check for all three answers and all five shield candidates against `data/legality.json`, whose
`entries[].bases` is an array. No entry staged; `data/combos.json` not written.

---

# Batch 26 — §119 IS WRONG, AND I FOUND IT BY READING THE NEXT PAGE

No entry. This batch exists to correct the batch I sent an hour ago, before anything is built on it.

## 123. THE CORRECTION: PLAYING A CARD DOES NOT LOCK THE DEFENDER OUT, IT HANDS THEM FOCUS

§119 concluded that because all three answers to a bloodless conquer are [Action] and 358.4 admits an
[Action] card **only as the card that closes the state**, the attacker — who holds Focus by 345 and priority
by 335.1 — could shut all three out by playing any 1-Energy [Action] card into the Showdown first.
**Every step of that is true and the conclusion is false**, because it treats a Showdown as a single window
and it is a loop:

> **347.1.b.** When that Chain closes, Focus passes to the next Player in Turn Order.
> **346.** When the last item on the chain resolves and the turn returns to an Open State during a Showdown,
> Focus passes, and the next Player gains both Focus and Priority.
> **347.2.a.** If all Players have passed once in sequence, the Showdown ends.
> **348.** If all players pass Focus without playing a spell or activating an ability, then the Showdown
> Closes.

So closing the state with an [Action] card buys a lock-out that lasts **exactly as long as that chain**.
When it resolves, 340.2 returns the Showdown to an Open State, 346 and 347.1.b pass Focus **to the
defender**, and the defender may now play their own [Action] card into an open Showdown — which is the
very thing §119 said they could not do. **The attacker's first card does not deny the window; it spends a
card and then gives the window away.** The Showdown ends only when every player passes in sequence
*without* playing anything (347.2.a, 348), so the defender always gets at least one turn with Focus before
348.2.a can hand over the battlefield.

**§118 stands unchanged** — the answer set is still exactly `OGN-173 Ride the Wind`, `OGN-262 Zenith Blade`
and `SFD-184 Relentless Pursuit`, still all [Action], still spanning three identities, and `OGN-262` still
stuns the body it walks into. **§119's remedy is withdrawn.**

## 124. WHAT THE REMEDY ACTUALLY IS, AND IT IS THE ELEVEN COUNTERS

The defender's answer is an [Action] card, so it **closes the state as it is played** — and 358.4's other
half then says that from that instant only [Reaction] gets in. The attacker holds priority in that Closed
State by 312.2.c and 338.1, and the pool's counters are **all eleven [Reaction]** (§68). So the real
sequence is:

1. you walk in; 344.2 opens a Non-Combat Showdown; 345 gives you Focus and 335.1 gives you priority;
2. you pass (there is nothing to gain by acting first — §123);
3. the defender plays `OGN-262 Zenith Blade` or one of its two siblings, closing the state;
4. **you counter it** — `OGN-045 Defy` at 1 Energy + 1 Power answers a 3-Energy spell inside its own window,
   and 425.1.c means the defender gets none of that cost back;
5. all players pass in sequence, 347.2.a ends the Showdown, 348.2.a gives you Control and 348.2.a.1 the
   Conquer.

**So the insurance is a counterspell, not a blank** — which is a materially different deckbuilding
conclusion from the one I sent: a 1-Energy [Action] card of any kind was the wrong answer, and the right
one is Calm's `OGN-045 Defy` (E1 P1, *"Counter a spell that costs no more than :rb_energy_4: and no more
than :rb_rune_rainbow:"*, which covers all three answers) or `OGN-064 Wind Wall` (E3 P2, unconditional).
That narrows the protection to identities with Calm in the pair for the cheap version, where the withdrawn
advice implied every deck could buy it for one Energy.

## 125. THE FAILURE SHAPE, WHICH IS THE SAME ONE §91 AND §92 FOUND FROM THE OTHER SIDE

§91 and §92 were **true and incomplete** citations — the general paragraph cited, the specific one missed.
This is the mirror: **a complete and correct citation reasoned one step past what it licenses.** 358.4 says
what may be played *at a given moment*; I used it to answer a question about *the rest of the Showdown*,
which is 346 and 347's subject and sits on the next page. No quote was wrong, no reference failed, and the
arithmetic of §118 was untouched — the defect was entirely in the scope of the inference.

**The check that would have caught it before it shipped is one question: does this rule describe an
instant or a loop?** 358.4 is an instant. 347 is headed *"During a Showdown, the player with Focus may do
one of the following"* and is plainly iterative. Both blocks were open in front of me and I read only one.
Caught within the hour because writing up the next batch's queue put 346–348 on the screen, which is an
argument for keeping the neighbouring block in the queue rather than skipping to a new one.

Companion measurement: **340.2.a and 347.1.a are cited zero times** and 347.2.a twice, against 348's 381 and
345's 73 — the catalogue holds the paragraph that OPENS a Showdown and the one that CLOSES it, and almost
nothing about the loop in between.

## 126. Validation

Explicit list split by source: 5 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 1
card passage against `data/corpus_flat.txt`, zero failures. All rule references resolve. The citation counts
in §125 were run in the turn they were written. No entry staged; `data/combos.json` not written. **The
upgrade flagged in §120 is amended, not withdrawn: the notable on
`ride-the-wind-overzealous-fan-non-combat-showdown-conversion` should name `OGN-262` and `SFD-184` as
siblings and the remedy as a [Reaction] counter, never as "play an [Action] card first".**

---

# Batch 27 — closing the Showdown block, because §123 said to finish a block before leaving it

No entry. Catalogue **762**. §125 ended with the observation that §119's error was caught only because
writing the next queue put the neighbouring paragraphs on screen, and drew the moral that a block should be
finished before the lane skips to a new one. This is that finish, and the block had one more sentence in it
that would have prevented the error outright.

## 127. THE ONE-PHRASE REFUTATION OF §119 SITS TWO PAGES BEFORE THE PARAGRAPHS I USED

> **342.** A Showdown is a Window of Opportunity in which Players have an Open State in which they may play
> Spells **in an alternating fashion**.
> **342.1.** Each spell played this way creates a Chain as normal.

342 is cited twice and 342.1 not at all. *"In an alternating fashion"* is the whole of §123 in four words:
a Showdown is not one window, it is a sequence of them, and each card played inside it opens and closes a
Chain of its own (342.1) before the next player's turn to act. Had this lane cited 342 when it first
reached for 358.4, §119 would never have been written.

**A wording discrepancy in Riot's own text, REGISTERED and never erratized.** 342 says players *"may play
**Spells**"*, and both operative paragraphs are wider: **316.8.c** — *"a structured Window of Opportunity
where Players may play cards and activate abilities with Action or Reaction"* (cited 52 times) — and
**347.1**, *"Play a Card or Activated Ability that is legally timed"* (cited 22). 342 is summary prose and
the permissions govern; a unit with [Ambush] and an activated ability with [Action] both reach a Showdown,
which several entries already depend on. Same treatment as the 383.1.b / `OGN-118` mismatch and the 763.1
comma: the discrepancy goes in the walk record, never into `data/errata.json`, whose find-string failure is
the only mechanism keeping our card text honest.

## 128. FOCUS AND PRIORITY ARE TWO THINGS THAT PASS AT TWO DIFFERENT RATES, AND THREE OF THE FOUR PARAGRAPHS ARE UNCITED

> **313.2.** A player who gains Focus also gains Priority.
> **313.3.** A player who passes Priority retains Focus.
> **313.4.** A player may not make discretionary actions with Focus unless they also possess Priority.
> **313.5.** If the turn is in a Neutral State, no player has Focus.

313.3 is cited four times; **313.2, 313.4 and 313.5 are zero.** Together they say the thing §119 got wrong,
in the abstract rather than through the Showdown's own steps: **a Showdown runs two nested loops.** The
inner loop is PRIORITY — 338 Execute, 339 Pass, 340 Resolve — and passing priority inside it **keeps your
Focus** (313.3). The outer loop is FOCUS, and it turns over only when a Chain closes (346, 347.1.b) or when
the Focus-holder declines to act at all (347.2, 347.2.b). Collapsing the two is exactly the error: I read
the inner loop's admission rule (358.4) as if it governed the outer loop's turn order.

313.4 is the pair's teeth — **Focus without Priority is not permission to act** — and 313.5 scopes the whole
concept: outside a Showdown nobody has Focus, which is why 312.2.a's Main-Phase priority and 335.1's
Showdown priority are two separate grants rather than one. **343.2**, also uncited, is the definition that
makes 344.2's precondition readable: *"The turn is said to be in a Neutral State if no Showdown or Combat is
in progress"* — and 344 and 344.2 are cited 177 and 126 times on a phrase whose second half was never
looked up.

## 129. The block is now closed; what it leaves

Cited-by-nothing and deliberately left, with the reason: **341** and **343** are headers; **347.1.a**
(*"The card or activated ability will start a Chain as normal"*) restates 342.1; **340.2.a** and **346.1**
are the same exception written twice (Focus does not pass when the chain was opened by a trigger or an Add
ability) and 346.1 is already cited ten times — that exception is §107's 337.2 appearing a third time, from
the Focus side, and is the one row in this block still worth an entry-level note if the catalogue ever
prices a combat chain: **the Combat Chain opens from triggered abilities, so Focus does not pass when it
resolves**, which Riot spells out in 346.1's own example.

## 130. Validation

Explicit list split by source: 8 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt`, zero
card passages, zero failures. All rule references resolve. Citation counts for 342, 342.1, 313.2–313.5,
343.2, 344, 344.2, 316.8.c, 347.1 and 346.1 were run in the turn they were written. No entry staged;
`data/combos.json` not written.

---

# Batch 28 — a stolen body changes sides at the next Cleanup, and a split pays the Deflect tax once per body

No entry. Two findings, the second of which refines my own §104. Catalogue **762**.

## 131. 323.2.b — THE DESIGNATION FOLLOWS THE CONTROLLER, AND ONE CARD IN THE POOL USES IT

> **323.2.b.** If there are Units present at the Battlefield the Combat is taking place at, but have the
> opposite designation of their controller, they lose that designation, and gain the same designation as
> their controller now.

**Cited zero**, while its siblings 323.2 and 323.2.a are cited 66 times each. It is the rule for a unit
whose CONTROL changes while a Combat is in progress: at the next Cleanup it stops being an Attacker and
becomes a Defender, or the reverse.

Swept: **four cards take control of an enemy unit, and three of them RECALL it** — `OGN-203 Possession`
(*"Take control of it and recall it"*), `UNL-140 Conscription` (*"Take control of it, exhaust it, and recall
it"*) and, at end of turn, the fourth. **Only `SFD-202 Hostile Takeover` leaves the body standing where it
is**: *"[Hidden] … Take control of an enemy unit at a battlefield. Ready it. (Start a combat if other
enemies are there. Otherwise, conquer.) Lose control of that unit and recall it at end of turn."* So it is
the pool's one card for which 323.2.b has anything to say.

**And the use it points at is defensive, which is not the one the catalogue has.** `SFD-202` is [Hidden], so
the tail of 811.1.b hands it [Reaction] from the following turn and it costs 0 — and 811.1.d.2 confines its
target to the battlefield it was hidden at, which is a battlefield you control, which is exactly where the
opponent will attack you. Steal their largest Attacker mid-combat and 323.2.b flips it to your side at the
next Cleanup, so 465.2.c sums its Might **on your side instead of theirs**: a swing of twice its Might for
zero Energy. The catalogue's one `SFD-202` entry, `ava-achiever-hostile-takeover-conquer`, uses the card
offensively for the conquer and cites 464.2.c.3 rather than 323.2.b. **UPGRADE, FLAGGED ONCE**, with the
defensive use as a notable; the line needs no second card, which is why it is a notable and not a row.
(103.2.d.2 still forces the legend: `SFD-202` is a Signature card tagged Renata Glasc.)

## 132. 355.14.a AND 355.14.d — EVERY BODY IN A SPLIT IS ITS OWN TARGET, SO EVERY BODY PAYS DEFLECT

> **355.14.a.** If a card specifies that an amount of damage may be split among some number of Units, then
> each Unit chosen is Targeted.
> **355.14.d.** Each Target is valid, and contributes to Targeting Effects individually.

Cited **once** and **zero**, against 355.14.h's sixteen. Read with 809.1.c — *"Spells and abilities an
opponent controls that target [me/this] cost an amount of Power equal to [Deflect Value] more to play as an
additional cost **for each time they choose** [me/this]"* — a split is not one choice, it is N, and **it
pays the [Deflect] tax once per body.**

The numbers make it real. **47 printings carry [Deflect]**, and 187.7 gives it to a token by rule: *"A 1 [M]
Bird token is a domainless unit token with 1 Might, the Bird tag, and the Deflect keyword."* So
`UNL-044 Flurry of Feathers`, which plays four of them at [Reaction] speed, taxes an incoming split **four
rainbow**. The pool prints exactly two splits — `UNL-192 Alpha Strike` (Calm/Body, E3 P1, and Riot's own
worked example at 355.14.h.1) and `OGN-041 Volibear, Furious`, who carries [Deflect 2] himself — and both
are priced in this catalogue without the tax. 355.14.c caps the targets at the damage available when played
and lets you take fewer, so the mitigation is to spread less: **[Deflect] converts a split from a free
spread into a rainbow per body.**

**It reaches the TRIGGER, not only the spell.** 809.1.c says *"Spells and abilities"* and 809.1.d repeats it
— *"an effect that imposes a Mandatory Additional Cost on Spells and Abilities that choose the Game Object"*
— so Volibear's *"When I attack, deal 5 damage split among any number of enemy units here"* incurs the cost
too, at 404 (Pay Costs) of the ability process §96 opened. And §97's asymmetry then bites a real card:
**404.2 lets you decline to pay, and declining removes the whole trigger from the Chain** — so against a
garrison of Birds, Volibear's trigger is pay-per-body or nothing at all, with no partial option beyond
choosing fewer bodies at 402.

## 133. A REFINEMENT TO MY OWN §104 — the mana half is not always negligible

§104 argued that 425.1.c's no-refund rule bites hardest on costs that are **not** mana, *"because on your
own turn Energy and Power were going to be spent anyway"*, and listed the six spells with a non-mana
additional cost. 809.1.d names the missing case: **a [Deflect] tax is a Mandatory Additional Cost (356.2.a.2)
that scales with the number of bodies you chose.** `UNL-192 Alpha Strike` aimed across four Bird tokens
costs 3 Energy + 1 Power + **4 rainbow**, and a counter refunds none of it — a larger loss than four of the
six cards on §104's list. The corrected statement: **the no-refund rule bites on any cost you would not
otherwise have paid, and a Deflect tax is exactly that** — you pay it because of what the opponent's board
looks like, not because of what you were going to do with your turn.

## 134. Validation

Explicit list split by source: 6 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 4
card passages against `data/corpus_flat.txt`, zero failures. All rule references resolve. Counts run in the
turn they were written: the four control-steal cards and the two split cards over `data/corpus_flat.txt`,
the 47 [Deflect] printings folded by name+type over `data/cards.json`, and the 0 / 1 / 16 / 66 citation
splits over `data/combos.json` plus `data/synergies.json`. No entry staged; `data/combos.json` not written.

---

# Batch 29 — a "choose" trigger reads TARGETING, so the pool's two mass buffers fire none of them

No entry. **One false sentence in two shipped entries and in CLAUDE.md**, refuted by a block cited zero
times. Catalogue **762**.

## 135. 383.4.b IS UNCITED IN FULL, AND ITS THIRD CLAUSE RE-BASES THE WHOLE CHOOSE FAMILY

> **383.4.b.** Targeting Effects are Triggered Abilities whose Condition includes a Game Object becoming
> targeted.
> **383.4.b.1.** These are commonly structured as “When you choose me …” or “When you choose a [Game
> Object] …”
> **383.4.b.2.** These Triggered Abilities are put on the Chain as Pending Items after a spell or ability
> that targets an appropriate Game Object is Finalized.
> **383.4.b.3.** Although these abilities say “choose” in their Condition, **they trigger specifically when
> an appropriate Game Object is Targeted.**

**383.4.b, .b.1, .b.2 and .b.3 are cited ZERO times each** — against 355.10.d's **151** and 702.2.a's **83**,
which are the two paragraphs this catalogue currently reasons with on exactly this question. The family is
the four cards §74 swept: `SFD-057 Irelia, Fervent`, `SFD-142 Jae Medarda`, `SFD-144 Spirit Wheel`,
`SFD-195 Blade Dancer`.

## 136. THE CORRECTION, AND IT IS IN TWO SHIPPED ENTRIES AND IN CLAUDE.md

CLAUDE.md states, from the #97 Might lens: *"And buffing is CHOOSING (702.2.a), so every mass buffer fires
Blade Dancer / Irelia Fervent."* Two entries repeat it:

- `peak-guardian-fiora-mass-ready` — *"Buffing is CHOOSING (702.2.a), which is why this also switches on
  SFD-057 Irelia, Fervent … and SFD-195 Blade Dancer."*
- `overt-operation-fae-dragon-industrialist` — *"AND EVERY BUFF IS A CHOOSE (702.2.a …), so a mass buffer is
  N chooses off one card. That is the trigger SFD-195 Blade Dancer and SFD-057 Irelia, Fervent read."*

**Both sentences are false for the cards those two entries are built on**, and the chain is three
paragraphs:

1. **383.4.b.3** — the trigger fires on TARGETING, whatever verb its own text uses;
2. **355.7** — *"When a card Chooses one or more specific Game Objects to affect, it is Targeted **unless
   indicated otherwise by the rules in this section**"*;
3. **355.10.d** — the exception, whose own worked example is the identical grammar: *"'Kill all units at a
   battlefield' targets a battlefield, but does not target any units."*

`OGN-223 Peak Guardian` reads *"buff **all other friendly units** there"* and `OGN-153 Overt Operation`
reads *"Then buff **all friendly units**"*. Both select by characteristic, so by 355.10.d neither TARGETS
anything, and by 383.4.b.3 neither fires a single choose-trigger. **702.2.a's "a player chooses a Unit and
then places a buff on it" is the buff ACTION's internal description, not a statement that the card targeted
anybody** — and 383.4.b.3 exists precisely to stop that word being read across.

**Swept over `data/cards.json`, folded by name: 39 cards buff, 37 of them choose and exactly TWO are
programmatic — `OGN-153 Overt Operation` and `OGN-223 Peak Guardian`.** So CLAUDE.md's sentence is true of
thirty-seven cards and false of the only two anyone would call a "mass buffer", which is what makes the
error worth correcting rather than trimming: **the word "mass" in that sentence points at exactly the two
cards the sentence is wrong about.**

**Neither entry's verdict moves.** `peak-guardian-fiora-mass-ready` says of the two choosers *"Neither is
declared, because neither is needed"*, and `overt-operation-fae-dragon-industrialist` says *"the chooses are
not the bottleneck there"*. So this is a false sentence inside a correct entry — the class CLAUDE.md names
when it says a false fact travels further than a walked line. **UPGRADE, FLAGGED ONCE**, on both entries and
on the CLAUDE.md sentence; the honest form is *"a buffer that CHOOSES fires Blade Dancer and Irelia Fervent;
the pool's two ALL-shaped buffers (OGN-153, OGN-223) target nothing and fire neither."*

## 137. IT IS THE SAME AXIS THE PROJECT ALREADY HAS ON THE REMOVAL SIDE, POINTING THE OTHER WAY

CLAUDE.md records, from #97: *"355.10.d — a Game Object programmatically selected based on its
characteristics rather than chosen is NOT a target, so Stare Down moves a whole garrison of [Deflect] bodies
for free … while Public Execution pays."* **That is this batch's rule, applied correctly, one lens over.**
The buff family got the same axis backwards, and the reason is visible in the two sentences: the removal
case reasons from 355.10.d and the buff case reasons from 702.2.a's verb. 383.4.b.3 is the paragraph that
makes them the same question.

Two riders fall out. **A mass buffer pays no [Deflect] either** — 809.1.c charges *"for each time they
choose"* and a programmatic selection is not a choice, which is the Stare Down result again, so
`OGN-223 Peak Guardian` buffs a garrison of Bird tokens for nothing where §132's split spell would pay a
rainbow each. And **383.4.b.2 gives the timing nobody has**: a choose-trigger goes on the Chain *"after a
spell or ability that targets an appropriate Game Object is **Finalized**"*, so with 340.1 (newest resolves
first) **the trigger resolves BEFORE the spell that targeted** — Blade Dancer readies the unit before the
spell choosing it does anything to it.

## 138. And the limit on the exception, also uncited

> **355.10.d.1.** This exception applies solely to objects for which no choice is ever possible.
> **355.10.d.2.** This exception does not apply to objects that are the only valid choice at the moment a
> spell or ability is placed on the chain, but which would require a choice under other circumstances.
> e.g., “Kill a unit at a battlefield” always targets a unit, even if that unit is the only unit currently
> at a battlefield.

355.10.d is cited 151 times, 355.10.d.1 **once** and 355.10.d.2 **never** — so the catalogue leans hard on
an exception whose two boundary clauses it has not read. 355.10.d.2 is the one that bites: **a lone enemy
unit is still a TARGET**, because the same instruction would require a choice on a fuller board. That is why "Ready your legend"
targets nothing (355.10.d's own example: you can only ever have one) and why a removal spell aimed at the
opponent's single body still pays its [Deflect]. Any future claim that something "chooses nothing because
there was only one option" has to clear 355.10.d.2 first.

## 139. Validation

Explicit list split by source: 8 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 2
card passages against `data/corpus_flat.txt`, zero failures. All rule references resolve. Counts run in the
turn they were written: the 39 / 37 / 2 buffer split over `data/cards.json` folded by name, the zero
citations for all four 383.4.b clauses and for 355.10.d.2, and 355.10.d's 151 and 702.2.a's 83, over
`data/combos.json` plus `data/synergies.json`. The two entry quotations were read out of `data/combos.json`
in the same turn. No entry staged; `data/combos.json` not written.

---

# Batch 30 — the win is checked as TASK 1 of every Cleanup, and rule 323's first line has never been cited

No entry. Found with a third probe, `.scratch/mid-orphan.mjs` (gitignored), which is the orphan-neighbour
method CLAUDE.md records for 815.3 and 813.4, run over 300–499: an **uncited** heading whose parent or best
sibling is cited fifteen or more times. It reports **158** rows, and the top one is sitting under a parent
cited **864 times**. Catalogue **762**.

## 140. 323.1

> **323.** When a Cleanup occurs, the following Tasks become Outstanding in the order described:
> **323.1.** 1. If a player has points greater than or equal to the Victory Score, and more points than any
> opponent, that player wins.

**Cited zero.** Rule 323 is cited 857 times in this catalogue and 323.6 alone 277; **the first task in its
list has never been opened.** Two things follow, and both apply to every one of the catalogue's finishers.

**(1) The win check runs BEFORE everything else in the Cleanup.** 323.2 (designations), 323.3 *"Handle
outstanding board state"* — also uncited — with 323.4 (Deathknells noted) and 323.5 (bodies to the trash),
323.6 (losing control of a battlefield your last body left), 323.9 (marking a Combat Staged), 323.11,
323.12, 323.13: **all of them are numbered after the win.** So once the points are on the board, **nothing
later in that Cleanup can take the win away.** A BURST that empties its own board wins before 323.4 and
323.5 process a single death; `bottled-constellation-time-warp`'s *"kill 3 other friendly units and/or gear
to score 1 point"* wins on the eighth point before those three corpses are handled. An evacuation line that
takes its last point from 348.2.a wins before 323.6 could strip the battlefield. A line that leaves a body
standing in front of an enemy garrison wins before 323.9 could stage the Combat. **None of the catalogue's
26 ALT_WIN entries or 20 BURSTs says this, and all of them rely on it** (class counts run over
`data/combos.json` at 762: INFINITE 14, BURST 20, CHAIN 16, ALT_WIN 26, ENGINE 686 — the "18" in an
earlier draft of this line was carried from CLAUDE.md instead of measured, which is the failure this lane
keeps naming, caught by running the count in the same turn as the commit).

**(2) 323.1 states 194.2's two-part test verbatim, as a Cleanup task.** CLAUDE.md carries the correction
that 194.2 is *not* "points ≥ Victory Score" but *"greater than or equal to the Victory Score **AND MORE
POINTS THAN ANY OTHER PLAYER**"*, with 194.2.b keeping tied players in the game — a correction made because
the shorthand had spread. 323.1 repeats the full test in the place where it is actually applied, which is
the sentence a reader checking a finisher would reach for. 194.2 is cited 26 times, 194.2.b **once**, and
323.1 never.

**The clean statement for a finisher's `terminatesIn`: the points win at the next Cleanup's first task, and
every other consequence of the turn is numbered behind it.**

## 141. Two companions from the same probe, both the uncited half of a rule the catalogue already uses

> **323.9.a.** The Combat remains Staged at that Battlefield as long as there are Units present from two
> opposing players there.
> **461.2.** If Staged Combats stop being Staged before the Steps of Combat are initiated, they are not
> resolved or executed.

Both cited zero, against 323.10's 21 and 461's 172. CLAUDE.md's #58 finding — the third evacuation route,
where a garrison walks off after a Combat is Staged and *"323.10 … the Combat will cease being Staged"* —
stands on 323.10 alone. **323.9.a is its precondition** (staging persists exactly while both sides are
present, so removing one side is what un-stages it) and **461.2 is its consequence** (*"they are not
resolved or executed"*). The catalogue has the middle sentence of a three-sentence argument. 461.1, cited
14 times, is the fourth: with several staged Combats the Turn Player picks the order, which is what lets an
evacuation be timed against one of them.

## 142. What the orphan probe says about this range as a whole

158 uncited headings sit beside a neighbour cited fifteen or more times, and the distribution is
instructive rather than alarming: the top of the list is **procedural scaffolding whose parent is cited for
a different sentence** — 464.2.a, 466.2, 466.4, 466.6 and 315.1.a are all *"The following Task becomes
Outstanding"* lines under blocks cited hundreds of times, and 485.2 and 485.6 are format parameters beside
485.4's setup numbers. Those are correctly uncited. What the probe is good for is the handful of rows where
the uncited sentence carries a **rule** rather than a step marker, and this batch is three of them; §135's
383.4.b (parent cited 528) was a fourth, found the same way. **The method's yield is high and its precision
is low: read the text, never the rank.**

Two rows it surfaced that this lane has already dispositioned, recorded so the next reader does not re-open
them: 355.13, 355.15, 355.16 and 355.17 (§85, §86) and 434.2, 434.3, 434.5 (§80, §82).

## 143. Validation

Explicit list split by source: 6 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 1
card passage against `data/corpus_flat.txt`, zero failures. All rule references resolve. Counts run in the
turn they were written: 323's 857, 323.6's 277, 323.1's zero, 194.2's 26 and 194.2.b's one, 323.10's 21,
461's 172, 461.1's 14 and 461.2's zero, over `data/combos.json` plus `data/synergies.json`. No entry staged;
`data/combos.json` not written.

---

# Batch 31 — three orphans that each carry a rule: who resolves first, whether a dodge lasts, and what a Recall moves

No entry. Three more rows from `.scratch/mid-orphan.mjs`, chosen by reading the text rather than the rank
(§142). Catalogue **762**.

## 144. 383.3.d.1 — THE NON-TURN PLAYER'S SIMULTANEOUS TRIGGERS RESOLVE FIRST, EVERYWHERE

> **383.3.d.** If more than one Triggered Ability is Triggered simultaneously, then the player that controls
> the Abilities selects the order to place them on the Chain.
> **383.3.d.1.** If multiple players separately control Triggered Abilities that are Triggered
> simultaneously, then starting with the Turn Player and proceeding in Turn Order, each player orders their
> Triggered Abilities on the Chain.

383.3.d is cited 75 times and **383.3.d.1 zero.** The parent settles ordering among *your own* triggers, and
the catalogue uses it constantly (*"a trigger you want to resolve FIRST must be placed LAST"*). The child
settles the cross-player case and nobody has read it: **the Turn Player places first**, and 340.1 resolves
the newest Finalized item first, so **the non-turn player's simultaneous triggers resolve BEFORE the turn
player's.**

CLAUDE.md has exactly one instance of this, scoped to combat: *"464.2.e.1 puts the Defender's triggers on
the Combat Chain last, which by 340.1 makes them resolve FIRST."* **383.3.d.1 is the general rule of which
that is a special case**, and it reaches every simultaneous event the two players share — a board wipe
killing bodies on both sides, a symmetric spell such as `OGN-209 Cull the Weak` or `OGN-179 Acceptable
Losses`, a Cleanup that kills across the table. In all of them **the opponent's Deathknells resolve before
yours**, which decides who gets to react to what and, where the two chains interact, which one sees the
board the other left. 303.2.a says the same thing for Game Actions rather than triggers (§99), and the two
together mean **turn order, starting with the Turn Player, is the universal tiebreak** — placement order
for triggers, execution order for actions, and both are inverted by 340.1 when they resolve.

## 145. 359.3.e.3 — A DODGE ONLY WORKS IF IT LASTS, AND A BOUNCE IS THE ONLY ONE THAT DOES

> **359.3.e.3.** If a target ceases to meet the targeting requirements while the spell is on the chain, then
> meets them again, it's a legal target.
> Example: A spell targets "a unit at a battlefield." A player reacts with a spell that moves the unit to
> base, then another player reacts with a spell that moves it back to that battlefield, then the original
> spell resolves. The unit is a legal target.

Uncited, under a block cited 155 times, and 359.3.e.2 (the "ceases to meet" half) and 359.3.e.4 (the
zone-change half) are both in use here. The missing middle is the one that says **validity is re-checked at
resolution and not latched at the moment it broke.** Two protections that this catalogue treats as
interchangeable are therefore not:

- **a MOVE-based save** — `OGS-011 Flash` ([Reaction], Chaos, E2, *"Move up to 2 friendly units to base"*)
  or `SFD-043 Emperor's Divide` — only works **if the body stays away until the spell resolves**. Anything
  that puts it back re-legalises the target, and 359.3.e.3's own example is that exact two-spell sequence.
- **a BOUNCE to hand** is permanent, by the very next clause: 359.3.e.4, *"If a target changes Zones to or
  from a Non-Board Zone and then returns to its original zone, it is no longer a legal target, because it's
  not treated as the same object."*

So the hand is a one-way door for targeting and the base is not. That is the same identity rule §45 used
from the other side — a bounce ends a delayed *"would die"* shield because the body that returns is a
different object — and it cuts the opposite way here: **the property that costs you a shield is the property
that makes a bounce the better dodge.**

## 146. 456.2 — A RECALL DOES CHANGE LOCATION, WHICH IS WHY A RECALL-SHIELD SWITCHES TEXT OFF

> **456.** Recalls are not Moves.
> **456.1.** They do not cause Triggered Abilities to trigger that are triggered by Move actions.
> **456.2.** A Recall causes a Permanent to change locations.
> **456.3.** A Recall cannot be prevented by actions and Game Effects that restrict or block Movement.

456 is cited 81 times, 456.1 and 456.3 are in use, and **456.2 — the only affirmative sentence in the block
— is cited zero.** This catalogue's recall-shield finding (*"a heal/exhaust/recall shield ERASES the
Deathknell … and a Recall goes to the BASE, so it switches off any 'While I'm at a battlefield' text"*,
`SFD-060 Tianna Crownguard` being the worked case) is sourced to **455**, which defines a Recall as a
relocation to the Base. 455 says where it goes; **456.2 says that the location CHANGES**, which is the half
a *"While I'm at a battlefield"* clause actually reads. The block is built as three negatives around one
positive, and the project has cited the negatives.

## 147. Validation

Explicit list split by source: 8 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 2
card passages against `data/corpus_flat.txt`, zero failures. All rule references resolve. Counts run in the
turn they were written: 383.3.d's 75 against 383.3.d.1's zero, 359.3.e's 155 against 359.3.e.3's zero, and
456's 81 against 456.2's zero, over `data/combos.json` plus `data/synergies.json`. No entry staged;
`data/combos.json` not written.

---

## 148. HANDOFF — rc-walk-mid, batches 15–31, replacing §59 for anyone arriving after this point

§59 (H14–H19) describes the lane as it stood at batch 14 and is now out of date in its numbers and its
method. It is still worth reading for **H19**, which is the durable half. Everything else below supersedes
it.

### H20. Lane, issue, staging — unchanged

Issue **#187**, sub-rules **300–499 and nothing else** (rc-walk-blocks holds 100–299, rc-walk-fc 500–829).
Commits are `Refs #187`. Staging file `/tmp/rc-walks/rc-walk-mid.json`, **currently empty**, and it should
be trimmed by ID against `data/combos.json` before any validation.

### H21. The ledger is now an instrument, not a note

**Do not keep a debt list in context.** §102 records the lane getting it wrong in both directions in one
week — first carrying applied upgrades forward as debt (§43), then reporting applied ones as outstanding.
`.scratch/mid-ledger.mjs` (gitignored) takes `entry-id:rule` pairs, or `--file`, and prints
APPLIED / OUTSTANDING / NO SUCH ENTRY against `data/combos.json`, echoing its entry count. **Every ledger
line in a report is its output, pasted.**

### H22. What batches 15–31 produced, and the three probes that produced it

No entries; **seventeen findings batches**, because §71's measurement holds: every card the probes name in
this range is already catalogued, and what is left in 300–499 is structure. Three probes, all gitignored,
all echoing their parsed range on the first line, all taking **two separate arguments**:

| probe | what it finds | count at catalogue 763 |
|---|---|---|
| `.scratch/mid-probe.mjs 300 499` | uncited headings **with a worked Example** | 117, of which 34 name a pool card |
| `.scratch/mid-probe-wide.mjs 300 499` | uncited headings with a **mechanic verb and NO Example** — the complement | 135 |
| `.scratch/mid-orphan.mjs 300 499` | uncited headings whose **parent or best sibling** is cited 15+ times | 157 |

The wide probe produced §96 (the whole 401–406 ability process, twelve sub-rules at zero) and §107 (337.2).
The orphan probe produced §135 (383.4.b, under a parent cited 528) and §140 (323.1, under a parent cited
864). **Its precision is low and its yield is high — read the text, never the rank** (§142).

### H23. The five findings a successor should not re-derive

1. **§107 — 337.2.** *Unit, Gear, or an ability that Adds resources resolves immediately.* One sentence
   containing three facts the project had derived separately in 118 citations. Everything true of one third
   is true of the other two.
2. **§96–§97 — rules 401–406.** The ability's own play process. **You may decline a trigger's COST (404.2)
   and may not decline its CHOICE (402.4.b)**; neither removal is a counter (402.4.a, 404.2.a).
3. **§84 + §66 — the zone chain.** 365.1 default / 366.1 override / 368.1 classifier / 370.3 the
   replacement-specific one / 424.1.a.2 / and 384.2 with 385.1 and 355.10.a.1 for why a deck-dweller must
   be a passive. **The trash can carry a trigger, the deck can only carry a passive, the hand carries
   neither.**
4. **§135 — 383.4.b.3.** A *"when you choose"* trigger reads **targeting**, so the pool's two ALL-shaped
   buffers fire none of them. This corrected a false sentence in two shipped entries and in CLAUDE.md.
5. **§140 — 323.1.** The win is **task 1** of every Cleanup, ahead of deaths, control loss and combat
   staging, and it states 194.2's two-part test verbatim.

### H24. The three self-corrections, which are the method working

**§91 and §92** — true-but-incomplete citations found by re-reading my own batches (the general paragraph
cited, the specific one missed). **§123** — the mirror: a correct citation reasoned one step past what it
licenses, caught within the hour, withdrawn before the manager promoted it. **§30 fix** — a class count
carried from CLAUDE.md instead of run. The standing checks that catch these: budget an H19 pass every third
or fourth batch; ask of any timing rule **"does this describe an instant or a loop?"**; finish a
neighbouring block before skipping to a new one (§125); and run every count in the same turn as the
sentence that quotes it.

### H25. Method, unchanged from H18 except in two places

Quotes from an **explicit list split by source file**, rules against the rules and cards against
`corpus_flat.txt` — this caught a mistyped 355.10.d.2 in §138 before it shipped. Rule references with the
`^[[:space:]]*` anchor via `.scratch/mid-b15-check.mjs`; **its lookbehind now excludes a leading hyphen**,
because `OGN-269` and `SFD-221` were being read as rule numbers, and note that a three-digit **citation
count in prose** still trips it (857, 864) and is a false positive by design. Scratch probes in `.scratch/`,
never `/tmp`. A census is run, never typed.

### H26. The exact next rows

1. **The rest of the orphan probe's rule-carrying rows**, which is where the yield now is: the **486 block** (the 1v1
   MATCH — best of 3, 486.6, and 486.5.a's battlefield reuse, all uncited under a parent cited 107, and
   distinct from 485's Duel), `383.4.c.3` and `383.4.d.3` (the sentences that NAME Conquer Effects and Hold
   Effects, uncited while the catalogue uses both terms constantly), `310.4`, `317.2.a`–`317.2.f` (the
   Ending Phase's Special Cleanup loop), `429.5`, `417.2`, `417.4`, `417.7`.
2. **The wide probe's remaining rows**: `428.2.a`/`428.2.b` (Kill is not a subset of Move; only a board
   origin counts), `438.4`/`438.6` (Replace against Banishment), `436.3.a`/`436.4`, `415.1.a`, `433.2.a`,
   `387.1.b`, `388.2`, `401.2`, `402.2`, `405.1`, `355.10.b`, `355.10.c`.
3. **A second H19 pass** over batches 21–31, which is due: the last one was §91–§94 over 15–19.

---

# Batch 32 — 81 entries cite the first half of 486.5 and none has read the second

No entry. One wide correction to a caveat the catalogue repeats on eighty-one entries, from the orphan
probe's 486 block. Catalogue **763**.

## 149. THE SENTENCE, BOTH HALVES

> **485.5.** Setup: Each player **randomly selects** one (1) of their three (3) Battlefields. The other two
> are removed and will not be used for this game.
> **486.5.** Setup: Each player **selects** one (1) of their three (3) Battlefields. The other two are set
> aside and will not be used for this round of play. The selected Battlefields are placed simultaneously in
> the Battlefield Zone. **After this game, if a player won, the Battlefields that were used are to be
> removed and not selected again for this Match. One of the remaining Battlefields that were set aside must
> be chosen instead.**

**486.5 is cited by 81 entries and 486.5.a, 486.6, 486.4, 486.4.a and 486.3 by none.** CLAUDE.md records the
difference the catalogue uses — *"485.5 and 487.5 say 'randomly selects', while 486.5 says only 'selects'"*
— and the standing caveat on a battlefield-dependent line is *"485.4.a, 485.5, 486.5 and 103.4.c as for
every battlefield entry"*, glossed everywhere as **one in three, at random**. **The second half of 486.5
says something different, and 486.6 spells it out:**

> **486.6.** Format: Best of 3. The first player to reach the Victory Score in Points wins the game. The
> winner of that game earns One Game Win. Players then reset the game state, **remove the Battlefields in
> play from the game, choose new Battlefields from those set aside**, and play again. The first player to
> earn Two Game Wins wins the match.

## 150. WHAT IT ACTUALLY MEANS FOR A BATTLEFIELD-DEPENDENT LINE

**In a Duel (485) it is one in three, at random. In a Match (486) it is once per match, on demand.** Those
are different enough that the shared caveat is misleading in both directions:

- **Game 1 is a free choice.** 486.5 says *"selects"*, with no randomness, so a combo that needs
  `OGN-293 The Grand Plaza` or `SFD-214 Power Nexus` has it in game 1 whenever you want it. The
  one-in-three discount does not apply at all. This is **strictly better** than the caveat says.
- **Games 2 and 3 cannot repeat it.** *"the Battlefields that were used are to be removed and not selected
  again for this Match"* — so after a decisive game the line is switched off for the rest of the match, and
  a deck built around one battlefield is a deck that can execute in exactly **one of three games**. This is
  **strictly worse** than "one in three at random", because random draws are independent and this is a
  removal.
- **486.5.a is the only relief:** *"If no player won a game, the battlefields presented for that game may be
  reused in a subsequent game."* A draw returns them.
- **486.6.a / 486.6.a.1 / 486.6.a.2**, all uncited, are the best-of-5 variant: a removed battlefield may be
  presented again in games 4 and 5, but only once you have presented **each** of yours at least once, and
  never more than twice in a match.

And 103.4.c is why the obvious dodge fails: you cannot bring three copies of the same battlefield, because
a deck *"cannot include more than one of a Battlefield of the same name"*.

**So the honest caveat for a battlefield-dependent entry is two sentences, not one:** *"485.5 makes it one
of three at random in a Duel; 486.5 with 486.6 makes it a free choice in game 1 of a Match and then removes
it for the rest of the match, so the line executes in at most one game of three (486.5.a returns it only on
a draw)."* That is a materially better deckbuilding picture than the current gloss, and it is available to
every one of the 81 entries for the price of reading to the end of a sentence they already cite.

## 151. Two more from the same block, and a note on why this is not an upgrade to file 81 times

**486.3** (*"Victory Score: 8"*) and **486.4** (*"Battlefield Count: 2"*) are uncited and confirm that a
Match game is a Duel in every parameter except the battlefield rotation and the best-of-3 wrapper — so
nothing else in a finisher's arithmetic moves. **486.7** and **485.7** are the identical sentence (*"The
player going second channels an extra Rune"*), which is worth knowing because the turn-clock model built
for #200 prices the slower seat from 485.7 and needs no adjustment for Matches.

This is a **fact about the format**, not about any card set, and it belongs wherever CLAUDE.md states the
battlefield caveat rather than as eighty-one separate notables. **UPGRADE, FLAGGED ONCE**, at that level.

## 152. Validation

Explicit list split by source: 7 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt`, zero
card passages, zero failures. All rule references resolve. Counts run in the turn they were written: 81
entries citing 486.5 and 157 citing 485.5, and zero for 486.3, 486.4, 486.4.a, 486.5.a, 486.6 and 486.7,
over `data/combos.json` plus `data/synergies.json`. No entry staged; `data/combos.json` not written.

---

# Batch 33 — a point lock stops the point and nothing else, and the paragraph that fires a legend's Conquer trigger is uncited

No entry. Two findings from the 383.4.c / 383.4.d block, where the catalogue cites the parents 72 and 91
times and has left the boundary clauses alone. Catalogue **763**.

## 153. 383.4.c.2.c — A CONQUER EFFECT IS KEYED ON THE CONQUER, NOT ON THE POINT

> **383.4.c.2.c.** If the act of gaining one point from Conquering is negated or replaced in any way, the
> Conquer Effect will still trigger.
> **383.4.d.2.c.** If the act of gaining one point from Holding is negated or replaced in any way, the Hold
> Effect will still trigger.

Cited **three** times and **twice**, both only inside conquer-engine arithmetic
(`nasus-ascended-brambleback-conquer`). The place they actually bite is the denial lens, and nobody has
taken them there.

`SFD-060 Tianna Crownguard` (Calm, E7 P2 M4, [Deflect]) reads *"While I'm at a battlefield, opponents can't
gain points"*, and the catalogue runs it in **four** entries — `tianna-wildclaw-point-lock`,
`warden-tianna-base-lock`, `tianna-guardian-angel-forge-god-lock`,
`counter-strike-tianna-crownguard-in-place-save`. **None of the four says what 383.4.c.2.c says: the
opponent still conquers, and every Conquer Effect they control still fires.** Tianna negates the *gaining
of one point* and 383.4.c.2.c is written for exactly that word. So a point lock is not a Conquer lock —
the opponent still takes the battlefield (348.2.a / 466.5), still fires `UNL-187 Piltover Enforcer`'s ready,
still fires `SFD-187 Void Burrower`'s dig, still replaces a battlefield with `UNL-195 Green Father`'s Brush
— and with 383.4.d.2.c the same is true of a Hold. **UPGRADE, FLAGGED ONCE**, on all four: a lock on points
is not a lock on triggers, and the entries should say which one they are selling.

## 154. 383.4.c.2.b AND 383.4.d.2.b — WHY A LEGEND'S "WHEN YOU CONQUER" FIRES AT ALL

> **383.4.c.2.b.** The Conquer Abilities of anything that references the player Conquering is put on the
> Chain as a Pending Item when the Condition that the player that controls the triggering source has
> performed a Conquer and gained 1 Victory Point.

**Cited zero** (its Hold twin 383.4.d.2.b has ten, its Unit sibling 383.4.c.2.a forty-two). It is the second
of the two halves 383.4.c.2 names: a Conquer Effect is *"triggered from Units that were present during the
Conquer action, **or Abilities that reference the player that performed the Conquer action**"*. The first
half needs a body at the battlefield; **the second half needs only the player**, which is the whole reason
a LEGEND can carry *"When you conquer"* despite 107.4.b putting the Legend Zone outside every location.

Swept over `data/cards.json`: five legends print a conquer clause — `OGS-023 Might of Demacia`,
`SFD-187 Void Burrower`, `UNL-187 Piltover Enforcer`, `UNL-195 Green Father`, `UNL-199 Deceiver`. CLAUDE.md
records the negative half of their story from #47 — *"a legend is not at a location (107.4.b), so
Reckoner's Arena's 'here' and Svellsongur's attach never reach a legend's own 'When you conquer' text — the
whole 'legend + Arena' family is dead on arrival"* — **and 383.4.c.2.b is the positive half that makes the
negative one interesting.** The legend's trigger fires because it references the PLAYER, and it cannot be
doubled by anything keyed to a LOCATION, because it was never at one. Stated together, that is one fact
rather than two unrelated ones, and it explains the shape instead of just recording it.

## 155. And the sentences that name the terms this catalogue uses everywhere

> **383.4.c.3.** These Triggered Abilities can be referred to as Conquer Effects.
> **383.4.d.3.** These Triggered Abilities can be referred to as Hold Effects.

Both cited zero. "Conquer Effect" and "Hold Effect" are terms this project uses in hundreds of places and
that `SFD-030 Skyfall of Areion` prints on the card — *"My hold effects are also conquer effects, and vice
versa"* — so the card is reading a defined term, and these are the two lines that define it. With 823.1.b
(*"Hunt is both a Conquer and a Hold effect"*, already cited) the vocabulary is closed. One citation each
wherever Skyfall is walked; no other consequence.

## 156. Validation

Explicit list split by source: 6 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt` and 2
card passages against `data/corpus_flat.txt`, zero failures. All rule references resolve. Counts run in the
turn they were written: 383.4.c's 72 and 383.4.d's 91 against 383.4.c.2.b's zero, 383.4.c.3's zero,
383.4.d.3's zero, 383.4.c.2.c's three and 383.4.d.2.c's two; the four `SFD-060` entries and their lack of
the citation; and the five conquer-clause legends folded by name over `data/cards.json`. No entry staged;
`data/combos.json` not written.

---

# Batch 34 — the second H19 pass, over batches 21–33, and it found three in my own work

No entry. §H19 says to budget this every third or fourth batch; the last was §91–§94 over batches 15–19, so
this is overdue rather than optional. Three catches, all in sentences I wrote, none of which any validator
could see. Catalogue **763**.

## 157. SELF-CATCH THREE — §140 said "every Cleanup" and did not cite the paragraph that makes that true

§140's whole force is that the win check is **task 1 of every Cleanup**, so nothing later in the Cleanup can
take a win away. A reader can immediately ask whether a **Special** Cleanup — the Combat Cleanup, the End of
Turn Cleanup — has the same step 1. I asserted it and cited nothing.

> **324.** Special Cleanups are Cleanup steps invoked at specific times that have **additional steps not
> present in a normal Cleanup**.
> **324.1.** When a Special Cleanup is invoked, the unique steps added will be inserted and defined by the
> sub-section that invokes it.

**A Special Cleanup is a normal Cleanup PLUS insertions**, which is why 466.1.a.1 and 317.2.b both read
*"Insert '3c. Heal all Units.'"* — they add a step to a list that already has 323.1 at the top. So §140
stands and it needed 324 to stand on. **324 and 324.1 are cited zero in the catalogue**; CLAUDE.md quotes
324.2 and no entry does. **UPGRADE, FLAGGED ONCE**, wherever §140's win-timing lands.

## 158. SELF-CATCH FOUR — §96 listed the ability block's contents and left out 405.2, which hides a third failure mode

§96 enumerated rules 401–406 and named twelve uncited sub-rules. **405.2 was not among them**, and it is
the one that changes a conclusion:

> **405.1.** Ensure that the Ability's targets are still legal.
> **405.2.** Ensure that the Ability's effect would not create an illegal state. If it would, resolve in the
> same way you would resolve a Card that creates an illegal state.

§93 set out **two** failure modes of a play and called them opposite — a play that fails legality is
**undone** and costs nothing (358.5), a play that is **countered** costs everything (425.1.c). The ability
process has a third shape: **its legality step prints no undo clause at all.** 358 has five checks and
358.5's undo; 405 has two checks and, for the illegal-state case, a **cross-reference** — *"resolve in the
same way you would resolve a Card"* — which sends the reader back to rule 349 rather than answering in
place. So the honest statement is that the ability block delegates its illegal-state handling to the card
block and says nothing of its own about targets that have gone illegal at 405.1, where 402.4 had already
handled the no-legal-options case at the CHOICE step. **§93's pair is a pair for cards; for abilities the
block is one clause shorter than it looks.**

## 159. SELF-CATCH FIVE — §113 presented a closed list of "statuses" and the rules do not supply one

§113 called 319.7 (*"After the status of any number of Game Objects changes for any reason"*) the widest of
the eight Cleanup triggers and listed five examples: *"Exhausting a unit, readying it, stunning it,
Empowering it, and applying Contested."* **The rules do not use "status" as a term of art with a closed
membership, and two of my five are called something else in their own paragraphs:**

> **414.2.** "Exhausted" is a **state** for Game Objects on the board that other game effects and rules can
> reference.
> **415.2.** "Ready" is a **state** for Game Objects on the board that other game effects and rules can
> reference.
> **441.2.** "Empowered" is a **state** … / **442.1.** Disempowering is the act of removing the Empowered
> **status** …
> **719.4.** The Exhausted and Ready **state** of the Top-Most card does not affect nor change the
> **status** of the Attached cards and vice versa.
> **719.4.a.** This is true of all **statuses** aside from location, Attached, and Top-Most.

So Empowered is called both within two paragraphs, 719.4 sets the two words against each other in one
sentence, and 719.4.a then counts *location, Attached and Top-Most* among the statuses — which is very
wide. **The conclusion of §113 survives** (319.7 is the widest trigger, and a Cleanup arrives for an exhaust
anyway through 319.1, 319.3, 319.4 and 319.6), **but the reason I gave was presented as a definition and is
not one.** The corrected form: *319.7 is the widest of the eight because "status" is used loosely and never
given a closed list — the rules apply it to Contested (190.3.a), Control (192.1), Stunned (423.1.a.2),
Empowered (442.1), Chosen Champion, and by 719.4.a to location, Attached and Top-Most.* **414.2, 415.2,
719.4 and 719.4.a are all cited zero.**

## 160. What this pass checked and did not find

- **§107 (337.2)** re-read against 330.2 and 185.2.a: a token unit played is still a *Unit* for 337.2, so
  the no-window rule covers it. Complete.
- **§135 (383.4.b.3)** re-read against 355.7 and 355.10.d: the chain is three paragraphs and all three are
  quoted. Complete.
- **§144 (383.3.d.1)** re-read against 340.1 and 464.2.e.1: the general/special relation is stated in both
  directions. Complete.
- **§149–§150 (486.5)** re-read against 103.4.c and 486.6.a: the best-of-5 exception is quoted rather than
  paraphrased. Complete.
- **§153 (Tianna)**: the one I most expected to break, because *"opponents can't gain points"* could be read
  as preventing the Conquer rather than the point. It holds, and 383.4.c.2.c is why — the paragraph is
  written for a point that is *"negated or replaced in any way"*, which presupposes the Conquer occurred.
  Recorded because the next reader will ask the same question.

**All three catches are the same two shapes this lane keeps producing**: a claim whose supporting paragraph
was never cited (§157), and a claim stated wider than the text licenses (§158, §159). Neither is visible to
a reference checker, a quote checker or a reviewer reading for truth, and both are visible to the author
asking *"which paragraph did I not cite, and did I use a word the rules define differently?"*

## 161. Validation

Explicit list split by source: 10 rules passages against `data/Riftbound-Core-Rules-2026-07-16.txt`, zero
card passages, zero failures. All rule references resolve. Citation counts for 324, 324.1, 324.2, 405.1,
405.2, 414.2, 415.2, 719.4 — all zero — were run in the turn they were written. No entry staged;
`data/combos.json` not written.
